# -*- coding: utf-8 -*-
"""SpeedC186 —— **c156 的学习副露阈值校准版**（claim_p 0.60 → 0.50）。

依据（2026-09-17，真机"被问到的窗口"上的阈值扫描；同窗配对、外部有效）：
  在 6,000 个真实窗口回放 c151（母本）与不同 claim_p 的 c156：

    | claim_p | 接受 | 相对母本 | 副露/轮估算(*) |
    |---|---|---|---|
    | 0.75 | 288 | 1.03x | 0.894 |
    | 0.70 | 304 | 1.09x | 0.944 |
    | 0.65 | 331 | 1.19x | 1.027 |
    | **0.60（现 c156）** | 347 | **1.24x** | **1.077** ← 略低于强者下界 |
    | 0.55 | 360 | 1.29x | 1.117 |
    | **0.50** | 372 | **1.33x** | **1.155** |
    | <=0.45 | 373~375 | 1.34x | 1.158~1.164 ← **饱和** |

  (*) 估算 = 0.866（我方真机副露/轮，field-audit）× 相对倍数；强者区间 **1.09~1.32**。

两条结论：
1. **c156(p=0.60) 其实还差一点**：1.077 落在强者下界（1.09）之下；
2. **这条轴天然有界**：阈值降到 0.45 以下**不再增加接受**（学习网络能补的窗口就那么多）
   ⇒ 即使推到饱和也只有 1.34x ⇒ 估算 1.16，**仍在强者区间内**，不存在"越放越滥"的无界风险。

本臂 = `speedc156` **唯一差别**：`claim_p=0.50`。仍然**绝不否决**基线的任何一次索取（单向增加，下行有界）。
判据与 c156 相同：150 房/臂 t>=1.50（组合臂口径），机制层看"有副露局占比"与 8 巡听牌率。
"""
from __future__ import annotations

from .speedc156 import SpeedC156

CLAIM_P = 0.50


class SpeedC186(SpeedC156):

    def __init__(self, name="speedc186", claim_p=CLAIM_P):
        super().__init__(name, claim_p=claim_p)

# -*- coding: utf-8 -*-
"""SpeedC131 —— `speedtugc` 的**单变量**候选：弃牌搜索里 `god_meld=False`。

唯一差异（一行）：`bot/speedtugc.py` 的弃牌调用改为 `god_meld=self.GOD_MELD`，
本类把它设为 `False` —— 即向听评估中**白板不得充当面子牌**，于是弃牌搜索倾向
**保留白板**、朝「4 组面子 + 白孤」这一**爆头形**（听牌态摸任意一张即胡）收敛。

⚠ **机制已被实测否证，不要据此花真机 A/B 的房（2026-09-15）**

本候选当初的假设是「`god_meld=False` ⇒ 弃牌搜索更倾向**保留白板** ⇒ 更容易走到爆头形」。
在 1,294 个真机「持白」出牌窗口上回放后：

| | 打掉白板次数 |
|---|---|
| `speedtugc`（GOD_MELD=True） | **2 / 1,294 = 0.2%** |
| `speedc131`（GOD_MELD=False） | **2 / 1,294 = 0.2%** |

两者在 **90 个窗口**改变了「打哪张」，但**没有任何一个窗口改变「是否打白」**
（`_best_discard_t` 的 `_pref()` 给白板最高保留权重 4，压过向听项）。
⇒ **`god_meld` 控制不了白的去留**，本候选的动机不成立；全量出牌决策也只有 **3.3%** 不同
（真机窗口回放 1,724 个决策：一致 96.7%）。
**结论：footprint 太小且机制不成立，不值得占用 A/B 的房间。** 保留代码作为记录（与
`speedc122` 同例），**默认不上线**。

仍然成立、可作为后续靶子的实测（见 `docs/iter/journal.md` R92）：
- 差距 **~91% 集中在「手里有白板」的局**；且**白板张数的分布两边几乎相同**
  （我方 48.9/38.7/11.1/1.3% vs 陶康 47.7/42.1/9.9/0.2%）。
- **逐档都差**：k=0 胜率 7.1%（前列 9.8–12.9%）、**k=1 34.5%（前列 37–49%）**、
  k=2 64.7%（前列 57–78%）。**单白局（占 39%）贡献了差距的 68%。**
"""
from __future__ import annotations

from .speedtugc import SpeedTUGC


class SpeedC131(SpeedTUGC):
    GOD_MELD = False

    def __init__(self, name="speedc131"):
        super().__init__(name)

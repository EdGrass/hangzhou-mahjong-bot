# 预登记 —— 四臂棋力战役（2026-09-17 00:0x 立；池子实验结束后立即执行）

> **为什么写在这里而不是临场决定**：本项目多次踩过"事后挑分桶/挑端点"的坑（§9.87 的 post-hoc 分桶、§9.87 被 §9.94 削弱）。
> 本文件是**开跑前**写下的臂定义、机制签名、失败处置与判据；开跑后**不得改**（只能加注）。

## 0. 背景（一句话）
`tools/vs_top.py`：与 top9 **同房 124 房** ⇒ 他们 **+98.6/房** vs 我们 **−28.8/房**、差 **+127.4（t=6.14）**、1st 率 44% vs 15%
⇒ **缺口是纯棋力**（同房/同池/同时刻，排除池子/座位/时刻）。而**池子杠杆只作用于自习房**、**不转移**到锦标赛（STATUS §9.102b）
⇒ **对外比赛唯一有效的动作是棋力**；本战役就是把队列里的三个棋力候选一次上齐。

## 1. 臂定义（**开跑后不改**）

| 臂 | 策略 | 相对基线改了什么 | 预登记类型 |
|---|---|---|---|
| A（基线） | `speedtugc` | — | baseline |
| B | `speedc151` | 压对数（更易形成爆头形） | bundle（阈值 t≥1.5 / 150 房） |
| C | `speedc156` | c151 + 学习到的选择性副露（`_want_claim`，阈值 0.60） | bundle |
| D | `speedc159` | c151 + 压对数**分时段**（河长 <12 → 0；12~28 → 1.0；>28 → 1.5） | bundle |

**启动命令**（池子实验停止后）：⛔**已被后续改臂作废 —— 最终臂集与命令见 §12**
```
python -X utf8 tools/ab_ctl.py start speedtugc,speedc151,speedc156,speedc159 --bundles=speedc151,speedc156,speedc159
```
并把 `var/.ab_mode.started` 写回 `2026-09-16 16:23:34`（复用 c151 的既存 13/12 房；**其余三臂没有更早的房**，不受影响）。

**为什么一次上三个**：一个基线臂服务三个候选 ⇒ 各 n 房共需 `4n` 房；顺序做三个两臂战役要 `6n` 房
⇒ **每候选省 1/3 墙钟**（n=150 时 600 房≈6 天 vs 900 房≈9 天；吞吐 4.4 房/h）。
N 臂通路已有单测：`tests/test_ab_mode.py` / `tests/test_ab_readout_multi.py` / `tests/test_ab_ctl_args.py`。

## 2. 主判据（**沿用既有规则，不新造**）
- 主端点 = **净胜/房（我方总分 − 三个对手均值）**，用 `tools/ab_readout.py`（多臂分支 `multi_arm_report`）。
- **采用**：`tools/ab_adopt.py` 的 `verdict`（bundle 臂：t ≥ 1.50 **且** 150 房/臂）。
- **无效收尾（futility，§4c-27 已实现）**：各臂 **≥100 房**且 **|t| < 0.60** ⇒ 判 **null**，立刻 `ab_ctl.py stop`，把房让给下一束。
- **中期只做"干预是否生效"，不做采用判断**（|t| 在 12 房时不可判）。
- ⚠ **灾难熔断的副作用**：`var/_ab_driver.guard_candidate` 是"候选 vs arms[0]、**整役一起停**"（−300/4 房 或 −150/12 房）
  ⇒ 一个臂崩坏会丢另外两臂的样本。可接受（阈值是灾难级），但**它不是终点判据**。

## 3. 机制签名（**快信号；每臂开跑后 12~20 房内必须看**）

| 臂 | 机制端点 | 基线（本战役窗口） | 预登记期望 | 可见样本 | **失败处置** |
|---|---|---|---|---|---|
| B `c151` | 拆对率（`ab_mechanism` 弃牌层） | ~3.0% | **≥ 8%** | 12~20 房 | 20 房后仍 <6% ⇒ **干预没生效，撤该臂** |
| B `c151` | 爆头占胡 / 对数签名 | 26.2% / 2.03 | 26~28% / ≤1.7 | 30~50 房 | 与分数冲突时**以分数为准**（本项只记账） |
| C **`c152`** | **弃后对数分布**（`pc`：0/1/2/≥3 的占比；`_c152_latency.py`/离线口径） | 平局约 1.95；`c151` 1.868 | **≤1.85 且 pc≥3 占比下降**（v3 链上的"压对数"信号） | 12~20 房 | 20 房后均值仍 ≥1.90 ⇒ **干预没生效，撤该臂** |
| C **`c152`** | **爆头占胡 / 胡率 / 均番 / 七对占胡**（门户口径 `tools/mech_mix.py`） | 26.8% / 25.2% / 1.36 / **2.8%** | 爆头占胡 ↑（链上机制）；胡率不降；均番 ≥1.33；**七对占胡 ≥1.8%** | 30~50 房 | 胡率降 >1.0pp、均番降 >0.05、或**七对占胡降 >1.0pp** ⇒ **v3 安全闸不过，不采用** |
| D `c159` | **逐巡拆对曲线**（门户口径） | T1 **9.0%** / 中盘 7.1~9.7% / T6 15.2% | **T1 → ≤5%** 且中盘抬升 | 12~20 房 | T1 仍 ≥7% ⇒ 撤该臂（早盘权重 0 没起作用） |
| D `c159` | 8 巡听牌率 | 43.8%（我方）/ 52.1%（强者） | ↑ | 30~50 房 | 不动 ⇒ 记账 |

**⚠ 本族共同的"大牌交换"风险（护栏已写进上表）**：`c151/c152/c159` 都靠**拆对/压对数**推进，而 `STATUS §4c-15/§9.x` 明确警告
"拆对会削弱**七对/豪华七对**"（占胡 3.35%、均番 3.24；**实测：七对占局每 1pp = +39.1 分/房**，SE 10.5、t=3.7、n=329 房）
⇒ 每臂读数**必须同时看 `七对占胡`**（`tools/mech_mix.py` 输出）：**若七对占比明显掉，就是"用大牌换速度"的净负交易**，
即便机制（pc 下降 / 拆对率上升）照设计移动，也不得采用。

**⚠ `c151` 臂的延迟折扣（生产实测，必须先写在读数口径里）**：bot 自记 `ms`（出牌决策）——
`speedtugc` p99 **271.5ms** / max 1290ms；**`speedc151` p99 1272.6ms / max 5599ms**（`speedc150` p99 936.6ms）。
⇒ `c151` 臂在真机上**可能因尾延迟丢窗口/超时**（3s 出牌预算已被 max 越过）。
**读数规则**：若 `c151` 的**机制照设计移动**但分数落后，**先查它的超时/丢窗口计数**再判策略差；
若 `c152`（生产未跑过，离线 p99 11.3ms）机制同样移动且分数不差，**优先采用 `c152`**（同族、快 ~90×）。

**撤单臂的规程（避免误操作）**：不要手改 `.ab_mode`；用
`python -X utf8 tools/ab_ctl.py start <保留的臂> --bundles=<保留的 bundle 臂>` 重起一役，并把 `.ab_mode.started` 写回**原值**
（这样被保留臂的历史样本继续计入），同时在读数里注明"该臂已按机制闸门撤除"。

## 4. 读数流程（每次读数照抄）

**第 0 步（新增，每次读数前必跑）**：`python -X utf8 tools/ab_integrity.py`
—— 检查"每条房只有一行、每房只属于一个臂、策略在本役臂内、全部 finished、exit_code=0"；
**不干净就先修数据再读数**（真实事故：房 `a_0aca4ad1f990` 曾有两行且策略不同 ⇒ 两臂各丢一房、t 从 0.60 虚高到 1.12）。

```
python -X utf8 tools/readout_bundle.py --fetch    # 补拉门户 + 主判据 + 机制/A级 + 零延迟两层 + 听牌端点（~12min）
python -X utf8 tools/mech_mix.py --arms speedtugc,speedc151,speedc156,speedc159 --since <战役 started>
python -X utf8 tools/vs_top.py                     # 同房头对头（不受桌强污染的进度尺）
```

## 5. 时间盒与外部约束
- 12 房/臂（≈12h）⇒ 首次机制读数（只判"干预是否生效"）；
- 100 房/臂（≈4 天）⇒ futility 检查；
- 150 房/臂（≈6 天）⇒ 终点判据（采用/null）；
- **硬约束**：战役进行中**不改**被引用的策略文件；跑重离线任务必须 `lower()` + 有界采样 + 跑完自查房延迟；
  **09-17 19:00~21:30 测试赛窗口**（A/B 停机、`var/.official_mode` 期间驱动自退）。

## 6. 开跑前的检查（照做）
1. `python -X utf8 tools/preflight.py` ⇒ READY；
2. `python -X utf8 var/_pool_signal_test.py --since "2026-09-16 23:19:47" --by-arm` ⇒ 池子实验已按预登记收尾（成立→启用/关闭→记录）；
3. 四臂策略在册且可实例化（2026-09-17 00:0x 已验证：`speedtugc/speedtugc_w50/speedc151/speedc156/speedc159/speedc158/speedc148/speedc153` 全部在册）；
4. 无 `_keeper` 残留（`Get-Process` 只应有 `_ab_driver`/`match_super`/`run_bot`）。

## 7. 【2026-09-17 00:4x 追加】副露线收口后的**队列修正**（本预登记的第 1~6 节不变）

**新增结论（STATUS §9.104(e)(g)(h)，均为本轮实测）**：
1. **权威覆盖率尺子**：我方 **61.7%** / 强者 **73.4%** 的局有副露（与既有 61.0%/74.5% 吻合 ✓）；
   副露/局 **0.866 vs 1.173** ⇒ 缺口 **11.7pp / 0.31 副露每局**（不是"一半"）。
2. **门控能拿的已经拿完**：对强者实际副露，`c151` 会要 85.1% → **`c156` 91.5%**（+6.4pp）。
3. **残差不可拿**：剩下 8.5% 集中在"已开口 + 中盘后 + 含白 + 伤进张"的窗口，但
   · 把候选阈值**全部放开**（c161 只留 `min_melds≥1`）天花板也只有 **+0.4pp**；
   · **同玩家同窗口类**的结局对照：他们**要了** 37.1% vs **过了** 35.1%（**+2.0pp，t≈0.8 不显著**）
     ⇒ 跨类的 +11.1pp 是**选局混杂**，不是"我们该跟他们要"。
4. ⇒ **副露门控线正式关闭**；由此：

| 候选 | 处置 |
|---|---|
| `speedc156` | **保留在四臂战役里**（该线最后一档，+6.4pp 已由门控口径证实） |
| `speedc158`（副露大束） | **降级为不作候选**（同一旋钮，边际已被上面 2/3/4 限死） |
| `speedc156c70`（退档） | 仅当 c156 **机制闸门不过**（有副露局占比 <65%）时才考虑 |
| `speedc160`（保白档）/ `speedc161`（第二三摊档） | **否掉**（离线预筛 +0.1pp / +0.4pp），保留代码+单测+注册作记录 |
| 之后的候选方向 | **不要再做副露门控类**；回到拆对/对数（c151/c159）与"机会数/评估函数"这两条**未证**线索（都不得直接上真机） |

**仍开放但未证（不得据此排臂）**：①"我方机会数是否真少"（跨玩家可比性未证）；②"评估函数（向听/白 语义）"是否该改
（本轮两个结局检验**都不支持**它有大价值）。

## 8. 【2026-09-17 00:4x 追加】读数与**采用规则 v3（功效感知）**——本战役按 v3 判读

见 `docs/STATUS-20260916.md` §9.105。要点：
- **不再用分数端点的显著性做采用闸**（150 房/臂 SE≈22.4 ⇒ 只能测到 ≥+24 的效果，而候选期望 +5~+15 ⇒ 结构上测不出）；
- 采用条件改为：**①机制按预登记方向移动 >2×SE ②该机制有已验证的因果分数链 ③高功效无害性闸**（胡率降 ≤1.0pp / 均番降 ≤0.05 / 爆头占胡降 ≤2pp / 分数 CI 下界 ≥ −60）；
- **已验证链路只有"爆头/C036 = +10.92 分/次"** ⇒ 本战役里 `speedc151`/`speedc159` 可走采用流程；
  `speedc156` 没有链路 ⇒ 即便分数为正也只能"无害并入束"，**不得单独采用**；
- 战役结束后：把通过成分打成**爆头束**跑 **≥400 房/臂** 的长样本（SE≈13.7）做最终确认。

## 9. 【2026-09-17 01:0x 追加】按 v3 换臂：**`speedc156` → `speedc152`**（同一族里的最优调度）

**依据（§9.105 v3 第 2 条）**：采用闸要求该机制有**已验证的因果分数链**。本役里只有
"**爆头 / C036 = +10.92 分/次**（898 次真机结局）"算已验证 ⇒ **`c151/c152/c159` 都在链上**；
**`c156`（副露覆盖）没有链**（§9.104(h)：跨类 +11.1pp 被证为选局混杂、同玩家同窗口 t≈0.8）
⇒ **换出 c156，换入 c152**。

**三个候选 = 同一旋钮（多对子惩罚 / 压对数）的三种调度** ⇒ 这一役同时做**模型选择**：

| 臂 | 调度 | 依据 |
|---|---|---|
| `speedc151` | 平奖励（`pc≤2` → 8） | 听牌率门槛（§4c-67） |
| **`speedc152`** | **分档** `W={0:12, 1:8, 2:2}` | **爆头形实测曲线**（pc=0 → 100% / 1 → 33.8% / 2 → 29.4% / 3 → 21.5% / 4 → 13.5%）⇒ **与爆头链对齐** |
| `speedc159` | 分时段（早 0 / 中 1.0 / 末 1.5） | 修 c151 的"早期过猛"（T1 9.0% vs 强者 2.3%） |

**离线预检（本节追加时实跑，两个工具）**：
- `tools/offline_replay.py --base speedc151 --cand speedc152 --phase draw --files 60`
  ⇒ 出牌决策 4,165 个：**同牌 87.2% / 不同牌 12.8%**、action 不同 **0%**、异常 **0** ✓（脚印远超 2% 门槛）；
- `var/_c152_latency.py`（新，150 个真机 draw 局面，`decide(view)` 口径，Idle 优先级）
  ⇒ **c152 p50 3.3ms / p99 11.3ms / max 12.0ms**；**同局面下 c151 p50 62.6ms / p99 1042.7ms / max 1214ms**；
    两臂同牌 134/150 = 89.3%（不一致 10.7%，与上面 12.8% 同量级 ✓）。
  ⚠ 含义：**c152 不只是"分档更贴爆头曲线"，它还比 c151 快 ~19 倍（p50）/ ~90 倍（p99）**
  ⇒ 在"延迟=丢机会"（§4c-21）的现实里，c152 是**同族里的更优调度**；c151 的 p99≈1.04s 虽仍在 3s 出牌预算内，
  但已接近 1s 的响应预算，**若 c151 在战役里出现超时/丢窗口，应优先看这条**。
- 既有记录（另一采样）：220 局面分歧率 15.5%、平均弃后对数 1.868（c151）→ **1.814（c152）**。

**启动命令（池子实验收尾后立即）**：⛔**已被后续改臂作废 —— 最终臂集与命令见 §12**
```
python -X utf8 tools/ab_ctl.py start speedtugc,speedc151,speedc152,speedc159 --bundles=speedc151,speedc152,speedc159
```
并把 `var/.ab_mode.started` 写回 `2026-09-16 16:23:34`（复用 c151 既存的 13/12 房；其余臂无更早样本）。

**⚠ 名次分/晋级口径（待今晚测试赛回填）**：`STATUS §9.59③` 实测 **名次分 ≠ 原始分**（逐房 r=+0.85）且**臂排序会不同**
（例：`speedtug` 原始分 +3.8/房 但名次分 +1.27、P(top2) 56.7%；`speedtugc` +13.6 / +1.02 / 48.7%）。
**若今晚测试赛确认"阶段按名次分/晋级线"**，则本役采用闸第 3 条追加 **`P(top2) 下降 ≤ 6pp`**，
并把 **P(top2)/名次分/房** 与 原始分/房 **并列记录**（`ab_readout.py` 已输出这三项）。
若阶段按累计原始分 ⇒ 保持现状（原始分为记录项、机制为采用闸）。

**v3 判读（本役输出）**：机制面 = **pc 分布 / 拆对率 / 爆头占胡 / 胡率 / 均番**；
采用取"**机制最对齐爆头链 + 安全闸通过**（胡率降 ≤1.0pp / 均番降 ≤0.05 / 爆头占胡降 ≤2pp / 分数 CI 下界 ≥ −60）"的那一臂。

**`c156` 的后续用途**：只在**长样本爆头束**里当"**无害并入项**"（先用被采用的 c152/c159 打底，
再单独测"已采用臂 + c156"的束），**不得单独采用**。

## 10. 【2026-09-17 01:0x 追加·最终臂集】按"同房缺口分解"重排：**baseline + c152 + c159 + c156**

**新证据（`var/_h2h_decompose.py`，1,061 场"我方与全榜 top10 同房"的事件流口径）**：

| 组 | 局 | **胡率** | **均番** | 爆头占胡 | 七对占胡 | 财飘占胡 | 杠开占胡 |
|---|---|---|---|---|---|---|---|
| 我方 | 8,480 | **23.2%** | **1.301** | 22.4% | 3.7% | **0.0%** | **0.4%** |
| **top10** | 8,880 | **28.6%** | **1.386** | **28.3%** | 2.5% | 1.1% | 2.0% |
| 其余 | 16,560 | 23.2% | 1.296 | 20.1% | 4.1% | 0.5% | 0.8% |

**缺口定价（实测斜率）**：
- **胡率 −5.4pp** ⇒ 按机械换算（1pp ≈ 11~16 分/房）= **−59~−86 分/房** ⇒ **占缺口一半以上**（⚠ 用我方历史回归的 27.4/pp 会得到 −148，**过高**：那个斜率把"赢得多"与"赢得大"混在一起）；
- **均番 −0.085** ⇒ 实测 **+377 分/房 每 +1.0 均番**（SE 68.5, t=5.5）⇒ **−32 分/房**；
- **爆头占胡 −5.9pp** ⇒ ≈ **−12 分/房**（+10.92 分/次 × 19 胡/房 × 5.9pp）；
- 财飘 −1.1pp、杠开 −1.6pp ⇒ ≈ −5~−7 分/房；**七对我们反而 +1.2pp（≈ +5 分/房，占优）**；
- 合计 ≈ **−100~−130** ✓ 与同房头对头实测的 +127 对得上。

**⇒ 两个词：胡率（一半）+ 均番（四分之一）+ 爆头（约 10%）。** 因此臂集定为：

| 臂 | 攻哪一项 | 机制签名（12~30 房判"是否生效"） | 主要风险（护栏） |
|---|---|---|---|
| **`speedc152`** | 爆头形/均番（**价值**侧） | 弃后对数 ≤1.85、pc≥3 占比↓ | 七对↓、均番↓ |
| **`speedc159`** | 同上（修早期过猛） | T1 拆对 ≤5% | 七对↓ |
| **`speedc156`** | **副露覆盖 → 胡率（缺口最大项）** | **有副露局占比 ≥65%**（基线 61.0~61.7%） | 番/爆头↓（speedtu 族教训：+2pp 胡率 −0.19 番 = 净负） |
| `speedtugc` | 基线 | — | — |

**⚠ 为什么砍掉 `c151`**：它与 `c152` 是**同一旋钮**，而 `c152` 是**优化过的调度**（平均弃后对数 1.814 vs 1.868、干预更少），
且 **c151 的生产尾延迟 p99 1272ms / max 5599ms**（§3 前）⇒ 保留它只是多花 1/4 墙钟换一个被支配的臂。

**启动命令（池子实验收尾后立即）**：⛔**已被后续改臂作废 —— 最终臂集与命令见 §12**
```
python -X utf8 tools/ab_ctl.py start speedtugc,speedc152,speedc159,speedc156 --bundles=speedc152,speedc159,speedc156
```
（`c151` 的既存 13 房**不再复用**：它属于被砍掉的臂；`started` 用**新的**当前时间戳即可。）

**v3 判读**：`c152/c159` 走"爆头链"（C036 = +10.92 分/次，已验证）；`c156` 走"**副露覆盖 → 胡率**"链
（同房同巡：有副露局 8 巡听牌 56.7% vs 门清 39.4%；speedtu 族实测 +0.53 副露/局 ⇒ +2.0pp 胡率），
**两条链都用四条安全闸兜底**（胡率/均番/爆头/七对）。

## 11. 【2026-09-17 01:1x 最终追加】**c162 保对子档（方向测试臂）** + 臂集最终定为两条路线对撞

**为什么加 c162（新证据与新冲突）**：
1. 出牌层对照（§9.106）：与强者弃牌**分歧 33.5%**，分歧时**他们留对子（pc 2.44）、我们拆对（pc 2.06）**；
2. **外生分桶**（起手对数 ← 随机发牌，600 场）：强者在**每个**起手对数桶都赢更多，且**对数越多赢率越高**
   （25.6% → 27.1% → **29.7%** → **30.8%**），而我方曲线更平（20.5/24.8/23.2/26.5/22.7）
   ⇒ **最大单桶差距在"2 对"（+6.5pp）**，而 2 对正是"七对/碰/门清"路线选择最关键的档；
3. 但 `§4c-67` 实测"**持对数与 8 巡听牌强负相关**"——这正是 c151/c152"压对数"的依据；而 §0★★★ 又证
   "**听牌速度与得分无单调关系**" ⇒ "为听牌而拆对"可能是在优化**代理指标**。
⇒ 两条证据不能同时为真 ⇒ **不再靠推理定方向，用真机对撞**。

**`bot/speedc162.py`（已实现）**：与 `c152` **同构**（主键仍是向听、进张、`_pref`、摸切），
**唯一差别 = 路线加成方向**：`c152 = W{0:12,1:8,2:2}`（越少对子越优）｜**`c162 = KP{0:0,1:2,2:8,3:12,4:12,5:12}`（越多对子越优，pc≥5 = 七对线给满）**。
- 离线脚印：与基线 **分歧 24.0%**（150 个真机 draw 局面）✓ 远超 2% 门槛；
- 延迟：p50 66ms / p99 1274ms（与 c152 同量级；**读数必须看超时/丢窗口**）。

**⇒ §11 当时的 4 臂（两条路线 + 副露）**：⛔**已由 §12 替换（c162 → c159；方向测试改为 c159 的分时段档）—— 最终命令见 §12**
```
python -X utf8 tools/ab_ctl.py start speedtugc,speedc152,speedc162,speedc156 --bundles=speedc152,speedc162,speedc156
```
| 臂 | 方向 | 机制签名（12~30 房） | 主要风险（安全闸） |
|---|---|---|---|
| `speedc152` | **对子 ↓**（爆头形/价值侧） | 弃后对数 ≤1.85、pc≥3 占比↓ | 胡率↓、七对↓ |
| **`speedc162`** | **对子 ↑**（保对子/碰/七对侧） | **弃后对数 ≥2.0、pc≥2 占比↑** | 番↓、胡率↓ |
| `speedc156` | **副露 ↑**（→ 胡率） | 有副露局占比 ≥65% | 番/爆头↓ |
| `speedtugc` | 基线 | — | — |

**判读**：三条都走 v3（机制 >2SE + 已验证链路 + 四条安全闸）。**这一役的价值一半在"方向"**：
若 c162 的**胡率/分数**明显优于 c152 ⇒ 说明"保对子/用对子换副露"是对的路（且推翻 c151/c152 的立论前提）；
反之则保留 c152 方向。`c159`（分时段压对数）**本轮不上**（与 c152 同向、信息增量低）。

## 12. 【2026-09-17 01:1x 最终】用**真机弃后对数**校准臂集与闸门（`var/pc_by_arm.py`，60k 弃牌）

**读数**（250 场，不变量通过 98.3%）：我方 **1.922 / 2.207 / 2.387**（1-4 / 5-8 / 9+ 巡）；
强者 **1.855 / 1.873 / 1.892**；其余 1.828 / 1.873 / 1.853
⇒ **我们攒对子、强者用掉对子；最大缺口在 5-8 巡（+0.33）与 9+ 巡（+0.50）**。

**⇒ 臂集（最终，替换掉 §11 的 c162）**：
```
**⚠ 交接顺序（2026-09-17 修：`ab_ctl stop` 原来只等 45s 就强杀驱动 ⇒ E002 窗口）**：
```powershell
python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc152,speedc159,speedc156 --n 20   # 上臂前冒烟（本役四臂已跑通 ✅）
python -X utf8 tools/ab_ctl.py stop      # 删哨兵 → 等驱动**自然**退出（现在最多 20 分钟；返回 2 = 还没到批次边界，继续等，别加 --force）
# 确认 _ab_driver.py 已不在再继续（keeper 被 watchdog 恢复也没关系，start 会先停它）
python -X utf8 tools/ab_ctl.py start speedtugc,speedc152,speedc159,speedc156 --bundles=speedc152,speedc159,speedc156
```
（强杀驱动**不会**杀掉在跑的 run_bot；watchdog 随后拉起的 keeper 会被幂等 `/api/match` 带回**同一房** ⇒ 第二个 run_bot = **E002**。）

```bash
 --bundles=speedc152,speedc159,speedc156
```
| 臂 | 机制闸门（**真机口径**） | 备注 |
|---|---|---|
| `speedc152` | **b5-8** ≤2.10、**b9+** ≤2.25（桶口径见 §9.130） | 全域压对数 |
| **`speedc159`** | **b1-4** ≤2.00 且 **b9+** ≤2.25（桶口径见 §9.130） | **分时段；直击 9+ 巡 +0.50 的最大缺口** |
| `speedc156` | 有副露局占比 ≥**65%** | 把对子"用掉"（→ 胡率那一半） |

**`c162`（保对子档）保留代码/单测/注册但不排期**：总量证据（我方中后盘对数偏高）与其方向相反。
§11 里"他们留对子"那条已降级：**只对"我们不同意的窗口"成立**（n=238/59），不能推翻 60k 弃牌的总量口径。

**★ 追加（01:1x）：闸门要加**分层**条件**（防"代理指标"陷阱）
`var/pc_by_arm.py` 已支持 `巡目|e0/e1/e2` 分层。实测：未分层差值（5-8 巡 +0.33 / 9+ 巡 +0.50）在分层后只剩 **+0.12~+0.24**
⇒ **约一半 pc 缺口是"我们副露少"的机械结果**（碰掉一对 ⇒ 暗手 pc 自然降）。
⇒ `c152`/`c159` 的闸门改为**两条都要**：①未分层（**b5-8** ≤2.10、**b9+** ≤2.25）②**e0 层（纯门清）b5-8 ≤2.60、b9+ ≤3.10**（基线 2.668 / 3.188）；
⇒ `c156`（副露臂）只看未分层均值下降 + 有副露局占比 ≥65%。

## 13. 【2026-09-17 01:1x】尺子检验 ⇒ **闸门换成 dec 口径**（`var/pc_from_dec.py`）

`var/_recon_ruler_check.py` 实测"事件流重建手牌"与 bot 自己的 dec 视图**只有 11.1% 完全一致**（差异恒为 1 张牌：
dec 在 draw 窗口**含刚摸牌**）⇒ **所有依赖重建的结论降级**（含 §11/§12 里"他们留对子""我们对子偏多"的对比、
以及 `_strong_meld_vs_gate` 的覆盖率/残差）。
✅ 可信的替代 = **只用我方 dec** 的 `var/pc_from_dec.py`（无重建）：

| 策略 | 1-4 巡 | 5-8 巡 | 9+ 巡 | e0 层 |
|---|---|---|---|---|
| `speedtugc`（基线） | 1.955 | 2.155 | 2.117 | 2.074 / 2.496 / 2.414 |
| `speedc151`（试点） | 1.887 | **1.777** | **1.713** | 2.012 / 2.138 / 2.041 |

⇒ **"压对数"机制在真机确实生效（c151 试点压低 0.34~0.40）**；"值不值得"仍由本役裁决。**闸门改为**：
| 臂 | 未分层 | e0 层 |
|---|---|---|
| `c152` | **b5-8** ≤2.05、**b9+** ≤2.05 | b5-8 ≤2.40 |
| `c159` | **b1-4** ≤1.95、**b9+** ≤2.05 | b9+ ≤2.40 |
| `c156` | 有副露局占比 ≥**65%**（事件口径） | — |

**臂集不变**（`speedtugc` + `c152` + `c159` + `c156`）：机制已由 c151 试点证实"能压低 pc"，
本役要回答的是 **pc 压低是否换来胡率/番/分数**（四条安全闸 + ab_readout）。

**★ 追加（2026-09-17 02:1x）闸门分辨率表（`var/pc_from_dec.py --compare <基线>`；输入取真实分布）
：pc SD=1.228（~72 决策/房）· 爆头 p=0.224（~19 胡/房）· 覆盖 p=0.617 · 胡率 p=0.242**

| 房/臂 → | 8 | 12 | 16 | 20 | 30 | 50 | 150 |
|---|---|---|---|---|---|---|---|
| pc（弃后对数） | — | **0.084** | — | **0.065** | 0.053 | 0.041 | 0.024 |
| 爆头占胡 | — | — | 6.8pp | — | **4.9pp** | 3.8pp | 2.2pp |
| 副露覆盖 | **5.4pp** | 4.4pp | — | 3.4pp | — | 2.2pp | — |
| 胡率 | — | — | — | 3.0pp | — | 1.9pp | **1.1pp** |

- **pc 闸门**：相对基线 −0.105 ⇒ **~12 房/臂可 2σ 判别** ✓（中期 12~20 房即可）。
- **副露闸门**：+3.3pp ⇒ 2σ 需 **~21 房/臂**。
- ⚠ **修正**：旧文写"爆头 2σ 辨 +4.8pp 只需 ~16 房/臂" —— **按 2σ 应为 ~32 房/臂**（16 房时 2σ=6.8pp）⇒ **爆头判读门限改为 ≥30 房/臂**。
- ⚠ **胡率安全闸**：1pp 级需 **150 房/臂**；**20~30 房时只能抓 ≥3~4pp 的大害**（中期别因"胡率没动"撤臂）。
- **c151 试点的 2σ 确认**（`pc_from_dec --arms speedtugc,speedc151 --compare speedtugc`）：1-4 巡 Δ−0.067±0.106（不显著）、**5-8 巡 −0.380±0.102**、**9+ 巡 −0.404±0.025**（两者 2σ 确认）✓

## 14. 【追加 2026-09-17 03:20】最终 4 臂：基线 + c159 + c156 + c159×c156 组合

**尺子**：`var/_recon_ruler_check.py` 修正后 30 个 dec / 6,879 条视图一致 **6,865 = 99.80%**。

**为什么组合臂值得占位**：
- 1,200 个强玩家出牌决策：强者弃后对数 **1.922**，`c159` **1.927**（几乎贴合），`c152` 1.849（压过头）⇒ 出牌路线选 c159。
- 500 场强玩家实际吃碰 6,292 次：质量门控覆盖 85.3%，`c156` 覆盖 **91.6%**；`c158` 只多 4 个窗口 ⇒ 副露补加选 c156。
- `c156` 的自然实验（同窗口“要了 vs 过了”）只有 **+2.3pp 胜率（SE≈2.4）**，说明它不能当稳赢臂；与 c159 的组合正是要检验“路线 + 副露”是否能叠加。
- `speedc163` = `SpeedC159` 的出牌钩子 + `SpeedC156` 的副露钩子；MRO 已实测：`_pick_discard ← c159`、`_want_claim ← c156`、C036 仍在链上。单测 5 项、真实局面冒烟通过。

**最终臂集（4 臂）**：
`speedtugc`（基线） + `speedc159`（分时段压对数） + `speedc156`（学习副露补加） + `speedc163`（组合臂）。

启动命令（池子实验自然收尾后）：
```powershell
python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc159,speedc156,speedc163 --n 20
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/ab_ctl.py start speedtugc,speedc159,speedc156,speedc163 --bundles=speedc159,speedc156,speedc163
```

预算：4 臂 × 150 房 = **600 房，约 6 天**。机制闸门：`c159/c163` 的 b1-4≤1.95、b9+≤2.05（e0 层 b9+≤2.40）；`c156/c163` 有副露局占比≥65%；安全闸仍用 v3 四条。若组合臂胜出，直接采用它；若两个主效应都正而组合不增，按交互不叠加记录。
## 15. 【2026-09-17 03:45 关键修正】正式赛 M=10 并发门禁 ⇒ 慢臂全部降级，改用快版 4 臂

**为什么这条压过前面的单线程延迟结论**：`bot/protocol.py::_play_concurrent` 在正式赛中是**一个 run_bot 进程内 10 个 play_game 线程共享同一策略实例**。单线程 `latency_bench` 不能代表锦标赛。

**M=10 并发实测（10 线程 × 50 个真实 draw 决策，预热 500；出牌预算 3s）**：
| 策略 | p50 | p95 | p99 | max | 超3s |
|---|---:|---:|---:|---:|---:|
| `speedtugc`（基线） | 1.6ms | 235ms | 429ms | 854ms | **0** |
| `speedc151` | 498ms | 5507ms | 10967ms | 15496ms | **52/500** |
| `speedc156` | 481ms | 5083ms | 9920ms | 13229ms | **46/500** |
| `speedc159`（预热后仍失败） | 116ms | 2296ms | 5176ms | 9484ms | **18/500** |
| `speedc167` K=4 | 3.7ms | 968ms | 2597ms | 4642ms | **3/500** |
| `speedc168` K=4+30ms 预算 | 64ms | 1131ms | 1884ms | 3826ms | **2/500** |
⇒ c150 全族的 `real_ukeire` 在 M=10 GIL 竞争下无法满足 3s 窗口；慢臂不能作为月底正式赛主线。

**快版门禁通过（同一 M=10 压测，warm=500）**：
| 策略 | draw p50/p95/max/超3s | window p50/p95/max/超1s |
|---|---|---|
| `speedc164` | 0.1ms / 0.3ms / 0ms / **0** | — |
| `speedc165` | 0.1ms / 0.1ms / 0ms / **0** | 0ms / 0ms / 3ms / **0** |
| `speedc166` | 0.1ms / 0.3ms / 0ms / **0** | 0ms / 0ms / 5ms / **0** |

**快版机制的离线核对**：
- `c165` 在真实 response_peng/chi 窗口上与全量 `c156` 一致 **99.6%**；差异仅 2/500。
- `c164` 的弃后平均对数 **1.925**，几乎贴合强者 **1.922**；但它与 c159 的具体出牌只有 43.3% 相同、强玩家分歧局对齐 17.8% ⇒ 它是**不同的低对数候选**，不是 c159 的等价替代。
- 因此快版必须走独立真机 A/B，不能把全量 c159/c156/c163 的结论直接移植。

**正式赛可用的最终快版 4 臂**：
`speedtugc`（基线） + `speedc164`（快版分时段压对数） + `speedc165`（快版学习副露） + `speedc166`（快版组合）。

启动命令（池子实验自然收尾后）：
```powershell
python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc164,speedc165,speedc166 --n 20
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/ab_ctl.py start speedtugc,speedc164,speedc165,speedc166 --bundles=speedc164,speedc165,speedc166
```

`c151/c159/c156/c163` 保留代码与自玩房实验记录，但**不作为月底正式赛采用目标**，除非后续证明能找到并发安全的等价实现。测试赛今晚仍用 `speedtugc`。

**附录（2026-09-17 04:00）**：`YouCaiBiKao=true` 分支的 `speedc148` / `speedc153` 也通过 M=10 并发门禁：draw p95≤0.3ms、max≤1ms，window max≤3ms，均为 **0 超时**。正式赛配置为 true 时切 `speedc148`（备 `speedc153`），不会落入“合法但超时”的两难。

## 16. 【2026-09-17 10:2x】YouCaiBiKao=true 专属快版合法臂：c169~c172（已过 M=10）

**目的**：c148/c153 合法但缺少 c164/c165/c166 的快速路线/副露增益；新增四条“快版 + 合法胡闸门”，避免月底 true 配置只能用较弱策略。

| 策略 | 组合 | M=10 draw | M=10 window |
|---|---|---:|---:|
| `speedc169` | c164 + YCBK 闸门 | p95 0.2ms / 0 超 3s | p95 0.0ms / 0 超 1s |
| `speedc170` | c165 + YCBK 闸门 | p95 0.1ms / 0 超 3s | p95 0.1ms / 0 超 1s |
| `speedc171` | c166 + YCBK 闸门 | p95 0.3ms / 0 超 3s | p95 0.1ms / 0 超 1s |
| `speedc172` | c171 + 被拦后优先转爆头 | p95 0.3ms / 0 超 3s | p95 0.1ms / 0 超 1s |

- 单测 `test_ycbk_fast.py` 6 项：平胡带白被拦、爆头放行、链放行、钩子组合正确、注册齐全。
- **月底 true 配置采用顺序**：主选 `speedc172`（最完整的合法 + 转爆头），备选 `speedc171`；c148/c153 保留为最小保险。
- `YouCaiBiKao=false` 仍走 fast 四臂战役 c164/c165/c166，不受影响。

## 17. 【2026-09-17 10:3x】fast 束下一层 c173~c176：加回 c144 明/自杠 + c141 财飘修正（全部过 M=10）

在 fast c164/c165/c166 之上加回 c150 束里两个**廉价、无需 real_ukeire** 的机制：
- `speedc173` = c164 + c144（明/自杠）+ c141（财飘链修正）
- `speedc174` = c165 + c144 + c141
- `speedc175` = c166 + c144 + c141（false 配置下一束主候选）
- `speedc176` = c175 + YouCaiBiKao 合法闸门 + 被拦后转爆头（true 配置终极候选）

M=10 10×50 warm=500：c173~c176 draw **全部 0/500 超 3s**；window **全部 0/500 超 1s**；真实 draw/window 冒烟通过；`test_fast_bundle.py` 通过。
采用顺序更新：true 配置主选 **c176**、备选 c172（c148/c153 兜底）；false 配置当前仍跑 c164/c165/c166 四臂，当前战役结束后下一束优先 `c175`，需要归因时再拆 c173/c174。

## 18. 【2026-09-17 10:4x】fast c136（c177~c179）**不排期**：轻量代理没有增益

- `speedc177` = fast c136（质量副露门控 + 轻量可见进张代理）；c178/c179 为叠加 c144/c141 或合法闸门的版本。
- 真实窗口对照（500 个 response 窗口）：
  - 严格基线 speedtugc：pass 482 / peng 13 / chi 5
  - full c136：pass 478 / peng 15 / chi 7
  - fast c165（学习门控）：pass 477 / peng 15 / chi 8
  - **speedc177/c178：pass 482 / peng 13 / chi 5**（与严格门控完全一致，没有多接受任何副露）
  - full c136 的 FIX1 真机窗口，fast c177 也错误拒绝。
- ⇒ 轻量进张代理过于保守，不能替代 full c136；c177/c178/c179 保留代码/注册作记录，**不排期**。
- true 配置主选仍为 **c176**（c166 fast 束 + c144/c141 + 合法闸门 + 转爆头）；false 下一束优先 c175。

## 19. 【2026-09-17 10:5x】BC 互补臂 c147 通过 M=10：下一阶段高优先级候选

**新测量**：
- `speedc073w4`（BC 弃牌模型，历史 32 房点估计 +7.4/room，未显著）M=10 draw：p95 0.1ms，**0/500 超 3s**。
- `speedc147` = c073w4 + c146（c144 明/自杠 + c136 质量副露 + c141 财飘）：
  - M=10 draw：p95 **0.2ms**、max 0ms、**0/500 超 3s**
  - M=10 window：p95 0.1ms、max 3ms、**0/500 超 1s**
  - 真实 draw/window 冒烟通过

**排期更新**：
当前 fast 四臂（c164/c165/c166）结束后，下一阶段候选优先级：
1. **`speedc147`**（BC 弃牌 + 杠/财飘/质量副露；独立机制，历史点估计正）
2. `speedc175`（fast combo + 杠/财飘）
3. 视当前战役结果决定是否拆 c173/c174
`YouCaiBiKao=true` 主选仍为 **c176**（备 c172）；c147/c175 仅适用 false 配置。

## 20. 【2026-09-17 11:0x】终极组合 c180/c181：BC + full c136 + c165 学习副露 + c144/c141

**组合**：
- `speedc180` = c165 学习副露 + c147（C073w4 BC 弃牌 + c136 质量副露 + c144 明/自杠 + c141 财飘）
- `speedc181` = c180 + YouCaiBiKao 合法闸门 + 被拦后转爆头

**验证**：
- M=10 draw：c180 p95 **7.2ms**、c181 p95 **8.4ms**，均 **0/500 超 3s**
- M=10 window：c180 p95 0.0ms、max 22ms；c181 p95 0.0ms、max 18ms，均 **0/500 超 1s**
- 真实 draw 100 局面：c180 产生 discard/hu/gang，全部成功；c181 同
- 真实 window 200 局面：两者成功；max ≤312ms（1s 窗口内）
- `test_ultimate_combo.py` 通过；rules_guard 动态识别 c181 为 gated。

**月底采用顺序（最终）**：
- false：下一阶段第一候选 **c180**（检查 BC + 全机制束），随后 c175（fast route + c144/c141），再 c147（BC + c136/c144/c141，c180 的子集）。
- true：主选 **c181**，备选 c176，再 c172（c148/c153 最小兜底）。

## 21. 【2026-09-17 11:2x】BC 臂优先级修正：下一阶段应做 c175 vs c180 vs c147 对撞

**真实 `decide()` 口径**（1,200 个强玩家 draw 决策；c180 走 BC `decide`，不是 `_pick_discard`）：
- 基线一致 63.5%；分歧 438。
- 分歧中对强者一致：c159 **31.1%**、c152 30.6%、c162 29.5%、c167 19.9%、**c164 17.8%**、**c180 14.8%**、pair-only 2.7%。
- 弃后平均对数：强者 1.922、c164 **1.925**、c180 **1.991**、c147（BC 弃牌）同 c180 弃牌口径应接近 c180。

⇒ BC/c147/c180 的**历史点估计可能为正，但真实决策与强者对齐和弃后对数都不如启发式 c164/c175**；不能把 c180 当第一候选。
**下一阶段 4 臂（当前 fast 战役结束后）**：
`speedtugc` 基线 + **`speedc175`**（fast route+meld+杠/财飘，主推）+ **`speedc180`**（BC+full c136+学习副露+杠/财飘）+ **`speedc147`**（BC 子集，归因 BC vs 学习副露）。
- 主采用优先级仍是 **c175**；c180/c147 作为 BC 机制对照，只有分数端点也显著更好才考虑采用。
- true 配置主选仍 c181（c180 + 合法闸门），备选 c176/c172。

**下一阶段启动命令（当前 fast 四臂结束后）**：
```powershell
python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc175,speedc180,speedc147 --n 100
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/ab_ctl.py start speedtugc,speedc175,speedc180,speedc147 --bundles=speedc175,speedc180,speedc147
```
三候选真实 draw/window 冒烟已通过；主采用优先级 c175，c180/c147 只作 BC 对照。

## 22. 【2026-09-17 11:4x】true 配置主选修正：c176 优于 c181（启发式 vs BC）

- `c176` = fake c175 + YCBK 闸门 + 转爆头 = c164 fast route + c165 学习副露 + c144/c141 + 合法闸门。
- `c181` = c180 + YCBK 闸门 = BC 弃牌 + c136/c165/c144/c141 + 合法闸门。
- 真实强玩家 `decide()` 对齐（1200 决策）：c164 17.8% > c180 14.8%；c175/c176 使用 c164 弃牌路线；c181 使用 c180 的 BC 弃牌路线。
⇒ 月底 true 配置主选改为 **c176**，备选 **c181**，再 c172；c148/c153 最小兜底。
false 配置下一阶段仍为 baseline + **c175（主推）** + c180 + c147。

## 23. 【2026-09-17 12:0x】终极启发式束 c183/c184（当前最强 heuristic）

- `speedc183` = **c164 fast route + c165 学习副露 + c136 full 质量副露 + c144 明/自杠 + c141 财飘**。
- `speedc184` = c183 + YouCaiBiKao 合法闸门 + 被拦后转爆头。
- M=10：c183/c184 draw p95 0.2ms、max≤1ms；window max 65.6/3.4ms，均 **0 超时**。
- 真实 draw/window 冒烟通过；`test_ultimate_heuristic.py` 4 项通过；c136 FIX1 在 c183 链上仍正确收下。
- 不含 BC，弃牌路线与 c164 一致（强玩家对齐 17.8% > BC c180 14.8%），同时补齐 c136 质量副露。

**最终月底映射**：
- false 下一阶段 4 臂：baseline + **c183（主推）** + c175（c183 去掉 c136 的归因臂） + c180（BC 对照）。
- true 主选 **c184**，备选 c176（c175+闸门），再 c181（BC+闸门）、c172；c148/c153 最小兜底。

## 24. 【2026-09-17 11:22 真实时钟】★真版解锁 ⇒ 战役改期为「真版四臂」（推翻 §15/§23 的前提）

**为什么推翻**：§15/§23 把月底主线定为"快版四臂 c164/c165/c166"，唯一理由是
**真版 c151/c156/c159 的 `real_ukeire` 尾延迟过不了 M=10**（52/46/18 超 3s）。
该尾延迟已修复（可证明精确的下界剪枝 + `_score` 内联，见 `docs/STATUS-20260916.md` 顶部 11:22 增量）：
- 等价性：35,700 条向听语料 / 1,070 条进张语料 / 400 条 c151 真实决策 **全部 0 差异**；全量回归 623 OK；
- M=10：c151 draw max=356ms、c156 326ms、c159 494ms、c152 1520ms —— **全 0 超时**。

⇒ 真版既能跑，就**不该再测代理**：c164 与 c159 出牌仅 43.3% 相同、强者分歧局对齐 17.8%（c159 31.1%）。

**本役设计（已启动 2026-09-17 11:22:11）**

| 臂 | 内容 | 角色 |
|---|---|---|
| `speedtugc` | 基线 | 对照（bundle 外，第 1 臂） |
| `speedc151` | 多对子惩罚（ROUTE_BONUS=8.0） | 家族母本 |
| `speedc156` | c151 **+ 学习选择性副露** | c151 的单变量臂 |
| `speedc159` | c151 **+ 分时段压对数** | c151 的单变量臂 |

- 启动命令（已执行，留档）：
  `python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc151,speedc156,speedc159 --n 100`
  （4 臂全过）→ `python -X utf8 tools/ab_ctl.py stop`（11:21:59 安全收尾，无 --force）
  → `python -X utf8 tools/ab_ctl.py start speedtugc,speedc151,speedc156,speedc159 --bundles=speedc151,speedc156,speedc159`
- 归因干净性：c156/c159 都只动 c151 的**一个**部件 ⇒ 任一臂胜出都能直接归因到"副露门控"或"时段调度"。

**判据（沿用 §1 的预登记，不改口径）**
- 采用：150 房/臂、t ≥ 1.50；futility：100 房/臂、|t| < 0.60。
- 机制闸门：每臂 ≥12 房跑
  `python -X utf8 var/pc_from_dec.py --arms speedtugc,speedc151,speedc156,speedc159 --compare speedtugc`
  看弃后对数分桶 b5-8 / b9+（目标贴近强者 1.922）；**c156 另看有副露局占比 ≥65%**（强者 73.4%）。
- stop 返回 2 = 未到房边界，继续等；**禁止 --force**。

**边界残留（读 integrity 时必看）**：旧役最后一房 `speedc164`（11:19:38 入场）会在新役
`since=11:22:11` 之后才落结果行 ⇒ `tools/ab_integrity.py` 会把它报成"未知策略/多一个 c164 房"。
这是**跨役残留，不是多行/多策略污染**，读数时手工剔除该房即可。

**自动化已同步**：heartbeat `fast4` 与一次性 cron `09-17-22-00` 都改成"当前真版四臂"，
22:00 复盘后重启的是 `speedtugc,speedc151,speedc156,speedc159`（不再启动旧快版三臂）。

**映射更新**
- `YouCaiBiKao=false` ⇒ 本月主线 = **c151 家族**（当前战役臂）。
- `YouCaiBiKao=true` ⇒ 仍 **c184**（备 c176/c181/c172，c148/c153 兜底）；c183/c184 的价值不受影响
  （它们是"合法闸门 + 组合增益"臂，不是快版代理的替代品）。

> ⏱ 时间戳卫生：本文件 §19~§23 的标题时间（10:5x/11:0x/11:2x/11:4x/12:0x）是**超前记录**
> （文件 mtime=10:46:03，当时系统时钟 ~10:46），读时间线请以 mtime 与真实时钟为准。本节起按真实时钟。

## 25. 【2026-09-17 12:14】下一役预登记：母本 + **组合臂 c163** + **副露饱和臂 c186**

**为什么现在写**：本役（§24）的 c156/c159 是 c151 的**两个单变量**。无论哪个胜出，下一步都只有两种走法——
把胜出的那条**推得更远**，或把两条**叠加**。这一节把两条都预登记好，战役一结束就能直接启动。

### 25.1 先说一个被推翻的旧前提：「因慢被淘汰」整条理由作废
`bot/ukeire.real_ukeire` 的下界剪枝（可证明逐位等价）修好后，把**所有当年因尾延迟被淘汰**的策略重跑 M=10
（`--threads 10 --per-thread 50 --warm 500`）：

| 策略 | draw 结果 |
|---|---|
| c135 / c142 / c146 / c147 / c150 / c154 / c155 / c158 / c159b / c159c / c162 | **12/12 全部 0 超 3s**（max 1~451ms） |
| **c163（组合臂）** | **max=326ms，0 超 3s**；window max=14ms |

⇒ 以后**不得再以"太慢"为由**把 c150/c151/c156/c159/c163 一类判死；判死只能因为 A/B 或机制不达标。

### 25.2 副露阈值扫描（决定 c186 的参数）
真机"被问到的窗口"上同窗配对（6,000 窗口，母本 = c151）：

| claim_p | 相对母本 | 副露/轮估算(*) |
|---|---|---|
| 0.65 | 1.19x | 1.027 |
| **0.60（现役 c156）** | **1.24x** | **1.077** ← 低于强者下界 |
| 0.55 | 1.29x | 1.117 |
| **0.50（c186）** | **1.33x** | **1.155** |
| <=0.45 | 1.34x | 1.16（**饱和**） |

(*) 0.866（我方真机副露/轮，field-audit）× 倍数；强者区间 **1.09~1.32**（`meld_acceptance` 同房对照另证缺口 −18.8pp）。

**两条结论**：① 现役 c156 的副露量其实**还差一点**；② 这条轴**天然有界**——阈值到 0.45 以下不再增加接受
（学习网络能补的窗口就那些），即使推到饱和也只有 1.34x ⇒ 1.16，**仍在强者区间内**，不存在"越放越滥"的无界风险。

### 25.3 组合臂 c163 已验证是"两条单变量的正确叠加"
| 检查 | 结果 |
|---|---|
| `SpeedC163._pick_discard is SpeedC159._pick_discard` | ✅ True（出牌取分时段） |
| `SpeedC163._want_claim is SpeedC156._want_claim` | ✅ True（副露取学习门控） |
| 窗口回放 vs c151（53,141 窗口） | 1.29x、索取率 69.5%→**89.9%**、**只基线收 0**（严格超集） |
| 出牌回放 vs c151（4,176 决策） | 94.6% 相同 / 5.4% 换牌 / **动作类型 0 差异** |

### 25.4 下一役 4 臂（本役结束或 futility 后启动）
```
python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc151,speedc163,speedc186 --n 100
python -X utf8 tools/ab_ctl.py stop            # 等 _ab_driver 自然退出，禁止 --force
python -X utf8 tools/ab_ctl.py start speedtugc,speedc151,speedc163,speedc186 --bundles=speedc163,speedc186
```
| 臂 | 角色 |
|---|---|
| `speedtugc` | 基线（bundle 外） |
| `speedc151` | 家族母本，**同池同时段对照**（c163/c186 都要相对它读） |
| `speedc163` | **组合臂** = c159 分时段 + c156 学习副露 |
| `speedc186` | **副露推到饱和附近**（claim_p 0.50） |

判据沿用：150 房/臂、t ≥ 1.50 采用；100 房/臂、|t| < 0.60 futility；每臂 ≥12 房跑
`var/pc_from_dec.py --compare speedtugc` 与副露占比机制闸门。

> ⏱ 本节时间戳按真实时钟记录（写于 12:14）；§19~§23 的标题时间仍为超前记录，见 §24 末尾说明。

## 26. 【2026-09-17 12:20】池子二次预登记：**入席秒 50 vs 58**（随机对照）

### 26.1 为什么这是目前**期望值最大**的一条线
先建立"对手强度 → 我方分数"的换算（403 房回归，斜率不是拍的）：

    我方净胜/房 = -6.34 - 0.520 x 同桌强度        (斜率 SE 0.097, t = -5.36)
    ⇒ 同桌每弱 1 分 ⇒ 我方分/房 +0.520

再接入席秒：
- **已成立的操纵实验**（随机化）：入席秒 ≥50 的桌比"立即入席"弱 **49.7 分**（402 房，t=-4.23；
  去掉前 3 名贡献对手后仍 -33.7，t=-3.46；15 个小时里 **13 个同小时内部**仍是负向）⇒ 全局已启用 wait=50。
- **自然实验**（当前战役的入席秒随机落在 50~59，不额外烧房；`tools/pool_second_trend.py`）：
  ≥54 秒 **-17.5**（n=37） vs ≤51 秒 **+32.7**（n=351） ⇒ **Δ=-50.2，t=-2.56**；
  同期我方分/房 **+8.1 vs -22.5 ⇒ Δ=+30.7（t=+0.67）**。
- **换算**：把入席秒从 ≤51 推到 ≥54 ⇒ **预测我方分/房 +26.1**。我方当前约 **-20/房**
  ⇒ 若成立，这是**把分/房做正**（§9.98 的唯一出路）最直接的一步。

⚠ **但自然实验不是随机化**：入席秒由"上一房何时结束"决定，可能与局的长度/时段相关（同小时分层只做了一半的排除）。
⇒ 必须做一次随机对照才敢改全局开关。

### 26.2 设计（2 臂，同策略，**只动入席秒**）
| 臂 | 内容 | wait_until_second |
|---|---|---|
| `speedtugc` | 基线（与另一臂**打法完全相同**） | **50**（逐臂钉死，不跟随全局，防中途被改） |
| `speedtugc_w58` | 同上，**只是名字不同**（integrity 需要一房一策略名） | **58** |

```
python -X utf8 tools/ab_ctl.py stop        # 等 _ab_driver 自然退出，禁 --force
python -X utf8 tools/ab_ctl.py start speedtugc,speedtugc_w58 ^
    --arm-opt=speedtugc.wait_until_second=50 --arm-opt=speedtugc_w58.wait_until_second=58
```

### 26.3 判据（预登记，先写后跑）
- **操纵校验（先过）**：w58 臂的入席秒 **≥58 占比 ≥75%**（上一役 w50 臂实测 ≥50 占比 91%）。
  不达标 ⇒ 实验无效，别读数。
- **主端点 = 同桌强度**（与上一役同一端点、同一阈值）：**Δ(w58 − w50) ≤ −15 且 t ≤ −1.7** ⇒ 成立，把全局 `.wait_until_second` 改成 58。
- **次端点 = 我方分/房**：方向应为正；仅作佐证（此端点噪声大，不当判据）。
- 样本：**≥20 房/臂**（上一役在 11 房/臂 就达到 t=-1.84）；**配对轮转**（同一批对手、同一时段）是端点低噪声的来源。

### 26.4 与"能力提升"的关系（必须分清）
池子是**阶梯**的杠杆，**不作用于锦标赛**（锦标赛是固定对阵，不看入席秒）。
所以它治的是"倒数 / 又输 3000 分"，**不直接让我们打赢强者**。
⇒ 它与 §25 的策略战役是**两条独立线**，谁先跑是排期问题，不是取舍问题。

> ⏱ 本节时间戳按真实时钟记录（写于 12:20）。

### 26.5 ⚠ **更正（2026-09-17 12:23，我上一版写错了）**

**错在哪**：我把"入席秒 **≥54 vs ≤51** 的差（−50.2，t=−2.56）"当成了"**50 → 58 还有余量**"。但那个对比的
**低档含大量"更早入席"（4/16/37 秒）的房**，量到的其实是**已经成立的「等待 vs 立即」效应**，不是"等更久"的增量。

**正确的增量对照必须同臂内做**（否则臂别与秒档共变）：

| 对照 | 对手强度 | 我方分/房 |
|---|---|---|
| **A) 增量（同臂 speedtugc，50-51 秒 vs 54-59 秒）** | **Δ=−30.7（t=−0.55，n=5 vs 23）** | Δ=+56.8（t=+0.77） |
| A) 增量（同臂，50-54 vs 55-59） | Δ=−36.8（t=−0.98，n=15 vs 20） | Δ=+86.0（t=+1.13） |
| B) 参考（臂间 `<50 vs ≥50`，已成立的随机化效应） | **Δ=−49.3（t=−4.24）** | Δ=+9.9（t=+0.38） |

**⇒ 更正后的结论**：
1. **"58 比 50 好"不成立**（t<1，点估计 −30 ~ −37 只是**暗示**，样本太小）；
2. **已采纳的 wait=50 也只有"对手强度"这个代理端点的证据**（t=−4.24），
   **在"我方分/房"端点上仍未显著**（+9.9，t=+0.38）⇒ 那 0.520 的斜率换算目前只是**外推**，没有随机化验证；
3. 上一版我写的"**预测 +26.1 分/房**"**作废**（它建立在被混杂的对比上）。

**因此 §26 的定位下调**：它不是"期望值最大的线"，而是"**代理端点已成立、分数端点未验证、增量未成立**"的一条线。
**不建议**为它抢占策略战役的产能；如果要跑，也只是把 2 臂的代价换来一个"增量到底有没有"的答案。
`tools/pool_second_trend.py` 已改为输出 **A（同臂增量，正确）/ B（臂间参考）两层对照**，并带混杂警告。

> 复盘：这次的错误形状是"**拿一个更宽的对比当更窄的对比**"。项目此前也被同形状坑过（WIP 用观测估计当操纵效应）。
> 记在这里，下次比较秒档时先问一句：**两档之间除了目标变量，还差了什么？**

> **估计量/样本口径提示（12:23）**：	ools/pool_second_trend.py 的 B 对照用**前缀历史均分**估对手强度，
> 同期窗口下得 **Δ(等待−立即) = −24.2（t=−1.16，n=24 vs 21）**；而预登记判词（.pool_verdict.acted）
> 用的是**另一套估计量/样本**（n=11 vs 11）得 **−44.3（t=−1.84）**。两者**不可直接相加或替换**——
> 可信的只有"**方向一致、量级 ±25~45、分数端点≈0**"这三条。
> 
> 因此本节的最终可执行结论只有一句：**在池子的分数收益被随机化验证之前，不要为它调整全局 wait，也不要为它抢占策略战役的产能。**


## 27. 【2026-09-17 16:08 去重后修订】下一役预登记（条件式）

**当前战役**：`speedtugc + speedc151 + speedc156 + speedc159`，started=2026-09-17 11:22:11，已恢复运行；等 150 房/臂或 futility 收尾。

**去重优化的影响**：`shanten_exact` 每层去重 `(m,p,t)` 后，精确向听成本大幅下降；此前因延迟被压制的策略现在可重新排队，但 M>10 仍以 n≥2000 实测门禁为准。

**下一役候选（4 臂，条件式）**：

| 臂 | 角色 | 依据 | M=20 n=1000 实测 |
|---|---|---|---|
| `speedtugc` | 基线（bundle 外） | 同池同时段对照 | 安全 |
| `speedc151` | 母本 | 爆头 31.7%、真进张 +1.16~+1.24；M=10 n=2000 **0/2000** | M20 2/1000 |
| `speedc152` | 分档对数奖励 | pc=0/1/2 按爆头曲线分档，直接对齐爆头链 | 2/1000，max=4229ms |
| `speedc163` | 组合臂 | c159 分时段 + c156 学习副露的正确叠加 | 2/1000，max=3308ms |
| `speedc186` | 副露饱和备选 | claim_p 0.60→0.50，副露/轮≈1.155（强者区间内） | **0/1000，max=2735ms** |

**臂集裁决**：
- 若当前战役显示 c159 或 c156 有机制增量 → 下一役优先 `baseline + c151 + c152 + c163`，验证“路线分档 + 组合叠加”。
- 若当前战役显示 c156/c159 都没有增量 → 下一役改用 `baseline + c151 + c152 + c186`，重点转向“路线分档 + 副露饱和”。
- `c186` 是 M>10 下最安全的备选之一；`c163/c152` 若在正式赛 M>10 的 n≥2000 门禁失败，只能用于 M≤10。

**启动护栏**：
1. 先 `python -X utf8 tools/arm_smoke.py --arms speedtugc,speedc151,<cand1>,<cand2> --n 100`。
2. `python -X utf8 tools/ab_ctl.py stop` → 等当前驱动自然退出，禁止 `--force`。
3. `python -X utf8 tools/ab_ctl.py start speedtugc,speedc151,<cand1>,<cand2> --bundles=<cand1>,<cand2>`。
4. 每臂 ≥12 房先跑 `pc_from_dec`、有副露局占比、`ab_tenpai`（先补拉门户）机制闸门。
5. 150 房/臂按 `ab_adopt` 判采用；100 房/臂 |t|<0.60 判 futility。

⚠ 由于去重是逐位精确的，当前战役 11:22 之后的样本在实现升级前后仍可合并；但**下一役必须重新设 started，不能和当前战役混算**。

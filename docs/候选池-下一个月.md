# 候选池 —— 下一个月（2026-09-17 起草）

> 目的：本月的真正瓶颈是**候选供给**（§8.1：一个月约 10 个决策位，但手上长期只有 1 个候选）。
> 本文件把**已排除的**写清楚，把**待做的**按证据强度排好，**每条都带触发条件、护栏、端点与机会频率**。
> 纪律见 `docs/STATUS-20260916.md` §9.28（三段证据）与 §9.30/9.32（离线分析用 `lower()`）。

## 0★. 30 天硬算术（**先读**；2026-09-16 19:50 实测重算）

> 起因：用户明确"几天之后只是测试赛，真正要准备的是**一个月后**的比赛" ⇒ 需要一张"一个月能买到什么"的账单，
> 防止把一个月花在**根本测不出来**的小候选上。以下每个数字都本机可复算（脚本路径见每段末尾）。

### 0★.1 榜单口径已对账（**rank = 累计原始分**）

- 门户 `GET /portal/api/leaderboard?period=all`（Cookie）：我方 rank **569** / 408 房 / **−6,885** / firsts 73（**17.9%**）。
- 本机 `var/auto_ranking.jsonl` 我方 400 房 `total_score` 求和 = **−5,856**，与榜单差 −1,029（= 尚未归档的 8 房，
  早期劣质策略）⇒ **榜单 ≈ 逐房原始分累加**，与 A/B 的"净胜/房 ×4/3"**同一把尺子**。
  - 复算：`python -X utf8 var/_sumcheck.py`

### 0★.2 我们现在到底什么水平（**不要再看单点估计**）

| 窗口 | 房 | 原始分/房 | SE | first% |
|---|---|---|---|---|
| 全史（榜单） | 408 | **−16.9** | — | 17.9% |
| 本机 09-11 起（C036 之后） | 347 | **−2.0** | ±7.8 | 19.9% |
| `speedtugc`（在跑基线） | 198 | **+14.8** | ±11.0 | 22.7% |
| 最近 100 房（多策略混合） | 100 | −10.8 | ±16.5 | 17.0% |

⇒ **诚实结论：我们的真值 ≈ +15/房，95% 区间约 [−7, +37]，即"可能略正、也可能 0"。**
"C036 = +33~44/房"是**局部配对口径**的估计；在 **198 房房级**只能复现出 **+14.8 ± 11.0**（t=1.35，未达显著）
⇒ 对预算**一律按 +15 记账**，不要把 +35 当已确立事实。
- 复算：`python -X utf8 var/_traj2.py`

### 0★.3 爬榜需要维持多少（榜单按累计分排）

| 目标 | 该名次当前分 | 与我们的差 | 未来 30 天必须维持的原始分/房 |
|---|---|---|---|
| rank 32 | +21,780 | 28,665 | **≥ +54** |
| rank 10 | +67,782 | 74,667 | **≥ +120** |
| rank 1 | +99,066 | 105,951 | **≥ +157** |

口径：30 天 × ~99 房/天（实测 4.14 房/h × 24h）≈ **2,950 房**，且要求**同时超过该名次玩家自己的速率**
（rank 32 现 +44.7、rank 10 现 +94.8、rank 1 现 +120.4）。
⇒ 从 +15 爬到 rank 32 = **再找 +40/房（乘 3.6 倍）**；到 rank 10 = 再找 +105/房。
- 复算：`python -X utf8 var/_ladder_gap.py`
- 吞吐是**硬的**：同账号并发 = E002（`/api/match` 幂等、"绝不双房双席"，平台上限 16 场/用户）
  ⇒ 一账号 ≈ 4.1 房/h，**加房量这条路不存在**，唯一自由变量是**分/房**。

### 0★.4 一个月能"验出"多大的效果（**分辨率表**）

| 投入 | 房/臂 | 可辨 Δ（t=1.5） |
|---|---|---|
| 3 天 | 150 | **±33 分/房** |
| 1 周 | 345 | ±21.7 |
| 2 周 | 690 | ±15.4 |
| 30 天（全月只测一个候选） | 1,480 | **±10.6** |

⇒ 一个月 ≈ **10 个"3 天 / 150 房/臂"决策位**，而每位只能判 **≥33 分/房** 的效果。
⇒ **±5~15 分/房的手调候选，一个月内既不能证真也不能证伪** ⇒ 要么不排队，要么换端点（下条）。

### 0★.5 唯一能提高功效的杠杆：**换端点**（不动吞吐）

一房 = 80 手/人（M=10 × Rounds=8），我方每房**胡率** p≈**25.5%**（159 房门户口径，§9.56）
⇒ 每房 SD = √(0.255·0.745/80) ≈ **4.9pp**。
⇒ 两臂各 n 房时 SE(Δ胡率) ≈ 4.8·√(2/n) pp ⇒ **辨 3pp 只需 ~12 房/臂**、辨 1pp 需 ~100 房/臂。
而 3pp 胡率的价钱 ≈ **+33 分/房**（§4c 价值分解：庄胡率 +34.6 / 闲胡率 +25.6 的量级）
⇒ **同样的钱买到约 10 倍功效 ⇒ 每个候选的"首要读数"改成机制端点**：
`ab_portal_readout.py`（门户口径）的**我方胡率 / 爆头占胡 / 番**，`ab_readout.py` 的净胜/房只作**交叉验证**。

**★ 换算系数已实测（2026-09-17）**：用 **329 房**做"每房我方胡率 → 每房净胜"回归 ⇒
**1pp 胡率 = +27.4 分/房（SE 0.89, t=30.9）** ⇒ 150 房/臂的胡率 SE（0.55pp）≈ **15.1 分/房**，
比分数端点（22.4）**分辨率高 ~1.5×**；采用规则见 `STATUS §9.105`（v3）。

### 0★.6 两个目标不要混（**榜 ≠ 比赛**）

- **榜单**（累计原始分）⇒ 优化 **分/房**。
- **真正比赛**：按指南 §2.6，m≥17 的普通锦标赛自动多阶段 **海选 → 16强 → 8强 → 决赛**（每阶段组内循环 M 场，
  决赛并列还会加赛）⇒ 优化的是**名次分 / first%**（晋级看名次，不看累计原始分）。
- 两把尺子**可能背离**（例：爆头路数抬原始分、压胡率）⇒ 每次读数**同时**报 first% / place_points。
- 正式赛 config（`YouCaiBiKao` / M / Rounds / BaseScore）**目前未知**：19:47 复查门户 `GET /portal/api/tournaments`，
  仍**只有二测那一场** ⇒ 发布后第一件事读 `/api/tournaments/me/rules`（Bearer），把上面的预算重算一遍。

### 0★.7 立即执行（今晚 → 明天）

1. 等 `speedtugc vs speedc151` 到 **25 房/臂（≈09-17 05:10）** ⇒ 主判据 + 门户口径（**先看覆盖率**）。
2. 读结果时**按 0★.4 的量级解释**：|t|<1 且 Δ胡率 <1pp ⇒ 视为未证实、淘汰，不要靠"看起来好"续命。
3. 候选要**先写出预期分/房**（≥ +30 才排队）；写不出来的走离线路由，不许占决策位。
4. 测试赛（09-17 20:00）作为**流程演练 + 真配置采样**（160 局/场待回填），赛后按操作单 §29b 三步审计。
5. 正式赛发布后：先读 `/rules`，再按 0★.3/0★.4 重算目标速率与决策位。

> **四臂棋力战役的完整预登记**（臂定义 / 机制签名 / 失败处置 / 时间盒 / 撤单臂规程）见
> `docs/预登记-四臂战役-20260917.md`。

### 0★.8 池子杠杆**不转移**到锦标赛（2026-09-17 00:0x 实证）+ 多臂排期

- **机制事实**：入席时机由 `tools/match_super.py --wait-until-second` 在 **`POST /api/match`（自习房）** 生效；
  **锦标赛分桌由服务器指派**（`var/_official_keepalive.py` 直接跑 `run_bot.py`，不经 match_super）
  ⇒ **`var/.wait_until_second` 对正式赛无效**。
- ⇒ 池子（若成立）只值**阶梯排名**（+15~30 梯分/房），**不转移**到"赢强者"；能转移的只有**棋力**改动。
  **排期原则：锦标赛优先 ⇒ 棋力实验优先；池子实验跑完预登记即止，不得挤占棋力排期。**
- **多臂战役（吞吐 ×1.5，通路已有单测，不需新代码）**：
  `python -X utf8 tools/ab_ctl.py start speedtugc,speedc151,speedc156,speedc159 --bundles=speedc151,speedc156,speedc159`
  —— 一个基线臂服务 3 个候选：600 房 ≈ 6 天，而顺序做 3 个两臂战役要 900 房 ≈ 9 天。
  ⚠ 灾难熔断（`_ab_driver.guard_candidate`）是"候选 vs arms[0]、**整役一起停**" ⇒ 一个臂崩坏会丢另外两臂样本（可接受，非终点判据）。

## 0. 已排除（不要再试）

| 方向 | 排除依据（出处）|
|---|---|
| 机会生产 / 听牌率本体 | §4c-59：与池内持平 |
| 兑现率本体 | §4c-60：差额是 C036 的账面成本 |
| 出牌层最小向听 | §4c-42：97.5% 已最优 |
| 杠（明/暗/补）| §4c-55/57：天花板 +0.9~4.7 分/房 |
| 放宽七对门控 | §4c-55：净增 +0.00016/轮 |
| 笼统"番型/爆头/财神"加分 | §9.22：除爆头外各番种与强手持平 |
| 对手强度建模 | R 系列：R²≈0.002 |
| **副露层（多吃碰）** | §9.21：三份**自体内**反证（09-10 副露最多那天听牌率反而更低…）|
| **§7-7 弃白=财飘低估** | §9.25：62 次候选解 / 306 房，**判据从不改变决定 ⇒ 0 次** |
| **放宽弃胡门控** | §9.25：被拒的 1,339 次里 1,181 次是"现在 2 番、爆头形也只有 2 番"⇒**拒绝是对的**；§9.33 再确认门槛非瓶颈 |
| **庄位专用线** | §9.29：庄位差 2/3 是**同一 edge 被 24m 放大**、1/3 是连庄的结果 |
| **"对数升为部分主键"** | §9.34：与 c151 **同源**（`ROUTE_BONUS` 本质=软容差） |
| **"调大 ROUTE_BONUS"** | §9.31：8→1.947 / 16→1.940 / 32→1.910 ⇒ **饱和**（4× 只改 3.7% 决策） |
| `meld_offer_audit.py` 档位 | §9.10：offer 分母口径错 ⇒ 以 `meld_acceptance.py` 为准 |
| `timeout` | §9.8：吃碰窗口超时 ≡ 主动 pass，无区分力 |

## 0★★. 「已关闭方向」的**证据强度审计**（2026-09-16 20:05；可复算 `python -X utf8 var/_closure_audit.py`）

> **动机**：§0 的"已排除"清单里，很多条是**在没有交替 A/B 的早期**、用 **n<35 房**判掉的。
> 而按 §0★.4 的分辨率：**n=32 房在 t=1.5 下只能辨 ±47/房、n=20 只能辨 ±59/房**
> ⇒ 那些"判掉"很可能是"**没测出来**"。本节把每条关闭项的证据强度量化，决定哪几条值得占用一个决策位**重开**。

**方法**：`auto_ranking.jsonl` 全部 400 个完成房 ⇒ 按"候选 × 当日 speedtugc 基线"做**同日比较**
（早期批次**非交替**，所以这仍**不是干净实验**，只是最保守的可用口径）；判据 = **Δ 的 95% CI 上限 < +30/房**才算真排除。

| 候选 | 房 | 自身均值/房 | 同日基线房 | 同日 Δ | t | CI 上限 | 判定 |
|---|---|---|---|---|---|---|---|
| speedc130 | 3 | −237.0 | 22 | −231.4 | −4.75 | −135.9 | **真排除** |
| speedtugw | 3 | −125.3 | 70 | −187.2 | −2.77 | −54.9 | **真排除** |
| speedc132（进张）| 3 | −133.0 | 58 | −138.1 | −3.29 | −55.9 | **真排除** |
| speedtug | 30 | +3.8 | 70 | −58.0 | −1.88 | +2.5 | **真排除**（CI 上限 <30）|
| speedc073w2（BC）| 32 | −3.1 | 28 | **+30.8** | 0.92 | +96.0 | ⚠ **未决** |
| speedc073w4（BC）| 32 | +7.4 | 58 | +2.4 | 0.07 | +69.0 | ⚠ **未决** |
| speedc068lo0（BC）| 20 | −19.8 | **6** | **+117.4** | 2.50 | +209.2 | ⚠ **未决**（见下）|
| speedc067（BC）| 4 | −4.0 | 6 | +133.2 | 2.14 | +255.3 | ⚠ **未决** |
| speedc118 | 10 | −23.1 | 22 | −17.5 | −0.33 | +84.9 | ⚠ **未决** |
| speedc150（机制束）| 3 | −18.3 | 36 | −29.9 | −0.50 | +87.3 | ⚠ **未决** |
| speedc151（在跑）| 6 | −17.2 | 36 | −28.8 | −0.44 | +100.0 | ⚠ **未决** |

**★ 基线自己的日间漂移（同一个 speedtugc）**：09-14 **n=6 → −137.2/房**；09-15 n=22 → −5.6；09-16 n=36 → +11.6；
全史 n=199 → **+13.6** ⇒ 早期那些"候选 vs 基线"的判词，**先被基线当天的运气决定**。
（`speedc068lo0` 那个 +117 里**几乎全部**来自"基线那天 6 房 −137"，**不是**候选做得好 ⇒ 只能算**未决**，不能当利好。）

**⇒ 决策：把一个月里的决策位重新排**

1. ~~BC 路径选择族（`speedc073w2`/`w4`、`speedc068lo0`、`speedc067`）列为下一个决策位~~
   **⛔ 2026-09-16 20:22 撤回（新证据，见 §9.58）**：高功效端点补上后立刻证伪了这个排队理由 ——
   该族**根本没有把听牌速度提上去**：8 巡听牌率 c073w2 **46.5%** / c073w4 **46.2%** / c068lo0 45.5%
   vs 基线 speedtugc **45.8%**（2400~12720 局口径，SE≈0.9pp ⇒ **Δ 在噪声内**），
   而**胡率更低**（23.5~24.5% vs 25.52%）、**净分更差**（+1.5 / −30.3 vs **+21.4**/房）。
   ⇒ BC 既没移动机制、也没赚钱 ⇒ **不再占决策位**（保留为"已试过、机制层无变化"的记录）。
2. **副露接受率（41% → 60%，**26 次/房**机会）** —— 第 2 位。现状是**三份自体内反证**（§9.11 / §4c-48 / §9.21），
   但三份共享同一个混杂：**手牌差才会去吃碰**（选择了副露 → 听牌率更低）。重开必须先做**吃碰窗口层的同房配对**，
   不许再跑观察性相关。
3. **微小增量（±5~15/房，典型 = `speedc152` 分档对数）** —— ~~降级为"不上真机"~~
   ⇒ **2026-09-17 推翻（v3）**：该处置只看**分数端点**，而 §0★.5 已指出要**换端点**；
   `STATUS §9.105` 把规则写成 v3（机制移动 >2SE + **已验证因果链** + 高功效安全闸）⇒
   `c152` 属于**唯一有已验证链**（爆头 / C036 = +10.92 分/次）的一族 ⇒ **排队上真机**
   （见 `docs/预登记-四臂战役-20260917.md` §9：本役臂 = `speedtugc` + `c151/c152/c159`）；
   只在离线配对里累计证据，等某个大束把它一起带上去。
4. **不重开的**：因**工具/口径错误**而作废的项（`meld_offer_audit` 档位分母错、本地 `dec` 座位归因、
   `timeout ≡ pass`）—— 那些是**方法问题**，不是"效果没测出来"。


### 0★★★. 听牌速度"是靶子"这个假设本身被削弱（2026-09-16 20:22，§9.58）

用新工具 `tools/ab_tenpai.py`（逐局口径、逐场座位；**基线 8 巡听牌 45.8%** 与文档值 45.8% 对得上）横扫 314 房：

| 臂 | 局 | 8巡听 | 4巡听 | 副露/局 | 胡率 | 番/胡 | 净/房 |
|---|---|---|---|---|---|---|---|
| **speedtugc（基线）** | 12720 | 45.8% | 12.1% | 0.872 | 25.52% | 1.322 | **+21.4** |
| speedtugh | 640 | **48.0%** | 10.6% | 1.250 | 23.59% | 1.278 | **−47.5** |
| speedc073w2 | 2400 | 46.5% | 12.0% | 0.859 | 24.46% | 1.368 | +1.5 |
| speedtu | 960 | 44.4% | 10.5% | **1.404** | **27.50%** | **1.133** | −25.3 |
| speedc067 | 320 | **41.9%** | 10.6% | 0.806 | **27.81%** | 1.169 | −4.0 |

⇒ **听牌速度本身与得分没有单调关系**（听牌最高那臂反而最亏；听牌最低那臂胡率最高）。
⇒ 结论：听牌率**只能当"干预是否生效"的体检项**，不能当靶子；不要为了抬它去换番数/七对（speedtu 就是这条路：胡率 +2pp、番 −0.19 ⇒ 净 −25）。

## A. 分档对数奖励（**已实现：`bot/speedc152.py`**；触发：c151 出结论后立即排队）

**目标**：把 c151 的**平**奖励（`pc<=2` 给同一分）改成**按实测曲线分档**，同时服务两个端点。

**机制假设（§9.26/§9.35 实测曲线）**

| 对数 pc | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 该形状"能给出爆头形"的比例 | **100%** | 33.8% | 29.4% | 21.5% | 13.5% | **76.0%** |
| 第 8 巡听牌率（§4c-67）| — | 63~72% | 39~51% | 13~30% | 13~30% | — |

⇒ 曲线是 **pc=0 特殊高、1→4 单调下降、pc=5 特殊高（七对线）**。
c151 用的是"听牌率"的门槛（≤2），而本月主线是"爆头形"（门槛更靠 ≤1，但不能设硬门槛：pc=1 与 2 只差 4.4pp）。

**✅ 已实现（2026-09-16 19:20，STATUS §9.44）**：`bot/speedc152.py`（已注册 `run_bot.STRATEGY_FACTORIES`）
＋ `tests/test_speedc152.py`（7 项，含方向护栏与"向听仍为主键"）。**实现复核**：与 c151 的平均弃后对数增量
**−0.054（与 §9.38 的设计预测完全一致）**、分歧率 11.8%（设计 15.5%，样本不同）。全量单测 437 项全绿。

**实现**：只覆盖 `_route_bonus`（与 c151 同钩子，单变量）：
```
w = {0: 12, 1: 8, 2: 2, 3: 0, 4: 0}      # ★ 已由 §9.38 的离线 footprint 定参
bonus = w[pc] (+ 白数项，可选)
```

**★ 离线 footprint 已完成（§9.38，220 局面）**

| 权重方案 | 平均弃后对数 | 分歧率 |
|---|---|---|
| plain（无加分）| 1.950 | — |
| **flat ≤2 =8（现 c151）** | **1.868** | — |
| **mono_hard 12/8/2 ← 推荐** | **1.814** | **15.5%** |
| p01_only 12/12/0 | 1.818 | 21.4%（**被 mono_hard 支配**：同结果、干预更多）|
| p5_bonus（+pc5=6）| 1.836 | 6.8%（**本采样无差异 ⇒ 先不加**）|

可达最低对数分布：pc=0→27 / 1→88 / 2→75 / 3→18 / 4→9 / 5→3（**pc=0/1 在 52% 局面可达**）。
**护栏**：① 只做**并列项**（主键仍是向听）⇒ 下行有界；② 必须**不牺牲 C036**（跑 `giveup_audit` 比对弃胡次数/每次收益）；
③ 单调性要有**方向护栏单测**（`w(0)≥w(1)≥w(2)≥0`）。

**端点**：任务级 = **爆头占胡**（`tools/ab_endpoint.py`，2σ 辨 +4.8pp ≈ **16 房/臂**）；
机制级 = **有爆头形占比 + 平均对数**（`tools/ab_baotou_probe.py`）；采用级 = 净胜/房 t≥1.50（150 房/臂）。

**机会频率**：c151 已把 ≥2 对从 69.8% 压到 60.3%；本候选的增量作用面 = **pc=0/1 与 pc=2 相对权重的改变**，
需先用离线 footprint 量"会改多少决策"（有界、`lower()`）。

## B. 强制爆头模式（**触发：正式赛 `/api/tournaments/me/rules` 读到 `YouCaiBiKao=true`**）

**为什么要有**：§9.36 实测 —— 若 `true`，我方 **64.4% 的胡牌会变成非法**（有白平胡），
而 **`speedc148` 只是闸门不是打法**：挡掉之后必须有"怎么继续打"的配套。

**目标**：在 true 环境下把"每一手都朝爆头/杠开走"变成**硬约束**，而不仅是"有机会才换"。

**设计**：在 `speedc148`（含合法性闸门）之上加**强制弃胡**分支：
- 若当前可胡但**不是爆头/杠开**（且手里有白）⇒ **拒绝胡**，并按 C036 的 `_best_giveup` 选弃牌；
- 若连"能形成爆头形的弃牌"都没有 ⇒ 退回**最大化爆头形可达性**的出牌（即分档奖励 A 的策略）。

**风险**：真环境下**所有人都受同一约束** ⇒ 相对表现由**爆头能力**决定；
我们的爆头占胡 21.4% vs 强手 28.2%（比值 0.76）⇒ **该模式下相对劣势会被放大**。

**验证路径（无法在阶梯 A/B，因为阶梯是 false）**：
① 单测（闸门 + 强制弃胡分支 + 方向护栏）；
② `offline_replay` 在**真机局面上**量触发率与窗口级 footprint；
③ 若时间允许，**用官方复盘的"胡牌合法性"一栏赛后自查**。

## C.（备选）吃碰层的路线偏好

**动机**：§9.21 说明"主动多吃碰"无效；但**在吃碰时选哪种形**（三张组合的方向）从未测过。
**端点**：有爆头形占比 / 平均对数（同 A）。**机会频率**：需先量"吃碰后手形差异"的窗口数。
**优先级**：低于 A，且与 A 共用同一套端点 ⇒ **排在 A 之后**，避免同时上两个同端点候选。

## D. 待验证的"下一个月大方向"（无具体候选，先记着）

- §4c-61 ④ 提出的**两段式"离线训练 + 真机小样本验证"**至今没做；
  但本会话已把**可测的手工空间基本穷尽**（见 §0）⇒ 若 A/B 之后仍缺候选，**下一层应是非手工搜索**（搜索/学习类）。
- 做之前必须先解决：**离线评估不预测真机**（§4c-61 的 r=−0.42）⇒ 需要先找到/验证一个**离线→真机的筛子**。

## 0★★★b. 05:10 中期读数 checklist（照做，不要临场改规则）

```powershell
# 0) 先补拉门户复盘（手动工具，必须每次读数前跑；见 2026-09-16 的教训）
#    实测：本地 recent/ 停在 15:28，而本役房 25 个 => 门户覆盖率 0% => 机制层读不出来
python -X utf8 tools/fetch_room_replays.py --rooms 10 --gap 1.2      # 约 10~12 分钟
# 1) 主判据（净胜/房 + 桌强调整 + top2/名次分）
python -X utf8 tools/ab_readout.py
# 2) 机制层 + A 级判据（**一条命令**，v2 规则机械化；含逐房得分校验）
python -X utf8 tools/ab_mech_verdict.py --since "2026-09-16 16:23:34"
# 2b) 机制层：门户口径逐臂端点（覆盖率 <80% 只看趋势）
python -X utf8 tools/ab_portal_readout.py --since "2026-09-16 16:23:34"
# 3) 机制层：零延迟（对子/拆对/进张遗憾/听牌率）
python -X utf8 var/_run_mech.py --since "2026-09-16 16:23:34" --jobs 2
# 4) 价值层：胡率/番/爆头/七对（零延迟）
python -X utf8 var/_own_fan_audit.py --since "2026-09-16 16:23:34"
# 5) 听牌速度端点（逐局口径）
python -X utf8 tools/ab_tenpai.py --since "2026-09-16 16:23:34" --lowprio
```

**判读规则（预登记，不许事后改）**：
| 情形 | 动作 |
|---|---|
| 各 ≥12 房 且 \|t\|≥3.0 | 采用/停用按方向执行（中期采用规则 §28）|
| 各 ≥12 房 且 \|t\|≤0.6 | 继续跑到 100 房/臂；若到 100 仍 \|t\|<0.6 ⇒ futility **停臂** |
| 机制层**没动**（拆对/对数/听牌与基线无差） | 即使分数好看也**不采用**（先查工具/生效性）|
| 机制层动了但分数明显反向 | 记录为"机制对但收益不成立"，停臂 |


### 0★★★c. 副露侧候选的**排队顺序**（2026-09-16 22:2x 定稿）

| 顺序 | 候选 | 增量（预测） | 说明 |
|---|---|---|---|
| **1** | **`speedc156`**（阈值 0.60）| 有副露局占比 **+5.4pp**（61.2→66.6%）| **单变量**：只在门控拒的窗口用 `c121_meld_net` 补加；延迟 +0.32ms；已单测/已注册 |
| 2 | `speedc158` = c156 + **末盘放宽**（河牌≥40、同向听、等张退化 ≤8）| 合计 **+6.8pp**（→~68%）| 大束；离线 1200 窗口里与 c156 **同档（89.2%）**⇒ 增量主要在末盘窗口，样本太小看不出差别 |
| 3 | `speedc156c70`（阈值 0.70）| +1.5pp | 只作"过冲时的退档" |
| — | 强者 | — | 74.5% |

**为什么 1 先于 2**：项目纪律是"一次只动一个变量"，而 c156 与末盘放宽**都还没真机验证**；
若把两者捆在一起失败，无法判断是"学习补加不对"还是"末盘放宽不对"。**当 c156 的机制闸门通过、但效应太小测不出来时，才上 c158 的大束。**

**机制闸门（先于分数，§9.68）**：有副露局占比 **+4pp 以上**；番/胡 ≥ ~1.30、爆头占胡 ≥ ~25%；否则停臂。


### 0★★★d. `speedc159`（分时段压对数）—— 排队第 2（2026-09-16 23:0x）

**为什么**：本役 c151 的拆对率 T1 **9.0%** vs 强者 **2.3%**（早期过猛），T6/T8 低于强者（后期不足）。
**做法**：压对数项按 `river_len` 调度：`<12 → 0`（不压，= c150 行为）、`12~28 → 1.0`、`>28 → 1.5`；其余 100% 继承 c151。
**已验证**：5 项单测（含早/中盘可区分夹具）；离线脚印 1429 决策差 **5.1%**、异常 0；已注册。
**端点**：机制层看**逐巡拆对率**（T1 应回到 3% 上下、T4-T8 应升到 8~12%）、对数、听牌率；判据仍是净胜/房。


### 0★★★e. `speedc159b`（分时段压对数·加力档）—— 排队第 3

同 `speedc159`，只把力度调大：早盘 **0**（我们 T1 已偏高）、中盘 **1.5**、末盘 **2.5**。
**触发条件**：`speedc159` 的机制层生效（逐巡拆对率上升）但 **T4~T6 仍明显低于强者**（<6%）⇒ 上这一档。
已注册、已单测（4 项）；全量单测 500 绿。


## 0★★★★. **月度决策规则 v2**（2026-09-16 23:5x 定稿）——当"证明更好"在一个月内做不到时怎么办

**先摆算术（用项目自己的功率表，`tools/ab_readout.py` 打印）**：
一房 = 80 局，房级 SD 很大 ⇒
| 投入 | 房/臂 | 可辨 Δ（t=1.5，**原始分/房**）|
|---|---|---|
| 3 天 | 150 | **±33** |
| 1 周 | 345 | ±21.7 |
| 30 天全押一个候选 | 1480 | ±10.6 |

而我们手上候选的**预期效应都在 +5~+15 原始分/房**（c156 副露补加、c159 时段修正、c159b/c158…）
⇒ **150 房/臂的标准战役根本证明不了它们"更好"**（只会一直读成噪声）。

**因此本月的决策规则改为两级（先写死，避免以后临场变通）**：

| 级别 | 判据 | 用途 |
|---|---|---|
| **A. 机制对齐 + 无害**（主路径）| ① 机制端点按设计移动（如"有副露局占比 +4pp"、"逐巡拆对曲线向强者靠"）；② 150 房/臂时 **净胜/房 的 95% CI 下界不低于 −20 原始分/房**（= 排除"大亏"）；③ 单变量、离线脚印 ≥2%、延迟门禁通过 | **允许采用**，作为新基线继续叠加 |
| **B. 证明更优**（仅在能凑出大束时）| 预登记规则不变（150 房/臂、t≥1.5；中期 t≥3 且各 ≥12 房）| 用于**束**（多个已按 A 采用的小改动的合并），把小效应叠到可辨线以上 |

**为什么允许 A 级采用**：本月的目标不是"发表结论"，而是**把分数往上推**；而候选的机制方向是**对着强者画像**校准的
（§9.64 逐巡拆对曲线、§9.66 有副露局占比 74.5%），A 级规则相当于"跟着强者画像走 + 用分数做安全带"。
**代价与风险如实记录**：A 级采用**可能**引入 −20 分/房以内的慢性损失；因此**每次 A 级采用后，都要在下一战役里把"新基线 vs 上一版基线"作为 B 级束的一部分再验一次**。

**执行顺序（叠积木，而不是平行试）**：
1. 当前战役（speedtugc vs c151）跑到预登记终点 → 若 A 级成立 ⇒ 新基线 = c151；
2. 下一个：**c156**（副露补加）对"当时基线" → A 级判据；
3. 再下一个：**c159**（时段修正）对"当时基线" → A 级判据；
4. 每采用两三个小改动，就用一次 **B 级束**（把它们合并 vs 上一版基线）确认没有累积损害。


## 0★★★★★. 现状交接（2026-09-16 23:05）

**战役**：`speedtugc`（基线）vs `speedc151`（c150 束 + 多对子惩罚），started 2026-09-16 16:23:34，
已 **12/12 房**、b−a = +89.9 净胜/房（t=1.20，未达任何预登记阈值）。
**读数只需一条命令**：`python -X utf8 tools/readout_bundle.py --fetch`（含补拉门户 + 归档到 `var/readouts/`）。

**判据（v2，见 §0★★★★）**：
- **A 级（主路径）**：机制端点按设计移动 + 150 房/臂时净胜/房 95% CI 下界 ≥ −20 原始分/房 ⇒ 允许采用为新基线；
- **B 级（束）**：预登记规则不变（t≥1.5；中期 t≥3 且各 ≥12 房），用于若干 A 级小改合并后的回验。

**队列（每个都已实现 + 单测 + 过脚印/延迟门禁）**：
1. `speedc156`（副露学习补加 0.60；预测把"有副露局占比"从 61.2% 推向 ~66.6%，强者 74.5%）
2. `speedc159`（拆对时段化：早 0 / 中 1.0 / 末 1.5；修早期 overshoot）
3. `speedc158`（副露大束 = c156 + 末盘放宽）；`speedc156c70`（退档）；`speedc159b`（仅当中末盘反而偏低时）
4. ⛔ 已按数据关闭：`speedc159c`（脚印 0.8%）、`c154`（0 分歧）、**防守**（无铳和、被吃碰只差 0.6pp）、
   `GOD_MELD`/白（改了 3.3% 但控制不了白的去留）、庄位专用（分解结论）、BC 路径选择（机制没动）

**结构事实（决定方向）**：本规则**没有铳和**（26,501 局 0 例单一付款人）⇒ 没有防守可做；
支付只在别人自摸时发生，番数 ×2 递进（1→2→4→8→16）+ 庄位 ×2.4
⇒ **能拉开差距的只有"更快听牌"**（副露 + 拆对时段），而**番/爆头我们已与强者同档**（1.361 vs 1.353、26.8% vs 26.4%）。


## 0★★★★★★. **池子/入席时机**（最高优先级的候选级发现，2026-09-16 23:1x）

**事实**：按房的**开打秒**分桶，**秒 ≥50 入席的同桌显著更弱**（三种强度口径 −33~−51，4/4 天同向，12/12 为负）。
折算我方收益 **+15~+23 原始分/房**，成本 ~+25s/房（+2.8% 周期）⇒ **潜在最大单一杠杆**。

**⛔ 现状**：分桶是 **post-hoc** 的，必须用**新样本**验证；机制不明。

**排期（已定）**：测试赛（09-17 19:00 切换）之后**第一件事**就是跑这个实验（优先于 c156/c159）；
跑满 150 房/臂后按判据裁决：主端点（对手强度）Δ≤−15 且 t≤−2 ⇒ 永久启用等待；无差异 ⇒ 永久关闭。
样本外检验工具：`python -X utf8 var/_pool_signal_test.py --since "2026-09-17 22:00:00"`。

**预登记实验（机制已实现，见 STATUS §9.88）**：
- 两臂**同一策略**：A = 立即入席（`speedtugc`）；B = **等墙上时钟秒 ≥50** 再 `/api/match`（别名 `speedtugc_w50`）；
- 主端点 = **对手强度（因果）**：150 房/臂 ⇒ 可辨 ~15 分；副端点 = 净胜/房（预登记）；成本端点 = 房周期时间；
- 实现要点：`match_super.py --wait-until-second N`；`_ab_driver.py` 读 `.ab_mode.arm_opts` 逐臂传参；策略别名只改名字不改打法。

> ⚠ **入口指引（2026-09-26 11:40 更新 · 在此之前的内容均为历史记录）**
>
> | 用途 | 看哪个 |
> |---|---|
> | **作战计划（唯一权威，§A–§V.273）** | `docs/iter/reports/next-month-plan-20260923.md` |
> | **运行日志 / 全部证据与撤回** | `docs/iter/journal.md`（最新 **R1551**） |
> | **役次判词读卡（必读）** | `yaku2-` / `yaku3-` / **`yaku4-`** / **`yaku5-verdict-readcard.md`** |
> | **运行期标记总账** | `docs/iter/reports/heartbeat-marker-inventory.md` |
> | **判词当天一屏摘要（R1547）** | `python -X utf8 var/_verdict_digest.py --since "<役起点>" --baseline <B> --candidates <C1,C2> --mechanism none`（**只读**；把读卡的 6 条命令按固定顺序跑成 1 条 + **汇总机制端点标记**，屏幕打关键行 + 落报告；**它不做判断**，读法看读卡） |
| 当前役进度 | `python -X utf8 var/_verdict_watch.py --label 役3bc --since "2026-09-25 20:52:39" --baseline speedvalue --candidate speedvaluebc --mechanism none --check-only`（另一条把 `役3bc/speedvaluebc` 换成 `役3v/speedvaluebaotouv5`） |
> | 一屏总览 / 排期 / 环境 | `var/_campaign_status.py` · `var/_schedule_guard.py` · `tools/preflight.py` |
> | **完整性（R1526）** | `python -X utf8 tools/ab_integrity.py --since "役3起点" --arms speedvalue,speedvaluebc,speedvaluebaotouv5`（含**每房 10 份复盘**检查） |
>
> **当前状态（2026-09-26 11:40）**
> - **役 3 在跑**：`speedvalue`（共享基线）+ `speedvaluebc` + `speedvaluebaotouv5`；窗口 `started = 2026-09-25 20:52:39`。
>   判词 ETA（★ 按实测 **1.23 房/时/臂**（20:52→06:36 走 12 房）重算）：**80 房/臂 ≈ 9/28 13:41**；役盒 120/臂 ≈ 9/29 22:06。当前 **36 房（12/12/12）**；已完成的 35 房**复盘各 10/10 份**（唯一未齐的是刚结束那房，抓取延迟）。
> - **机制端点**：`speedvaluebc` 足迹 **15.6%**（预登记 10–20% 带内）✓；
>   **V 的机制 = 爆头/胡 与 赢分/胡 均上升**（R1520 修正：原代码误用番/胡）+ **护栏胡率 ≥ −1σ**（R1517 实现），
>   门槛 **≥3200 局（≈ 40 房/臂）**（R1521 修正：原 320 局只等于 4 房）⇒ 现为 `.v_mech_unknown`（**自愈、不阻塞**）；`_mech_watch` 每 6h。
> - **排期**：护栏 24h 口径 **3.96 房/时**（当晚自测 16.1 分钟/房）；全到盒收口投影 **10/04 20:22**（保守下界：决定性判词会提前收口）⇒ 截止 10/7 08:30 余量 **~2.5 天**。
> - **判词那一刻该读什么（全部已验）**：`_gate2`（主/副/机制/护栅 + **熔断两档**，R1522）→ 两半 Pareto（`hu_gap_split --by-arm`、`_seat_h2h --by-arm --top 32`）→
>   `_pick_arm`（**桌强表 + |z| 读法 + 否决预检**，R1505/R1512/R1518）。
> - **上场侧（已封口）**：`YouCaiBiKao=true` ⇒ 13 根同剂量孪生 + 18:50 自动映射（R1502/R1503）；`M>10` ⇒ `.EVENT_M_HIGH`（R1504）；
>   组合臂 **1s 响应窗口同步起爆全过**（R1523）。
> - **末端五步已重彩排**：10/5 提案（含否决/桌强/|z|）· 10/7 换臂（六步）· **10/8 提交门禁现为 GO** · 10/10 上线（fail-closed）· 10/10 T-5 保险。
> - **交付物**：本地全量 **1281 项全绿**；**公网 clone 实测 1101 项全绿（60 skipped）**——差的那 143 项是**需要本机对局语料/令牌**的模块**按模块自跳**（原因逐条都是“无 var/replays（clone）”“缺 .token_4test_*（clone）”，**没有一条是“缺应发布的脚本”**；见 §V.261） + 文案↔代码版本门 + 泄密门 + 闭包门 + **可移植性门（R1522）** + 行尾门（R1500b）。
> - **★ R1534（判词前的关键修复）**：V 的采用路径原本 **fail-open** —— `_adopt_pair.mech_state()` 只在
>   `_mech_watch` **判出“不达标”**时写 `.mech_warn`，而**判不出**（读数缺失/样本不足）写的是 `.v_mech_unknown`，
>   **采用链不读它** ⇒ 旧路径把“判不出”当成“机制正常” ⇒ **未验证的 V 可以被采用并起役 4 的 B 行**。
>   已修（`v_mech_verdict` + `apply_v_gate`，只在四格**由 V 决定**时拦；`unknown` ⇒ 原地不动）。
>   同轮还修了 `--dry-run` 会删真实 `.v_mech_unknown` 的副作用（`marker_action`）。
> - **★ 役 3 当前实测投影（非判词，读卡 §0c）**：两个候选**赢法不同** —— `speedvaluebc` = **赢得更大**
>   （爆头/胡 20.7%→**25.6%**、赢分/胡 18.29→**20.47**，机制两项都成立）；`speedvaluebaotouv5` = **赢得更频繁**
>   （胡率 24.66%→**29.00%**，但爆头/胡 **−2.2pp**）⇒ **机制第一项不成立** ⇒ 任何一种读法都是 **B3**。
>   9/28 最可能落 **NONE 行**（役 4 = `speedvalue` + `speedvaluemeldp45`），`speedvaluebc` 进 `.B2_CANDIDATES`。
> - **★ R1535（10/5 提案的素材缺口）**：预登记要求把「机制成立、主端点未证实」的臂（`.B2_CANDIDATES`）
>   **保留为备赛备选、与 §V.66 口径并行比较**，而生成比较材料的 `_final_pick_proposal.py` **不读它**
>   ⇒ 役 5 还在跑时，10/5 提案里**看不到役 3 的 B2 臂读数**（例如 `speedvaluebc`）。
>   已修：B2 标记头补 `窗口 since=`；提案新增独立一段（按**它自己那一役**的窗口跑 `_pick_arm`，标注与当前窗口不可混读）。
>   **纪律不变**：B2 不进自动池（§V.161 规则 4），只有人按 §V.247 显式改选才可能上。
> - **★ R1536（V 轴的测量管线是坏的，已修）**：`_lowprio_run.py` 在**计划任务的 pythonw 下会吞掉子进程输出**
>   （实测 176 字＝只剩 banner，真表 3600+ 字）⇒ `_mech_watch` 的 V 机制读数**每次都解析为空** ⇒ V 只能 UNKNOWN：
>   **「赢得大小」这根轴在生产里从没被测量过**（它是 `speedvaluebaotouv5` 的全部理由），
>   且一旦四格由 V 决定 ⇒ R1534 的 fail-closed 会停摆。已修（显式转发句柄）＋真·pythonw 回归测试（非空洞性已证）。
>   ⇒ **下一次 `_mech_watch`（~12:36）起读数不再为空**（会正常报「样本不足 ⇒ 不判」）；
>   **V 的第一次真判（PASS/WARN）要到「每臂 ≥40 房 ⇒ ≈9/27 06:36 那一次」**（按实测 1.23 房/时/臂）。
> - **★ R1537（10/10 的静默失效洞，已修）**：`_official_guard.py` 拉起 keepalive 时**不重定向输出**
>   ⇒ keepalive“启动即崩”时日志只剩一句「✓ 已按 spec 拉起」；而心跳第 2 步把「`.official_mode` 在位」
>   当**正常静默**、`_final_event_ready.py`（19:25）**也只查 mode 存在** ⇒ 两道关都“过”、分是 0 也没人报。
>   已修：两个 launcher 都留证（`var/_official_keepalive.out`）＋心跳新增**低误报**规则
>   （official_mode 在位 ∧ keepalive/run_bot 都不在 ∧ `_official_guard.log` 末 3 行有 ⚠/✗ ⇒ 立刻报人）。
> - **★ R1538（提交物缺文件，已修）**：闭包门只认字面量 `var/x.py`，看不见本仓惯用的
>   `os.path.join` + `var` 三段式引用 ⇒ 漏掉 3 个未被 git 跟踪的依赖，其中 `_replay_endpoint.py` 是
>   **`_gate2` 的导入期依赖** ⇒ **公网 clone 里役判词执行者直接崩**（实测 `FileNotFoundError` rc=1）。
>   已修（ROOT 从 `__file__` 推 + 三个补进 `$opsScripts` + 门加 `RX_JOIN`）；提交物门复验 **87/87 全绿**。
> - **★ 正式赛赛制（四测实测：§V.155 / §V.166）**：**切线淘汰制**（89 人海选 → 16 强 → 前 8 决赛；
>   四测我们第 6 名晋级 16 强、第 2 轮第 12/16 止步）。**资格线附近的 1 分价值 ≫ 名次中段** ⇒ 严格意义上
>   目标是 **P(晋级)** 而不是 E[分]。计划 §V.156 第 4 条**已明确决定不做**“阶段感知风险偏好”轴（只在有役 7 余量、
>   且前三条轴都已决策时才考虑）⇒ **我们上场的是 E[分]/强手房最优的臂**，这个取舍是**已知且已登记**的，不是遗漏。
>   另：“淘汰即干净退出”已在四测真机验证（`intent=eliminated` ⇒ exit 0，无僵尸轮询）。
> - **★ R1542（9/28 那个自动动作的保命修复）**：`_bsegment` 切役成功、但**看护注册失败**时不落任何记录
>   ⇒ `_adopt_pair` 每 10 分钟重试会**重跑整个 B 段**、用**新时戳重切役** ⇒ 已打的房全废且**无限重切**（排期护栏也看不出来）。
>   已改为幂等：切役后先落 `.bsegment_switched_<label>`，重试时若与当前 `.ab_mode` 一致就**只补注册**、不再重切。
> - **★ R1543（R1542 的下游）**：`.adopted_pair_役3` 原本记「**算出来的**」役4 配置；重试路径会重算四格，
>   若判定翻转，标记就指向**没在跑的臂** ⇒ `_next_yaku_notice` 找不到哨兵 ⇒ **役4→役5 静默停摆**。
>   已改为**以 `.ab_mode` 的实际配置为准**（不一致时大声记日志）。
> - **★ R1546（9/28 读判词时的关键提示）**：**决赛相似层（`>=2 top32`）天生很薄** —— 今天占比仅 19–27%，
>   投影到 80 房/臂只有 **15–21 房**，正好贴着否决门槛 15 ⇒ **9/28 的「决赛相似层否决」可能判 UNKNOWN**。
>   届时**不许只看 rc**：`_strong_veto` 会照样打印该层的「分/房」「第1率」原值，**必须人读方向**（§V.183）。
>   预登记的 per-役窗口与 15 房门槛**不动**；更宽窗口重跑只能算 **off-prereg 旁证**，不能当判据。
> - **★ R1549（10/5 决策材料的最后一块）**：10/5 提案（`_final_pick_proposal.py`）现在**自己跑两半 Pareto**
>   （R1501 认定的**真正判据**）——此前它只把这两条写在「下一步」里，人工很可能没跑就定臂。
>   ⇒ 10/5 的提案已同时含：主序列+分层读数、**两半 Pareto**、B2 备选臂（R1535）。
> - **唯一人工输入**：10/10 前把当天令牌存成 **`var/.token_final_20261010`**（缺它 ⇒ 不上线）。

# 交接 Prompt —— 杭麻 SpeedA Bot 项目（交接日期 2026-09-09 夜）

> ⚠️ **2026-09-10 大更新（务必先读这一段，它会覆盖下文若干结论）**
>
> 本文下半部分写于 2026-09-10 之前，其**核心判断「规则空间已打穿、需换算法」
> 已被推翻并重建**。当天做了下列已验证改动（详见对应报告）：
>
> **1. 执行层致命缺陷（最大发现）**：吃/碰提交 **67.8%/77.5% 被服务端 409 拒绝**
> ——`bot/api.py` 把 `/state` 轮询与 `/action` 提交挤在同一个 12/s 令牌桶里，M=10
> 并发下动作排队 ~800ms，超过 1s 响应窗口；1109 次失败 claim 中 **66% 那张牌最终
> 无人要**（我方迟到，非被抢）。修复（拆桶 + notify 帧合并 + `/state` 瞬退重试）后：
> **409 38.5%→0.7%、claim 成功率 27.8%→96.6%、落地副露 0.198→0.94/局**。
> 报告：`docs/iter/reports/exec-layer-409-2026-09-10.md`。
> ⚠️ 因此 **C010/C013/C017 等"自动房排除"结论全部作废**——它们建立在坏执行器上。
>
> **2. C019 听牌升级副露（当前最优策略家族）**：原「已听不副露」判据亲手堵死了爆头
> 路线。改法：`before>0` 沿用 mild；`before==0` 仅在「副露+最优弃牌」后仍是听牌且
> 等待严格变多时接受（爆头 34 张必然通过）。真机 A/B（各 160 席局）：
> **听牌 48.1%→65.0%、胡 16.9%→29.4%、爆头 0%→5.0%、副露 0.97→1.34**。
> 报告：`docs/iter/reports/C019-tenpai-upgrade-2026-09-10.md`。
>
> **3. 当前最优与战绩**：`--strategy speedtu`（3 房均值：听牌 **62.1%**、胡 **27.9%**、
> 爆头 3.3%、**均名次 3.0、均分 +12**；修前同类基线为 37.2%/16.0%/**−196**）。
> 拿过两次 **rank2（+197 / +106）**——此前从未有过。策略默认仍是 `speedtm`，
> **切换需用户批准**。相关策略：`bot/speedtu.py`（最优）、`bot/speedtw.py`（sim 优、
> 真机 n=3 未复现，已降级 paused）、`bot/speedtv.py`、`bot/speedtma.py`（均判负/暂停）。
>
> **4. 番数缺口是结构性天花板（已 12 代否证）**：我方 fan/胡 1.12 vs 冠军 1.34-1.55，
> 差距几乎全在**爆头胡占比 9.8% vs 25.3%**。但 12 代策略改动
> （C013/W/WM/godoff/keep1/keep2/C024/C025/C026/C027/C028/C029）**全部无法提升爆头**，
> 覆盖「弃牌评分/白保留/对子保留/听牌宽度/七对路线/弃白扰局/不喂副露」七个方向。
> → **爆头率由「副露机会 + 别家是否打出触发牌」决定，不由弃牌策略决定**。
> 继续追番前请先读 `journal.md` 的 R17/R18。
>
> **5. 方法学工具（新增，可复用）**：`var/_score_model.py` 证明
> **「胡率 × fan/胡」与实际房内得分 r = +0.95** → 可作快速代理指标；
> `var/_fan_real.py` 从全信息复盘重建胡牌手牌算真机番数；
> `var/_bao_headroom.py` 量化爆头可达面；`HM_API_TRACE=1` 出请求级追踪
> （`var/api_trace.jsonl`：wait_ms vs http_ms）。
>
> **6. v29 适配**：`GUIDE_VERSION_KNOWN=29`；`/api/match` 关闭时 403
> FEATURE_DISABLED 是**永久条件不要重试**，已在 `match_super`/`run_bot` 分流。
> preflight 为 READY；单测 162 全绿。
>
> **7. 2026-09-10 23:40 续记（比上面更新）**
> - **C040（顶级弃牌行为克隆）否证**：tie 层 MLP 验证 top1 58.3% vs 静态基线 54.6%，
>   但 sim 门禁 V1 听 33.3%/胡 **0.0%**、V2 听 62.5%/胡 16.7%（同批 SpeedTUG 66.7%/37.5%）
>   → 与 C039 同源失败：**预测一致率 ≠ 棋力**（模型学的是风格，如「拆东对留南孤」）。
>   脚本 var/_c040d_gate.py、var/_c040d_diag.py；报告 docs/iter/reports/C040-tie-bc-2026-09-10.md。
> - **E002：同账号并发开房不可行**——第二个 `match_super` 的 `/api/match` 返回的是
>   **正在进行的那个房**（不新开房），重复 `run_bot` 会与主实例抢同一席位
>   （日志出现「手牌形态暂不一致」+409）。**刷分速率上限 = 单房串行 ≈ 4 房/小时**，
>   严禁并发启动第二个 match_super。
> - **speedtug（C036）真机 8 房**：均分 **+13.6/房**、均名次 **2.25**（2 次 rank1）；
>   机制：听牌 **55.2%**、胡 **25.5%**、**fan/胡 1.227**、**爆头胡 21.3%**
>   （同房冠军池 1.340 / 26.5%；修前我方 1.138 / 7.9%）→ **番数缺口基本补齐，
>   剩余缺口集中在听牌率 55% vs 63%**。排行榜 -6057（56 房 / 3 次第一 / rank 524）。
> - 转正数学：+13.6/房 → 需 ~445 房（~106h）；+33/房 → 需 ~184 房。
>   批次接续：run17（10 房）→ 自动接 run19（24 房，见 var/_chain.py、var/_run19.out）。
> - 工具：`var/_rate.py`（近 N 房均分/SD/均名次）、`var/_fan_real2.py`（按房前缀真机番数/爆头胡）。
>
> **8. 2026-09-11 00:15 续记（方向性修正 + 当前状态）**
> - **★ 房内得分 = 番数，不是听牌率**：52 房 × 4 席回归（`var/_score_drivers.py`）
>   `score ≈ 18.3 × 番和`，R²=0.83；corr(番和)=+0.895、corr(胡数)=+0.819，
>   而 corr(听牌数)=+0.121（OLS 系数为负）。→ **后续优化目标锁定「番和 = 胡数 × fan/胡」**，
>   不要再把"听牌率 55% vs 63%"当头号缺口（那部分是坏手局的自然结果）。
> - **番数缺口只剩一种牌型**：同房 141 胡 vs 顶级 267 胡，
>   我方 fan1 77.3%/fan2 22.7%/**fan3+ 0%**；顶级 74.2%/22.8%/**3.0%**，
>   且顶级 9 例高番胡**全部是「七对+爆头」fan4（门清）**。
> - **缺口位置已定位**：门清爆头机会 我方 0.10% 席局 vs 顶级 1.40%；
>   但**机会出现时我们 22/22 全取（与顶级同为 100%）** → 卡在「成型」不在「临门」。
> - **C036 真机核对通过**：C036 上线后 9 房 51 次"弃胡可换爆头"机会 → 弃胡 19 次（19/19 正确），
>   其余 32 次是番2→番2 不划算（规则判定正确）。历史房同样机会 196 次只弃 3 次（策略差异确凿）。
> - **C041（门清七对路线保护）rejected**：sim 3 seed×192 局，代理 0.417 vs 0.422、
>   爆头 13.0% vs 14.1%、fan 1.468 vs 1.519（触发真实）→ 不送真机。
>   报告 docs/iter/reports/score-drivers-qidui-baotou-2026-09-11.md。
> - **比赛改为不间断接力**：`var/_keeper.py`（守夜人，严格单实例）保证始终恰好一个 match_super；
>   当前链条 run17(10 房) → `var/_chain.py` 接 run19(24 房) → keeper 每轮自动 12 房。
>   排行榜 -6196 → **-5663**（57 房 / 4 次第一 / rank 522），speedtug 近 9 房均分 **+55.9**、均名次 2.11。
>
> **9. 2026-09-11 00:45 续记（★ 实力定位更正 + 策略收敛）**
> - **★ 我们目前在同房是领先的一方**（`var/_inroom_gap.py`、`var/_room_ab.py`，7 房同基准）：
>   我方均分 **+33.4**、胡 **20.1 把/房**、番/房 **24.7**；同房三对手均 **−11.1**、18.0、23.3
>   → **净胜 +44.5 分/房**（胡牌多 2.1 把/房，番和反而更高）。
>   之前的「落后顶级」是拿我们对比**按名字挑出的最强 bot 子集**，不是实际对手。
>   因此总分 −5xxx 的主因是**历史欠账**（执行层 409 缺陷期的 47 房），不是当下实力。
> - **策略已在本轮可测空间内收敛**（全部 sim 否证）：C041 门清七对路线保护（0.417 vs 0.422）、
>   C042 副露门控严格度 D1/D2（0.396/0.417 vs 0.422）、C043 P_conv 0.50/0.85（0.354/0.422）；
>   叠加此前 C038/C039/C040 否证 → **当前 speedtug = 本地最优**。
> - 剩余理论增益仅「七对路线」（fan/胡 1.227 vs 对手 1.276 ≈ +15~18 分/房），
>   但三种改法都过不了 sim 门禁 → **不再投机，改为靠量**。
> - 当前战绩：排行榜 **-5544**（58 房 / 4 次第一 / rank 521），本会话净 **+652**；
>   speedtug 10 房均分 +43（含 +394/-239 两极）。守夜人保证接力不中断。
>
> **10. 2026-09-11 01:00 续记（三个方向永久关闭 + 状态）**
> - **E004 点炮不存在（永久）**：1600 局全信息复盘，`round_ended` 前一个事件 1545 次是
>   `tile_drawn`（自摸）、**仅 1 次是 `tile_discarded`（0.06%）** → 纯自摸制，弃牌不送分。
>   **防守/安全牌族（C004）永久归档**。本游戏 = 纯竞速（先听先自摸）→ 优化目标锁定
>   「听牌速度 × 等待宽度 × 番数」，与 C019/C036 方向一致。
> - **C044 白保留 = 结构性 no-op**：随机 1621 手含白牌，`_best_discard_t` **弃白 0 次**；
>   sim 220 个含白决策，硬保留规则改变 0 次 → `_pref` 的白=4 早已保护到极致。
>   **白保留整族（C013/C024-C029/C044）从根上无优化空间，永久关闭**。
>   （真机 0.021 次/席局的弃白来自抓打圈强制弃刚摸，是规则不是决策。）
> - **E003 杠**：我方 0.027 杠/局（**暗杠 0、补杠 0**），对手 0.069（暗 0.01/补 0.03）。
>   但 `mahjong/sim.py:_own_gang` 不给补牌、弃牌断链 → **sim 天生测不到杠的「多摸一张+杠开」价值**，
>   只能真机 A/B，预估 +1~3 分/房 → 低优先级已记档。
> - **状态**：排行榜 **-5587**（59 房 / 4 次第一 / rank 521），本会话净 **+609**；
>   近 8 房均分 **+72.9**、均名次 2.12。按 +52~73/房，转正需 **~77–107 房（约 19–26 小时）**。
>   接力链四进程健在（match_super / _chain / _keeper / run_bot）。
>
> **11. 2026-09-11 00:40 续记（长跑守护 + 进程匹配缺陷修复）**
> - **新增 `var/_watchdog.py`（长跑看门狗）**：每 90s 检查；判「卡死」= 最近房日志 mtime >20min
>   且 `auto_ranking.jsonl` mtime >20min。卡死时：
>   ① 若 match_super 输出尾部有 ≥3 次「match 失败」→ 轮换全局令牌（`var/_rotate_token.py`）
>     并杀掉卡死实例，交给 keeper 重启；
>   ② 若无 run_bot 存活（监督器卡住）→ 杀 match_super 交 keeper；
>   ③ 其余只记日志。**正常对局中绝不动作**（长局的日志始终在写）。
> - **⚠️ 修复一个危险缺陷（keeper/watchdog 通用）**：原进程匹配用
>   `"match_super.py" in cmdline`，会被**指令里恰好含该字符串的无关进程**（如 agent 的
>   shell/codex runtime）误匹配——实测一度把 match_super 数成 2、run_bot 数成 2。
>   已改为精确匹配：**可执行名含 python + argv 中存在基名等于脚本名的参数**。
>   附带修 `psutil.pid_exists(0)` 在 Windows 返回 True（PID 0 = System Idle）导致
>   守护进程读锁即退出的问题（改为 `pid > 0 and pid_exists`）。
> - **当前守护链（每个脚本恰好 1 实例，已核）**：
>   `match_super(2324)` → `run_bot(12980)` / `_chain(29008)` / `_keeper(21472)` / `_watchdog(8804)`。
>   停止长跑 = 杀 keeper + watchdog + match_super（三者）+ 其 run_bot 子进程。
> - 状态：排行榜 **-5587**（59 房 / 4 次第一 / rank 521）；近 8 房 **+72.9/房**。
>
> **12. 2026-09-11 01:00 续记（★ 顶级差距精确量化 —— 下一阶段的靶子）**
> - **E005 同房同基准画像**（执行层修复后 12 房；我方 560 席局 vs 朱雀/卢本伟/豆包 480 席局）：
>
>   | 项 | 我方 | 顶级三强 |
>   |---|---|---|
>   | 胡/局 | 0.255 | **0.300（+17.6%）** |
>   | fan/胡 | 1.210 | **1.299** |
>   | **爆头/局** | **0.054** | **0.081（+50%）** |
>   | 番和/局 | 0.309 | **0.390（+26%）** |
>   | 听牌率 | 55.2% | **60.4%** |
>   | 首听巡 | 6.35 | **6.23** |
>   | 副露/局 | 1.196 | 1.050 |
>
>   差距**可完全分解**：多 0.045 胡/局（≈+0.054 番）+ 多 0.027 爆头/局（≈+0.027 番）。
> - **C045（收紧未听副露）rejected**：sim 3seed×192 局，副露 1.661→1.172（确实降到顶级水平）、
>   胡率 +2.1pp，但**爆头 −3.7pp、fan −0.13 → 代理 0.411 vs 0.422**。
>   即「副露既买速度也买爆头」，单边收紧过不了门禁。同类权衡已在 C041/C042/C045 三次复现。
> - **当前墙**：一切规则级旋钮（副露门控/白保留/七对路线/爆头阈值/tie-break）都撞在
>   「速度 ↔ 番数」这条权衡线上；顶级三强是**同时**更快、更多爆头（不是权衡结果）。
>   → 下一阶段只能换决策算法（rollout / 自对弈值函数），**需用户批准后才动 bot/**。
> - 状态：排行榜 **-5483**（60 房 / 4 次第一 / rank 522），近 8 房 **+85.1/房**、均名次 2.12；
>   按 +45~85/房，转正还需 **~65–122 房（约 15–29 小时）**。
>
> **13. 2026-09-11 01:15 续记（爆头差距构成 + 两个排除项）**
> - **E006 顶级爆头机会的「构成」才是差距**（修复后同房 12 房；机会数我方 51 vs 顶级三强 76）：
>
>   | 维度 | 我方 | 顶级 |
>   |---|---|---|
>   | e=0（门清） | **8** | **22** |
>   | e=1 / e=2 | 14 / 17 | 23 / 28 |
>   | e=3 | **12** | 3 |
>   | 手中白 ≥2 | **23** | **49** |
>
>   → 顶级的是**门清 + 多白型**爆头（七对家族），我们偏「多副露 + 单白」型。
>   注意：门清「5对2白」「6对1白」形态双方都极罕见（0% vs 0.2%），说明差距来自**更早期的
>   手牌形态选择**，不是最后一步。
> - **C046 财飘 = 频率 0**：全量 4168 席局里「已爆头且摸到白」只有 **1 次**（0.02%）。
>   `fan.py` 确认财飘是链+1 番翻倍，而 C036 的 EV 没算这个 ×2（理论该修），但频率决定它**无价值**，
>   C008 关闭结论成立。
> - **排除「偷懒弃白」嫌疑**：全量我方弃白 88 次中只有 4 次在强制窗口内；**修复后 12 房里 5 次弃白
>   有 4 次紧随 timeout**（超时产物，非策略 bug）。超时/席局：我方 **9.05** vs 顶级 17.5
>   （顶级更多是主动不响应窗口）→ 超时不是我们短板。
> - 状态：排行榜 **-5722**（61 房 / 4 次第一）；run19（24 房）已在跑（当前第 1 房），
>   守护链 match_super/run_bot/keeper/watchdog 齐全。
>
> **14. 2026-09-11 02:00 续记（方案A rollout 已实测否证）**
> - **C048 rollout 原型 rejected（4 变体全负）**：sim 基线代理 0.422；
>   V1(P(听牌),k=2) 0.266；V1b(修向听顺序 bug) 0.344；V1c(候选按基线同键) 0.281；
>   V3(目标改 P(胡牌),k=3) **0.297**。
> - **失败机制（关键）**：听牌率能维持（64.1% vs 基线 67.2%），但**胡率崩到 18.8~21.9%
>   （基线 29.7%）**——rollout 只优化「到达听牌」，不管「听形能不能胡」（自摸制下必须真摸到），
>   也不建模等待宽度质量/番数爆头/副露/对手。且耗时 8s/局（基线 0.8s/局）→ 真机 1s 窗口放不下。
> - **下一步若要继续方案A**，必须同时满足：目标 `P(胡)×E[番]`、状态含副露与爆头潜力、
>   等待宽度进判定、性能靠缓存/蒸馏。详见 docs/iter/plans/rollout-next-level-plan.md §5。
> - 状态：排行榜 **-5761**（64 房 / 4 次第一）；run19（24 房）第 4 房在跑；
>   近 10 房 +16.8/房（近期偏冷，SD~170 波动正常）。
>
> **15. 2026-09-11 02:05 续记（三项审计 + 性能验证）**
> - **E007 执行层修复的生产验证（保持）**：全部决策日志 17 万决策点 ——
>   **修复后 draw/discard 409 = 0.01%（3/24,090）、碰 1.33%、吃 2.77%、胡 0%**；
>   修复前 3.21% / **68.99%** / **57.27%**。→ E001 修复在生产持续有效，执行层不再是瓶颈。
> - **E008 catch_play 审计（无问题）**：摸牌决策 44,980 个，`cp=true` 仅 307 次（0.68%），
>   其中 299 次（97.4%）正确弃刚摸；`cp=false` 弃刚摸 20,664 次属正常战术偏好。
> - **E009 门清「5对+白」路线：noise-dominated，方向封存**。到达率 4.7% vs 顶级 5.3%（同量级）、
>   该状态下弃牌选择完全一致（都弃单张保对保白）、首次进入巡目我们 **6.57 更早**、剩余摸牌 **4.0 vs 2.6 更多**；
>   转换数（2 vs 9）落在个位数样本 → 不足以支撑规则改动，**勿再投入**。
> - **最新 12 房机制体检（同基准，每席）**：我方 胡 20.4/房、番 27.1、**fan/胡 1.327**、
>   **爆头 5.83/房**、均分 **+36.8**；同房三对手 胡 17.9、番 23.0、fan/胡 1.284、爆头 3.50、均分 −12.3。
>   → **我们已全面领先对手（含 fan/胡 首次反超）**，近期「rank2 且小负」是波动非回归。
> - 状态：排行榜 **-5761**（64 房 / 4 次第一）；run19 第 5 房在跑；守护链齐全。
>
> **16. 2026-09-11 02:15 续记（★ 方向的第二次修正：从「追番数」转为「追胡率」）**
> - **E010 画像反转**（最新 12 房同基准逐席）：我方 胡 **20.42/房**、番 27.08、
>   **fan/胡 1.327**、**爆头/房 5.83**；顶级席 胡 **23.00**、番 29.86、fan/胡 1.298、爆头/房 4.71。
>   → **fan/胡 与爆头率我们已反超**，唯一落后是**胡牌频率（−11%）**。
>   番和/房 = 胡 × fan/胡（27.08 vs 29.86）→ **新靶子 = 提高胡/房**（听牌率与听牌转化），
>   而 C036 之后「追番数」已不再是短板。
> - **E011 弃胡对照（排除）**：可胡点弃胡率 我方 14.4% vs 顶级 16.0%，且都几乎只弃成爆头（58/58、38/39）。
> - **E007/E008/E009**：执行层修复生产验证（draw/discard 409=0.01%）、catch_play 无问题、
>   门清5对+白方向因噪声封存。
> - 状态：排行榜 **-5780**（65 房 / 4 次第一）；run19 第 5 房在跑；守护链齐全。
>
> **17. 2026-09-11 02:20 续记（新增排除项 + 长跑链路验证 + 剩余价值的量化）**
> - **E013 喂牌率无差距**：弃牌被对手吃/碰/明杠 我方 11.17% vs 顶级 11.36% vs 其他 11.68%
>   → 纯竞速下唯一互动杠杆也没有差距（第 14 个被排除的方向）。
> - **E014 长跑链路全部验证**：接续器实测生效（run17→run19 仅 5 秒间隔）；
>   keeper 启动路径用 monkeypatch 单元测试验证（7 项校验 PASS，命令/cwd/重定向均正确）。
>   → 无人值守 28 小时计划的每个环节都有证据。
> - **剩余价值的量化**（供决策）：顶级 胡/房 23.00 vs 我方 20.42 → 若能补上这 2.58 把，
>   按我方 fan/胡 1.327 与 `score≈18.3×番和` 折算 = **+62 分/房**（+37 → ~+99/房），
>   转正时间从 ~28 小时缩到 **~14 小时**。这就是「换算法」这条路的全部奖金上限。
>
> **18. 2026-09-11 02:35 续记（缺口定位到「中局转化」+ 副露时机错位）**
> - **E015 ★缺口在「第 4–7 巡的中局转化」**（消除截断效应后的干净对照）：
>   ≤3 巡已听 **6.5% vs 7.5%**（持平）、≤5 巡 23.9% vs 25.5%、**≤7 巡 40.2% vs 47.1%（−6.9pp）**。
>   早巡我们不吃亏，**中局把「没听的手」转成听牌的能力落后 ~7pp** —— 这是一切规则改动的靶心。
> - **E016 副露时机错位**：顶级在**已听(s0)时副露 16%**（我们 10%）、在 s1 副露 28%（我们 39%），
>   且我们副露整体晚 0.3 巡。下一个候选：**s0 副露门控从「等待种类严格增加」放宽到
>   「种类不降 + 活张数增加」**（利用剩余张数信息，C038 当年只测了全向听级张数、未测 s0 门控）。
    **→ 已实测否证（C049）**：sim 3seed×192 局代理 0.417 vs 0.422（听牌 +0.5pp、fan +0.014、
    副露 +0.05 方向对，但胡率 −0.5pp）。**至此候选总数 15 个，全部否证。**
>
> **19. 2026-09-11 02:50 续记（价值模型：可预测但不可行动 —— 候选总数 16）**
> - **E017 中局转化价值模型**：真实复盘 71,073 个中局状态 → MLP 验证 AUC **0.921**；
>   关键是**同向听内 AUC 0.709~0.742**（说明「同向听下哪种手形更能听牌」确实可预测）。
> - **C050（用该模型重排同向听弃牌）= rejected 且大幅负**：sim 代理 **0.312 vs 0.422**
>   （听牌 67.2%→54.2%）。
> - **教训（重要，写入方法论）**：**预测性 ≠ 可操作性**。模型预测「这手牌最终会不会听」
>   时，学到的很大部分是「这手牌本身好不好（含未来摸牌运气）」——这是**选择效应**；
>   而决策需要的是反事实信号「弃这张 vs 那张，哪个让结果更好」。同类失败已第三次
>   （C039 静态偏好、C040 行为克隆、C050 价值重排），**除非做真正的反事实评估（rollout/离线搜索），
>   否则再多的监督学习都无法转成棋力**。
> - 状态：排行榜 **-5739**（66 房 / 4 次第一）；run19 第 6 房在跑；守护链齐全。
>
> **20. 2026-09-11 03:00 续记（监督学习族彻底关闭 —— 候选总数 17）**
> - **C051 = rejected（大幅负）**：sim 内 ε=0.35 探索生成 9,088 个 (状态,动作,结果) 样本，
>   训练 Q(state,action)→本局胡牌（验证 AUC 0.673）；用它选牌 → 代理 **0.292 vs 0.422**
>   （听牌 67.2%→49.0%，75% 决策被改）。
> - **四连否证**：C039 静态偏好、C040 行为克隆、C050 状态价值（AUC 0.921 却打得更差）、
>   C051 动作价值（含 ε-探索）。**监督学习在本问题上无法超过启发式**——单步弃牌的效应
>   远小于整局结果方差，小样本无法把它从状态质量里分离出来。
> - **本会话总计：17 个候选全部否证/收敛**（C038–C051 + 杠/白保留/防守三族）。
>   当前 speedtug 是**实用上限**：同房净胜 +49/房、fan/胡 1.327（已超顶级）、爆头/房 5.83（超顶级），
>   唯一差距是**中局（4–7 巡）转化 −6.9pp**，而这一项已证明无法用手写规则或小规模学习补上。
> - **下一步只有两条**：① 继续跑量（确定路径，~28 小时转正）；② 投入大规模搜推
>   （rollout 搜索 + 大值网络，工程量大、需算力，且不保证成功）。
>
> **21. 2026-09-11 03:20 续记（★★ 重大更正：请以此条覆盖前面所有「我们已追平/反超顶级」的说法）**
> - **E019 番数重算不可信**：用 fan.py 重算房分对照服务器原始 `total_score` ——
>   **每房误差均 19.9（中位 18）、33 房只有 4 房完全一致**（fan.py 自带 KNOWN-DELTA）。
>   因此 E005/E010/E012 里「fan/胡 1.327 超顶级」「爆头/房 5.83 超顶级」等结论**全部作废**。
> - **用服务器原始分数的真实分级（修复后 36 房）**：我方 **−0.2/房**（95%CI [−48,+48]，均名次 2.58）、
>   顶级席 **+171.1/房**、其他 **−41.2/房**；**同房配对 17 房：我方 − 顶级 = −214.1/房**（中位 −175）。
> - **E020 只用可靠指标的同房对照（17 房 / 5120 席局）**：
>   我方 听牌 **55.8%** / 胡 **24.8%** / 副露 1.241；顶级 **62.4% / 31.3% / 1.128**；其他 43.4% / 19.6% / 0.865。
>   → **真实格局 = 顶级 > 我们 > 其他**；核心差距是**听牌 −6.6pp、胡 −6.5pp**（与我们早先 E015
>   「中局转化落后」一致，但这次是干净样本）。顶级**副露比我们少**而听牌更多 → 副露质量仍是唯一
>   未被完全解释的点，但 C018/C019/C041/C042/C045/C047/C049 已多轮否证各种门控改法。
> - **推论**：按 −0.2/房 的现状**分数永远不会转正**；必须拿到真实棋力提升（+50~130/房）才有意义。
>   这是「换算法」路线的新论据：它不再是「锦上添花」，而是**转正的唯一途径**。
>
> **22. 2026-09-11 03:50 续记（★★ 门禁口径错配 —— 旧否证作废，候选复活）**
> - **E021**：把 sim 门禁对手从 `3×SpeedTM`（弱场）换成 `3×SpeedTUG`（强场=我方同级）后，
>   同一批候选结果**完全翻转**：
>   弱场代理 基线 0.422 → C042-D2 0.417 / C045 0.411 / C047 0.422 / C049 0.417（当初全判否）；
>   强场代理 基线 **0.286** → **C042-D2 0.359 / C045 0.365 / C047 0.354 / C049 0.365
>   （全部 +24~27%）**。
>   → 这四者共同点 = **少副露**，与真机事实吻合（顶级副露 1.128 < 我方 1.241）。
>   **C038–C051 中所有「副露/门控类」否证一律作废，须用强场口径重跑。**
> - **E022 真机策略对比不可判定**：speedtu（12 房）均分 −25.3、听牌 61.1%；speedtug（20 房）
>   均分 +29.6、听牌 54.1%；但两组对手强弱/时段不同（对手听牌 52.6% vs 44.2%）→
>   分数差 +55（SE≈40）不显著、**机制差 −7pp 提示 speedtu 可能更好**。需要随机交错 A/B。
> - 正在进行：**强场 5-seed 复现**（验证上述增益稳定性），通过后再谈真机 A/B。
> - **5-seed 复现通过**：强场代理 基线 0.334 → **C045-strict 0.409（+0.075）**、C047 0.403、
>   C049 0.375；听牌 58.8%→66.2%、胡 25.3%→32.5%、**副露 1.55→1.20**（与真机顶级副露更少一致）。
> - **C052 已落地并进入真机 A/B**：新策略 `bot/speedtugc.py`（SpeedTUGC = SpeedTUG 把 before>0
>   副露判据收紧为「向听严格下降」；已听分支/C036 弃胡原样保留），注册进 run_bot.py；
>   **162 单测全绿 + preflight READY**。
> - **E023 A/B 守夜人上线**：`var/_keeper_ab.py` 按批交替 speedtugc / speedtug（每批 4 房），
>   状态记录 `var/_ab_state.json`、`var/_ab_log.jsonl`；**批 #1（speedtugc）已于 03:18:55 开跑**。
>   分析口径：只用服务器分数 + 听牌/胡/副露（禁用番数重算，见 E019）。
>   **注**：本次为启动 A/B，已停止原 run19（第 10 房中途；分数本就持平）并停用旧 keeper。
>
> **23. 2026-09-11 03:50 续记（组合候选否决 + A/B 首批数据）**
> - **C053 组合候选（严格副露 + 活张升级）rejected**：强场 5seed 代理 **0.372** < C045 单独 **0.409**
>   （听牌同高 67.5%，但胡率 29.7% < 32.5%）→ **C045（已部署 speedtugc）确认为最优候选**。
> - **真机 A/B 首批**：speedtugc 前 2 房 **rank1 +228 / rank2 +58**（+143/房，n 太小不作结论）；
>   排行榜 **−5258（72 房 / 7 次第一 / rank 520）**，本会话新高。A/B 继续按批交替（每 4 房）。
> - **C054 定向搜索（强场 3seed）**：V-A「只允许副露直接听牌」0.302、V-C「3 向听以上不副露」0.354，
>   均 **< C045 的 0.365** → **C045（严格向听下降）就是甜点**，已部署候选无需更换。
> - **A/B 进行中**：speedtugc 已完成 3 房（均分 +86.7、均名次 2.00）；机制读数仍等你 ≥8 房/臂。
> - **E024 强场重测又救回两个候选**（3seed）：基线 0.286 → **C041-v1 0.375（+0.089）**、
>   **C038-ukeire 0.365（+0.078）**、C045 0.365、C039 0.328、C043 P0.50/0.85 = 0.271/0.286。
>   → **「弱场门禁」把 C038/C039/C041/C045 全部误杀了**；其中 C041（门清七对路线保护）略优于
>   已部署的 C045。已启动 **5 seed 强场头对头**（基线/C045/C038/C041）选优，
>   选出的最优者将作为**第二个 A/B 候选**（当前 A/B 跑完后接上）。
> - **E025 5-seed 头对头结果**：基线 0.334(±0.077) → **C045 0.409(+0.075)**、C038 0.391(+0.056)、
>   C041 0.391(+0.056) → **已部署的 speedtugc(=C045) 确认为最优**，A/B 测的就是最好的那个 ✓
>   （C038/C041 留作后续备选，不必急着换）。
> - **真机 A/B 中期读数**（2026-09-11 04:30）：speedtugc 4 房 均分 **+68.2**、均名次 2.25、
>   听牌 **56.6%**、胡 **27.2%**、**副露 0.84/局**（speedtug 历史 1.24 → 候选确实少副露 32% ✓）；
>   speedtug 1 房 −179（n=1 无意义）。
> - **C056/C057 正交组合搜索 = rejected**（强场 3seed）：C045 0.365；**C056（+C038 ukeire 弃牌键）0.276**
>   （比不带还差）、**C057（+C041 门清保护）0.359**（无增量）→ **弃牌键与副露门控不叠加**（都在优化同一件事）。
>   **C045 在 9 个变体中确认为最优**，已部署的 speedtugc 无需更换。
> - A/B 进展：speedtugc 4 房（+68.2/房、听牌 56.6%、胡 27.2%、副露 0.84）；speedtug 2 房（−106/房）。
> - **C058 s0 分支验证**：砍掉 C019 换听升级（A）或只保留爆头换听（B）→ 代理都掉回 **0.286**
>   （与基线同），而 C045 是 0.365 → **「严格未听 + 已听换听升级」是最优组合，不能再动**。
> - A/B 进展：speedtugc 4 房（**+68.2**，听牌 56.6%、胡 27.2%、副露 0.84）；speedtug 3 房（**−130.7**）。
> - **E026 混合场稳健性**：弱场 −0.010 / 混合场 **+0.047** / 强场 **+0.078** → C045 的增益随对手变强而增大，
>   **不是自对弈偏袒**。
> - **E027 A/B 中期（各 4 房 / 320 席局）全指标同向**：speedtugc +68.2 分、名次 2.25、听牌 **56.6%**、
>   胡 **27.2%**、副露 **0.84**；speedtug −112.5、2.75、50.6%、23.1%、1.24。
>   差：**+181 分/房、+6.0pp 听牌（SE≈3.9pp）、+4.1pp 胡、副露 −32%** —— 与 sim 预测方向一致，
>   但 **n=4 不足**，需 ≥8–12 房/臂。
> - **E028 历史 backlog 强场清仓（重大方法学结论）**：把 C022–C037 的旧候选全放到强场重跑 ——
>   **几乎全部为正**：GodOff +0.036 / Safe +0.031 / Keep1 +0.031 / ChiStrict +0.021 /
>   BaoTie·BaoWidth +0.016 / Keep2 +0.010（基线 0.286）→ **弱场口径把整批旧候选系统性误杀**。
>   但都低于 **C045（0.365）**，故不影响当前部署决策。
>
> **24. 2026-09-11 07:15 ★★ speedtugc 晋级为比赛策略（本会话最大成果）**
> - **A/B 最终（各 ~8–9 房，逐批交替，避免时段混淆）**：
>   **speedtugc 9 房 +130.7/房、均名次 1.89、第一名 55.6%（5/9）、听牌 55.8%、胡 29.8%、副露 0.81**；
>   speedtug 8 房 −63.5、2.75、12.5%（1/8）、50.6%、23.1%、1.24 → **四项预设标准全过**。
> - **佐证链**：强场 sim +0.075（5seed）/ 混合场 +0.047（非自对弈偏袒）、真机副露 −35%、
>   执行层零 draw-409、历史 speedtug 22 房基准仅 +28.3/房·23% 第一名。
> - **已切换**：停 A/B 守夜人 → `var/_keeper.py speedtugc 12`（pid 27652）接管；当前批次自然跑完后
>   全程 speedtugc。**回滚**：`var/_keeper.py speedtug 12`（步骤见 docs/iter/plans/speedtugc-promotion.md）。
> - **方法学教训（写进 HANDOFF）**：本次真正的突破不是新算法，而是**修掉了门禁口径**
>   （弱场 3×SpeedTM → 强场/混合场）——它把 C038/C041/C045、C022/C023/C029 等**十余个被误杀的候选**
>   一次性复活，C045 由此被发现并在真机复现。后续任何候选评估**必须用强场/混合场口径**。
> - 排行榜同期：**−6196（会话起）→ −4654～−4876（87 房）**，第一名 4 → 12 次。
> - **C060/C061 门控分档扫描（强场 3seed）= 平坦 landscape**：C045 0.365（副露 1.24）、
>   C061（s1-2 mild）0.370（副露 1.58）、C060 0.344、旧全 mild 0.286 → **C061 与 C045 噪声内打平**，
>   因 C045 已有真机验证（11 房 +94.9/房），**保持 C045**；C061 记为备选档位。
> - 状态：排行榜 **−5063（90 房 / 12 次第一）**；speedtugc 全程接管（keeper 27652）。
>
> **25. 2026-09-11 16:00 续记（Nomad 专项 + A/B 第 2 轮）**
> - **用户报「一直被 Nomad 虐」→ E042 专项解剖**：同房 4 场 **Nomad 4/4 第一、均赢 451 分**。
>   **服务器权威数据**（`round_ended.data` 自带 fan/scores，零重算）：胡率 **35.3% vs 20.3%**、
>   fan/胡 1.381 vs 1.246、爆头胡 25.7% vs 16.9%、**副露 1.44 vs 0.79（1.68×）**、听牌 69.4% vs 48.1%。
>   **但**其 4 场 +342/房远高于生涯 +95.4/房（256 房）→ 含大量运气；对我们总速率拖累 ≈ −9/房。
> - **★新工具口径**：`round_ended.data` 含服务器权威 `fan`/`detail`/`scores` → 今后番数分析**不再重算**
>   （旧的 fan.py 重算有 ~20 分/房误差，见 E019）。
> - **C062（持白放宽副露）= 强场更强、混合场略弱（判平）**：强场 5seed C062 0.422 > C045 0.409；
>   混合场 C045 0.447 > C062 0.431。已实现为 `bot/speedtugw.py` 并注册（162 单测全绿、preflight READY）。
> - **A/B 第 2 轮已上线**：`var/_ab_strats.json = ["speedtugw","speedtugc"]`，A/B 守夜人（pid 16056）
>   按 4 房交替；上一轮状态归档 `var/_ab_state_round1.json`。判定标准同前（听牌/胡/副露/分数）。
> - **E034 speedtugc 长样本（15 房/1200 席局）**：**+76.8/房（SE 36，95%CI [+6,+147]）** vs speedtug
>   30 房 +3.8（CI 含 0）；均名次 **2.20 vs 2.40**、第一名 **40% vs 20%**；
>   机制 听牌 **55.2% vs 53.6%**、胡 **27.0% vs 25.2%**（早前 +4.4pp 收窄到 +1.8pp，但同向）。
>   → **晋级维持**；继续累积到 25–30 房。状态：排行榜 **−4882（92 房 / 13 次第一）**。
> - **A/B 最新**：speedtugc **5 房 +93.6/房、均名次 2.00、2 次第一**（听牌 56.6%、胡 27.2%、副露 0.84）；
>   speedtug 4 房 −112.5（听牌 50.6%、胡 23.1%、副露 1.24）。
> - **C059（C045 + C029 安全弃牌）rejected**：0.276 vs C045 0.365 → **弃牌键类改动一律不能与副露门控叠加**
>   （与 C056 同结论）；C045 仍是 15+ 变体中的最优。
> - **A/B 最新（候选臂领先扩大）**：speedtugc **6 房、均分 +130.2、均名次 1.83、3 次第一**
>   （听牌 56.6%、胡 27.2%、副露 0.84）；speedtug 4 房 −112.5（50.6%、23.1%、1.24）。
>
> ---


> 你是接手 `D:\hangzhouMaj`（杭州麻将竞技平台 AI Bot）的下一任 AI。本文件
> 浓缩了前一任的全部上下文、已验证事实、已排除方向与当前待决策项。请先完整
> 读完本文件，再读 `docs/PROJECT.md`、`docs/iter/reports/gap-full-picture-2026-09-09.md`。

## 0. 协作规则（用户级约束，最高优先级，见 ~/.dsh/AGENTS.md）

- **先商量方案、经用户明确确认后再动手改代码/文档**；只读操作（审查/查询/
  分析/汇报）不受限。用户回复"继续/可以/修吧/按你说的做"即视为确认。
- 用户是技术主导者，重视**真机证据**，讨厌"自我感觉良好的指标改进"。
  汇报时先给数据（复现命令 + 数字），不要粉饰，无效应如实说"排除"。
- 用户口头禅式指导："去自动房验证"、"继续"、"还是太差了"——意味继续推进。
- 平台级动作（正式赛报名、令牌轮换、策略默认切换）必须先请示。

## 1. 项目一句话

杭州麻将（自摸制、白板=财神万能牌、多高番）竞技平台 Bot；参加内网 AI
锦标赛，与外部顶级 bot（阿飞/陶康智能/卢本伟/Nomad/国士无双十三面听/赤木茂/
朱雀-5973/妈妈/八木仙/姚金毅/李兆坤…）在自动房与正式赛竞争。

- 我们的账号：`u_7a3fba48d70b`，**昵称当前为"EdGrass"**（曾用"貔貅-6837"，
  历史上 server 复盘里两种名字都会出现——按 uid 匹配，不要按名字）。
- 服务器：`https://10.240.169.190:18080`（自签证书，需 `ssl._create_unverified_context()`）。
- 指南版本：**v27**（`bot/__init__.py: GUIDE_VERSION_KNOWN = 27`，变更审计已做）。
- Python 3.12，所有脚本用 `python -X utf8` 运行。

## 2. 关键凭证与运行方式

- `var/.global_token`：全局令牌（**每次使用会轮换**，用于 `--match`/显式 tid/API 查询）。
- `var/.portal_cookie`：门户登录 cookie（**复盘/房间数据端点必需**，会过期需重取）。
- 启动自动房对局：
  - 单场：`python -X utf8 run_bot.py <tok> --match --strategy speedtm --log logs/x.log`
  - 监督循环（推荐）：`python -X utf8 tools/match_super.py --rooms 3 --strategy speedtm`
    （自动 预占房→显式 tid 跑→房终即时抓 ranking 归档 `var/auto_ranking.jsonl`）
  - **默认策略已切为 `speedtm`**（2026-09-09 用户批准）。
- 一房 = 10 场 × 8 局 = 80 局，约 15-30 分钟（看对手快慢）。

## 3. 代码结构速查

```
mahjong/        引擎：hu(胡判定/v21语义) fan(番) shanten_exact(精确向听,含
                god_meld 参数) shanten(waits) sim(自对弈引擎,含听牌/爆头统计)
bot/
  api.py        客户端(长轮询/SSE notify/match)
  game.py       play_game 主循环(协议适配核心：guard/offer注入/seq重建)
  model.py      snap_view(快照→策略 view，melds 服务器权威)、window_pending
  speed.py      SpeedCore：弃牌/窗口基础判据(_want_claim/_best_discard)
  speede.py     SpeedE ★历史基线（速度向听最小+孤字tie）
  speedm.py     +副露放宽(after<=before)         speedt.py  +tie弃牌偏好(拆对后置)
  speedtm.py    T+M ★当前默认                   speedq.py  七对路线锁定
  speedtqm.py   T+M+Q                          speedtp.py  T+M+碰严格(C017)
  speedu/v/k/w/wm/tk 等 = 历史候选(见§7)
tools/
  match_super.py     自动房监督循环(ranking 归档)
  gap_analysis.py    房 ranking 差距分析(--strategy 过滤)
  fetch_room_replays.py  批量拉服务器全信息复盘→var/replays/server/
  tenpai_race.py     听牌竞赛分析(听牌率/均听巡/爆头态率, --prefix 按房过滤)
  discard_profile.py 弃牌牌型画像      session_health.py 会话健康(409细目等)
  replay_oracle.py   复盘离线 oracle(任意座位重放对比)  stability.py sim 稳定性
  ab_balanced.py / replay_rec.py / replay_fetch.py / oracle_audit.py
tests/          unit tests（改引擎/策略后必跑：test_speed/test_speedt/
                test_protocol_integration/test_notify/test_engine_v21 等）
docs/PROJECT.md 事实基线   docs/iter/{queue.md,journal.md,reports/}
var/            运行时数据(gitignore)：令牌、replays、auto_ranking.jsonl、探针脚本
```

## 4. 必须知道的协议/数据事实（都是实测，别再重新踩坑）

1. **服务器快照（v24+）自带 `melds`（四家副露数组）与 `last_discard`**——
   副露以服务器 `melds[seat]` 为权威，本地 tracker 只兜底（自动房竞态下
   tracker 会分叉，曾致 guard 风暴 300-500 次/场，已修）。
2. **窗口 offer 缺失曾致副露被压 20×**：409/seq=0 重建路径下 tile_discarded
   事件会丢；已用快照 `last_discard` 兜底注入（`bot/game.py`）。
3. **SSE notify**（`/api/games/{id}/notify`）已接入，稳态 409 从 65+/分降到 ~0；
   `pass` 类 409 是无害噪音（服务器视同超时 pass），看 `session_health` 的
   `inv_harmful`（非 pass 409）才是真损失。
4. **复盘金矿**：`GET /portal/api/games/{gid}/events`（**带 cookie**）返回
   全信息复盘 `{blocks(start_hands 四家全手牌+全事件), rounds, seats(真名)}`；
   房间列表 `GET /portal/api/test-rooms/{room}`（带 cookie）。**自动房 ranking
   保留窗口很短**（房结束几分钟内），必须跑完即抓（match_super 已内置）。
5. 免 token 的 live 事件流里**对手摸牌 tile 不可见**（仅自己），但复盘全可见。
6. 场终批事件要在退出前喂 recorder（否则 round_ended 丢）；live 流近乎不推
   round_ended（逐局细分走复盘）。
7. v25：吃最多 2 摊（服务端校验，策略已自限）；v26 抓打圈豁免（低频未适配）；
   v27 门户榜（零影响）。

## 5. 方法论（血泪教训，务必遵守）

- **sim 只能验证"速度类"指标**（听牌率/听牌速度/胡率/合规）：sim 里平均番恒
  1.0-1.05（同质速度局无大番环境），**打点/爆头类策略 sim 无信号，只能真机**。
- **场分对照受房间强度严重混淆**：每房冠军随机（+0.6~+10.8/场），1-3 房场分
  不可判；**判定候选要看机制指标（听牌率/胡率/bao）+ 多房累计**。
- 真机候选验证标准流程：sim 合规（stability 零违规）→ 自动房 3 房
  （match_super）→ 拉复盘（fetch_room_replays）→ tenpai_race 逐房对比 +
  gap_analysis 场分 → 登记 queue.md/report。
- 历史"本地 A/B/自对弈场分"判据噪声巨大（σ≈33 sim）已弃用；改机制指标后
  多个候选效应才第一次可测。

## 6. 当前策略水平与差距（2026-09-09 真机复盘实测）

| 策略 | 自动房场分 | 听牌率 | 胡率 | 备注 |
|---|---|---|---|---|
| speedE（旧基线） | −2.47/场 | 31.5% | 13.1% | 880 席局 |
| **speedtm（当前默认）** | **−1.54/场** | **43.4%** | **20.8%** | 机制最优 |
| speedtp（C017 碰严格） | −0.91/场* | 41.2% | 20.8% | *冠军池偏弱，机制≈TM |
| speedtqm（+七对锁定） | −1.87/场 | 44.2% | 19.2% | Q 组件无效 |
| 顶级对手池 | +2.5~+10.8/场 | 60-72% | 25-46% | 番 1.3-1.72、爆头 21-44% |

**差距三层归因（已验证）**：
1. 表现层：听牌率差 ~20pp（冠军每 2/3 局听牌，我们不到一半）→ 胡率差 15pp；
   被截局已听率我们 28% vs 冠军 38-64%（我们多数局还没听就被胡）。
2. 决策层（根本）：我们一局做 30-40 次弃牌决策；现有 11 个候选**全是
   "单步贪心 + 手工判据"**（向听最小/有效牌/tie 偏好/白保留/七对锁定…）——
   单步最优 ≠ 整局听牌概率最大，30 步复利成 20pp。K1（+1 ply ukeire）只
   +3.6pp 证明"多算一步"不够 → 冠军用的是不同的决策算法（多步规划/学习）。
3. 番型层：我们 200+ 胡 0 爆头、fan 1.10；冠军爆头态到达率 8-16%（我们
   0.0%/1360 局）。爆头=副露 2-3 组+暗面子齐+白孤 或 6对+白 七对形，
   80% 起手有白——这是"现在牺牲一点速度换将来万能听"的多步规划问题，
   规则化尝试（W/WM/Q）全部失败。

## 7. 已排除方向（不要重复劳动，附证据）

| 方向 | 结论 | 证据 |
|---|---|---|
| 单步有效牌 tie（K1/speedu） | 无实质增益 | sim +3.6pp 听牌率、真机无差 |
| 副露频率（M mild） | 无效 | 真机 3 房 −2.13 vs E −2.47（噪声内）|
| tie 弃牌偏好（T） | 有效但量级小 | 听牌 +5pp sim；并入 TM |
| T+M（TM） | 当前最优 | 真机听牌 43.4%/胡率 20.8% |
| 白保留引擎约束（W/WM，god_meld=False） | **排除** | sim bao 0.62% 未迁移；真机 240 席局 bao 0 |
| 七对路线锁定（Q/TQM） | **排除** | 真机七对率 10.6% 未升、bao 0 |
| 碰/吃结构（C017 碰严格） | 机制≈TM | 场分 −0.91 系弱房 |
| T+K1 组合（TK） | 键序冲突不可叠 | sim 62.5% vs T 74% |
| MCTS/学习型 | **用户认为当前探索程度还不需要**（但规则空间已打穿，见§6.2）|

## 8. 待决策的下一步（前任已向用户提出，等用户选择）

跨过差距需要换决策算法，三条路径（工程量从小到大）：
1. **关键局面 rollout**：只在向听≤2 的弃牌上做 1-2 ply 采样前瞻
   （~200ms/决策，真机 3s 窗口可容纳）；先在 sim 用采样加速验证
   "多步视野值多少钱"。**最推荐先做**（轻、可证伪）。
2. **离线自对弈搜索/值迭代**：用现有 `mahjong/sim.py` 跑百万局，学一个
   "整局听牌概率/期望得分"评估函数替代向听主键。工程量中。
3. **冠军行为克隆**：`var/replays/server/` 已有 28000+ 顶级 bot 决策样本
   （含全手牌上下文），可监督学习弃牌模型。工程量中，数据现成。

## 9. 常用命令备忘

```powershell
# 跑自动房对照（后台）
python -X utf8 tools/match_super.py --rooms 3 --strategy speedtm
# 全量排名汇总 / 按策略过滤
python -X utf8 tools/gap_analysis.py [--strategy speedtp]
# 拉服务器复盘（房结束几分钟内！）
python -X utf8 tools/fetch_room_replays.py
# 听牌竞赛/爆头态分析（--prefix 用房 gid 前缀，注意完整前缀含前 2 字符）
python -X utf8 tools/tenpai_race.py --prefix "a_775f"
# sim 稳定性/听牌率回归
python -X utf8 tools/stability.py --strategy speedtm --seeds 6
# 单测（改引擎/策略后必跑）
python -X utf8 -m unittest tests.test_speed tests.test_speedt tests.test_protocol_integration
```
注意 PowerShell 引号易炸：复杂 python 一律写脚本文件到 `var/` 下再跑。

## 10. 立即该做的三件事（建议）

1. 读 `docs/iter/reports/gap-full-picture-2026-09-09.md`（全部数据与结论）
   + `docs/iter/queue.md`（候选队列与排除登记）。
2. `git log --oneline -30` 看最近改动（30+ 提交：协议修复/复盘打通/候选谱系）。
3. 向用户确认路径 1/2/3 的选择（前任最后停在这里），然后按 §5 流程推进，
   每一步先给方案再动手。

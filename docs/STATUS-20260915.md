# 项目状态速览（2026-09-15 05:40）

## 一、当前生产策略

| 项 | 值 |
|---|---|
| 策略名 | **`speedtugc`**（`run_bot.py --strategy speedtugc`）— 2026-09-15 按真机逐轮口径更正，见 `docs/iter/reports/field-audit-20260915.md` §9.1 |
| 模型文件 | `var/c073_orig_w2_net.pt`（154 维「相对基线优势」网络，din=154） |
| 训练数据 | `var/_bc_data_c073.npz` → 只取**最初 10 个顶级后缀教师**（28,361 组），**赢家样本损失权重 ×2** |
| 覆盖范围 | 本人摸牌弃牌且弃后向听 ∈ {0,1,2}、最小向听候选 ≥2 张（其余全部回退 C045） |
| 决策延迟 | 0.34 ms（弃牌窗口 3s） |
| 上线时间 | 2026-09-15 23:18（首房），keeper 常驻 |

**sim 证据（120 房配对，同 seed 同洗位，强场 3×SpeedTUG，C045 基线 +2.1）**：
W2 **+14.1/房（Δ+11.9，t=2.11）** > W2-k6 +12.9 > W2-k8 +11.6 > lo0 +10.8 > C045 +2.1。

**真机证据（同日口径）**：

| 策略 | 日期 | 房数 | 原始/房 | 净胜/房 | 正分率 | 均名次 |
|---|---|---|---|---|---|---|
| speedtugc（旧生产） | 9/14 | 6 | −137.2 | −182.9 | 0% | 3.33 |
| speedc068lo0 | 9/14 | 20 | −19.8 | −26.4 | 35.0% | 2.50 |
| **speedc073w2** | **9/15** | **23** | **+15.0** | **+19.9** | **56.5%** | **2.39** |

（净胜 = 我方分 − 同房另三家均值；跨日不可比，对手池日内波动可达 ±180/房。）

## 二、运行架构（三层自愈）

```
Windows 计划任务 HangzhouMajAutoHeal（每 5 min，pythonw 静默）
  └─ var/_ensure_all.py（幂等：缺什么补什么，不杀进程）
       ├─ var/_watchdog.py（90s：卡死检测 + 重启 match_super + 自愈 keeper）
       └─ var/_keeper.py <策略> 4（120s：保证恰好一个 match_super；策略名存 _keeper_strategy.txt）
            └─ tools/match_super.py → run_bot.py（4 房/批，串行；同账号禁并发 E002）
```

**日常检查命令**：
```powershell
python -X utf8 var/_ab_report.py      # 按(日期,策略)看 原始/净胜/正分率/均名次 + 最近 12 房明细
python -X utf8 var/_rate.py           # 最近 N 房均分/名次
python -X utf8 var/_lb2.py            # 门户榜单（我方 rank/rooms/firsts/score）
python -X utf8 tools/preflight.py     # READY 期望（指南版本 / fan-calc / 引擎单测）
```

**回滚**：把 `var/_keeper_strategy.txt` 与 keeper 参数换成 `speedc073w4`（次优）即可，模型文件都在 `var/`。
（2026-09-15 更正：`speedc068lo0` 真机 7 房、净胜 -53.7/房、胡率 23.2%，是 BC 组最差，**不要再用作回滚目标**。）

## 三、开赛（9/17 20:00 t_65d538e905c5）

见 `docs/开赛操作单-20260917.md`。关键三点：
1. **先停测试房**：同账号禁并发（E002），停 keeper + 等 run_bot 退出，再起正式赛；
2. `python -X utf8 var/_official_keepalive.py --strategy speedtugc`；
3. 开赛入口已用真令牌验证：`register` → `{'registered': True}`、`ready` → `{'ready': True}`；
   多阶段（海选→16强→8强→决赛）每阶段需重新 ready，v13 要求开赛前 90s 内有已认证请求
   （bot 的轮询满足），标准流程测试 10 项全绿。

## 四、已封存的实验轴（避免重复投入）

**弃牌族（19 个自由度全部走完，仅 W2 为正）**：语料×2.4、教师集合×4 种筛法（orig-10 最优）、
赢家加权、只训赢家、route 特征（C069）、可见牌活等（C068b）、集成×2、候选 k5/k6/k8、
状态价值贪心（C076）、价值+BC 混合（C077）、宽网 512、早巡 hi=4（C080）。

**副露族**：C018/C060/C061（sim 中性/负）、C070（学习型，真机 −240/房）、C070b（扩容语料更差）
→ 维持严格门控。

**搜索族**：C032/C063（rollout）两次否证。

**其它**：白保留（结构性 no-op）、防守/安全牌（E004 无点炮）、财飘（频率 0）、杠（sim 不可测、频率极低）。

## 五、关键报告索引

- `docs/iter/reports/model-registry-2026-09-14.md`（全部模型臂与证据）
- `docs/iter/reports/C069-C070-route-weights-2026-09-14.md`（舍牌/吃碰权重再估计）
- `docs/iter/reports/C071-real-room-forensics-2026-09-14.md`（真机同房/庄闲/同日口径）
- `docs/iter/reports/positive-rate-target-2026-09-14.md`（80% 正分率的量化门槛）
- `docs/iter/journal.md`（逐条实验流水，R1–R54）

---

## 六、赛前就绪审计（2026-09-15 08:10，实测）

| 检查项 | 命令/证据 | 结果 |
|---|---|---|
| 单元测试 | `python -X utf8 -m unittest discover -s tests -p "test_*.py"` | **196 全绿** |
| 体检 | `python -X utf8 tools/preflight.py` | **READY**（指南 v34 / bot v30，非 BREAKING；fan-calc 抽样一致；检测到 1 个 run_bot） |
| 生产策略 | `var/_keeper_strategy.txt` + keeper 进程参数 | **speedc073w4**（一致） |
| 模型加载 | `run_bot.STRATEGY_FACTORIES["speedc073w4"]()` | 加载 `var/c073_orig_w4_net.pt` ✓，range (0,2)、max_cand 4 |
| 自愈链 | 计划任务 `HangzhouMajAutoHeal` | State=Ready，最近执行 08:04:43，**LastTaskResult=0** |
| 进程数 | keeper / watchdog / match_super / run_bot | **恰好 4 个**（无重复、无残留） |
| 吞吐 | 最近 12 批耗时统计 | **4.30 房/小时**（平台上限） |
| 执行质量 | w4 房日志（2,961 弃牌） | 409 103（3.5%，基本为过期 pass）、**异常 0**、改判率 28.4% |
| 开赛入口 | 真令牌 `register`/`ready` | 均 200/幂等；token 作用域 `t_65d538e905c5` |
| 多阶段协议 | `tests/test_protocol*.py` | 10 项全绿（含每阶段重新 ready、候选人名单外退出） |

结论：**系统处于可无人值守连续运行状态**；唯一需要人工确认的动作是 9/17 19:45 的「测试房 → 正式赛」切换。

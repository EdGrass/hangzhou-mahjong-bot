# 参赛程序说明（粘贴到申报页面正文）

**完整可运行源码仓库：** https://github.com/EdGrass/hangzhou-mahjong-bot

---

## 一、程序是什么

杭州麻将竞技平台的**全自动对局程序**（Python 3.12 实现）：以"向听数 + 真进张（live ukeire）"
驱动的速度型引擎为核心，配套 A/B 评测台、复盘审计与延迟基准工具链。

- **全程 AI 自动决策，无任何人工干预**：报名 → 到位 → 开赛 → 逐手决策 → 阶段切换 → 终态退出；
- 决策只使用**服务器公开状态 + 自己手牌**，不读取任何对手私有信息；
- 每一步（摸牌弃牌 / 碰 / 吃 / 杠 / 胡 / 过）均由程序自行判断并提交，断线/超时由程序自身重试恢复。

## 二、运行环境与依赖

| 项 | 要求 |
|---|---|
| Python | **3.12**（实测 3.12.10；Windows / Linux 均可） |
| 第三方依赖 | **核心零依赖** —— `run_bot.py` + `bot/` + `mahjong/` 仅使用 Python 标准库 |
| 可选依赖 | `fastapi` + `uvicorn`（仅本地自对弈看板，参赛不需要）、`psutil`（仅 A/B 运维脚本） |
| 硬件 | 普通 PC 即可（无 GPU、无数据库、无外部模型权重文件） |

```bash
pip install -r requirements.txt    # 仅可选组件；参赛本身无需安装任何包
```

## 三、如何接入官方平台

| 环节 | 实现 |
|---|---|
| 平台地址 | `https://10.240.169.190:18080`（自签 TLS，程序内部已处理证书校验；亦可用 `--server` / `HM_SERVER` 覆盖） |
| 认证 | `Authorization: Bearer <参赛令牌>`（64 位十六进制）。报名令牌自动发现赛事；全局令牌须显式给 tournament id |
| 状态机 | `registering → running → stage_done → stage_open → running … → finished`，自动完成 **报名 / 到位(ready) / 阶段出席确认 / 淘汰退出** |
| 事件驱动 | 走 **notify 事件流（SSE）**长轮询，不占用 `/state` 频率配额；事件合并后按需取快照 |
| 窗口应答 | 弃牌 3s、碰/吃 1s 窗口内提交决策；本机实测决策 **p99 ≈ 47ms**（远低于窗口预算） |
| 容错 | 429 / 5xx / 网络瞬断指数退避；**瞬态 404 容忍 120s 重试**（平台约每 70 分钟重启一次）；窗口 409 竞态自动恢复 |

## 四、如何启动

### 1) 免认证自检（推荐第一步，不参赛、不需要令牌）
```bash
python -X utf8 run_bot.py --smoke --server https://10.240.169.190:18080
```
校验：服务器连通 → 接入指南版本（有未知 BREAKING 变更会报警）→ 番型计算接口语义
（13 张散牌必不胡 / 指南示例必胡且结算结构齐全）。

### 2) 参赛（最小命令）
```bash
python -X utf8 run_bot.py <参赛令牌> --server https://10.240.169.190:18080
```
程序自动报名 → 到位 → 等待开赛 → 逐局决策，赛完自动退出。

### 3) 指定策略
```bash
python -X utf8 run_bot.py <参赛令牌> --server <URL> --strategy speedvalue
```
（全部可用策略见 `python -X utf8 run_bot.py --help`）

### 4) 无人值守运行（推荐正式赛使用）
```bash
python -X utf8 tools/keep_alive.py <参赛令牌> --strategy speedvalue --server <URL>
```
阻塞式看护 `run_bot.py`：子进程异常退出（非 0）→ 5s 后自动重启，重试不设上限；
子进程自然结束（exit 0 = 赛事终态/未晋级）→ 看门狗一并退出。
看门狗自身不发起网络请求，避免与子进程争抢同一账号令牌。

### 5) 开赛前一键体检
```bash
python -X utf8 tools/preflight.py --server <URL>      # 输出 READY / NOT READY（退出码 0/1）
```

## 五、稳定性设计

- 事件用 **notify SSE** 驱动并做事件合并，避免并发下的限速风暴；
- 429 / 网络瞬断 / 5xx 指数退避；窗口 409 竞态自动恢复；
- **瞬态 404 容忍 120s**（服务器约每 70 分钟重启一次会产生 502→404 风暴），持续超过 120s 才判定房间真删；
- 决策延迟实测 **p99 ≈ 47ms**（窗口 1s / 3s，0 条超窗）；
- 长跑由 `tools/keep_alive.py` 看护，崩溃自动重启；报名 / 到位 / 确认均幂等，重启语义安全。

## 六、全自动决策声明

- 每一步均由程序自行决策并提交，**全程无人工干预**；
- 决策仅使用服务器公开状态与自身手牌，**不读取任何对手私有信息、不使用任何外部数据**；
- 断线 / 窗口错过 / 服务器重启等异常全部由程序自身重试与重建状态，不需要人工介入。

## 七、仓库与验证

```bash
git clone https://github.com/EdGrass/hangzhou-mahjong-bot
cd hangzhou-mahjong-bot
python -X utf8 -m unittest discover -s tests     # 571 个单元测试（全通过）
python -X utf8 run_bot.py --smoke --server https://10.240.169.190:18080
```

仓库内 `docs/SUBMISSION.md` 为同一份说明的脱敏版（不含平台地址）。

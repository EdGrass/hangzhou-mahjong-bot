# c151 后下一轮棋力战役（预登记，2026-09-17）

> 触发条件：09-17 二测 `speedc151` 无延迟/409 硬故障，且分/轮不为严重负值。
> 触发后 c151 是新生产基线；旧基线 `speedtugc` 只作历史回退。

## 1. 臂集

| 策略 | 角色 | 单变量/组合 | 机制假设 |
|---|---|---|---|
| `speedc151` | 新基线 | — | 平奖励压对数；已验证 5 房 +222.7 净胜/房、t=2.68 |
| `speedc159` | 候选 | 组合 | 分时段压对数：早 0 / 中 1.0 / 末 1.5，修 c151 早期过猛、后期不足 |
| `speedc155` | 候选 | 组合 | 庄位条件化路线：做庄用 c151 压对数换速度，做闲退回 c150 保对子/爆头价值 |
| `speedc146` | 候选/保险 | 组合 | 不含重型真进张的 M>10 安全兜底：明杠+自杠+质量副露+财飘 |

启动命令（新 started；不要继承 11:22 旧役）：

```powershell
python -X utf8 tools/ab_ctl.py start speedc151,speedc159,speedc155,speedc146 --bundles=speedc159,speedc155,speedc146
```

## 1b. 开局前离线预检（2026-09-17 18:4x）

- ~~`c152`~~：虽然脚印 12.2%，但真实决策后 **3+对从 c151 的 30.7% 反升到 35.5%**（≥2对虽降），分档权重被进张项反向主导 ⇒ **不进入本役**。
- `c146` vs `c151`：同口径 tile 不同 **499 = 17.5%**，action 不同 0% ⇒ M>10 兜底路线确实与重型路线有明显行为差。
- `c188`：机制上安全，但 20 房旋转 sim 对 c151 **−17.67/房（t=−1.84）**；暂不占本役工位，作为 c159/c155 失败后的备选。
- `c155` 的离线 dec 足迹无法可靠测（dec view 缺 `dealer`）；真机按“庄胡率↑、闲番/爆头↑”读机制，分/房作为主判据。

### 1c. 当前 c151 机制基线（本地弃牌流）

窗口 `since=2026-09-17 11:22:11`，直接统计弃牌前持对数：

| 臂 | n | 3对+ | ≥2对 |
|---|---:|---:|---:|
| `speedtugc` | 3520 | **23.4%** | **70.5%** |
| `speedc151` | 3441 | **14.7%** | **59.9%** |
| `speedc156` | 4029 | 15.3% | 60.8% |
| `speedc159` | 4287 | 17.1% | 62.4% |

⇒ c151 的压对数机制已在本地决策流上确认；下一役不再用 c152 继续硬压，而用 c155 只对庄位条件化。

## 1d. 算法层复测：旧 C063 rollout 不进入本役

dedup 优化后重测（同批 120 个真实 draw 决策）：

| 配置 | p95 | max | 超 3s | 改判 |
|---|---:|---:|---:|---:|
| look1 / 100ms | 124ms | 304ms | 0 | 27/120 |
| rollout s=4,h=3 / 100ms | 95ms | 142ms | 0 | 28/120 |
| rollout s=8,h=4 / 150ms | 159ms | 371ms | 0 | 29/120 |

⇒ **性能瓶颈已被 dedup 解除**，旧 C063 从“超时不可用”变成可运行。

但质量门禁未过：5 seeds × 4 座位旋转、20 房 sim 对 3×SpeedTUGC：

- fast rollout：净胜 **−13.0/房**，SD 50.2，t=−1.16；
- look1：净胜 **+0.47/房**，SD 69.3，t=+0.03，且 350 次启用只改 3 次。

⇒ 结论：**本役不放入 C063/rollout 臂**。算法层若继续，必须重做叶值目标（真实结算期望）而不是沿用旧 P(听)/简化 payout 叶值；当前优先做 c188/c155/c146。

### 1e. 被拒副露窗口解剖（2026-09-17 19:1x）

`tools/meld_reject_anatomy.py`，最近 600 个 dec 文件的真实有效窗口 2,662 个（**混合策略口径，只作方向证据**）：

| Δ向听 | 窗口 | 接受率 |
|---|---:|---:|
| −1 | 1072 | 99.1% |
| 0 | 1493 | 13.8% |
| +1/+2 | 97 | 0% |

Δ=0 再按真实进张变化切：

| 进张变化 | 窗口 | 接受率 |
|---|---:|---:|
| 涨 | 1192 | **4.4%** |
| 平 | 285 | **54.0%** |
| 跌 | 16 | 0% |

⇒ **明确缺口**：大量“向听不变但真进张变好”的窗口被拒，而一部分“进张持平”的窗口反而被收。正是 c188/c146 的机制目标；下一役必须把“覆盖率上升”与“分/房/第一名率不下降”同时作为闸门。

⚠ 该工具按 `--strategy` 过滤 c151 时当前样本为 0（近期 dec 文件与归档策略映射覆盖不足）⇒ 本表是混合策略方向证据；臂级正式读数仍走 `meld_from_dec`/门户 `mech_mix`。

### 1f. 五策略同窗对照（2026-09-17 19:1x）

同一批真实有效窗口（分类后 94 个），按 Δ向听 / Δ真进张分层：

| 桶 | base | c151 | c136 | c146 | c186 |
|---|---:|---:|---:|---:|---:|
| Δ向听 −1 | 100% | 100% | 100% | 100% | 100% |
| Δ=0，进张涨 | 7.0% | 34.9% | 32.6% | 34.9% | **88.4%** |
| Δ=0，进张平 | 75% | 75% | 75% | 75% | 87.5% |
| Δ=0，进张跌 | 0% | 0% | 0% | 0% | **100%（3/3）** |

⇒ **好消息**：c186 的额外覆盖确实集中在“进张涨”桶（88.4% vs c151 34.9%），不是纯阈值乱放；c188 仅对这 3/94 坏尾部加否决，1,600 窗口实测差异 0.44%。  
⇒ **风险**：对极少数“进张跌”窗口也不设否决；这些窗口可能承载真进张无法表达的爆头/四白价值，也可能只是坏副露。  
⇒ 决策：**不无条件放行无白坏窗口**；c188 保留含白/爆头路线，作为下一役候选。真机仍必须看分/房与爆头占胡是否同时受益。

## 2. 端点

### 主端点
- **原始分/房**（服务器 `total_score`，`tools/ab_readout.py`），终点阈值沿用 v3：组合臂 150 房/臂、`t>=1.50`。
- **第一名率** 作为并列主端点之一：当前同房 top32 为 59% vs 我方 14%，比赛晋级比总分更看名次。
- 中期采用只能在预登记终点满足后执行；12 房/臂只用于机制止损，不用于提前采用。

### 机制端点
- `speedc159`：按牌河阶段读 pc/听牌/爆头占比；要求早盘回到 c150、中末盘向强者曲线靠。
- `speedc155`：按庄/闲拆庄胡率、闲番与爆头占胡；要求做庄提速、做闲价值不下降。
- `c159`：`pc_from_dec` 分桶，要求早盘不压、中末盘继续向强者靠。
- `speedc146`：M=20 实际并发门禁 `0/2000 >3s`；若 M=10 正式赛则同样必须 0 超窗。

### 安全闸门
- `tools/official_latency_audit.py` / `var/_concurrency_bench.py`：draw 无 >3s，response 无 >1s。
- `tools/rules_guard.py`：与 `YouCaiBiKao` 一致。
- 不可牺牲 C036；`giveup_audit` 若有可用语料必须复核。

## 3. 中止/淘汰规则

12 房/臂时只做**机制止损**：
- c155：庄/闲机制端点没有分化，或做庄机制未提速 → 停臂。
- c159：若中末盘 pc 没有移动，或分/房没有改善 → 停臂。
- c146：若实际 M 并发门禁失败，作 M>10 主选淘汰，只保留 M<=10 观察。
- 任一候选：若近 12 房相对基线净值 < -150/房（`_ab_driver` 自守阈值），立即熔断回 c151。

不得因为一次正/负波动改判；任何结论必须带样本数、t 和机制读数。

## 4. 赛后读数命令

```powershell
python -X utf8 tools/ab_readout.py --since "<started>" --arms speedc151,speedc159,speedc155,speedc146
python -X utf8 tools/ab_winrate.py --since "<started>" --arms speedc151,speedc159,speedc155,speedc146 --lowprio
python -X utf8 var/pc_from_dec.py --arms speedc151,speedc159,speedc155,speedc146 --compare speedc151
python -X utf8 tools/meld_from_dec.py --arms speedc151,speedc159,speedc155,speedc146 --compare speedc151
python -X utf8 tools/ab_integrity.py --since "<started>" --arms speedc151,speedc159,speedc155,speedc146
```

## 5. 一个月目标

- 先把 c151 的生产分/房转正（目标 `>0`，最好 `>=+50`）。
- 再用 c159/c155 把第一名率推向 **35% 以上**，而不是只优化听牌率。
- 最终正式赛策略选择以 `docs/正式赛执行清单-一个月后.md` 的 M/YouCaiBiKao 双分支为准。

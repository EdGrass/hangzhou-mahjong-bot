# 强场恢复战役（2026-09-17 二测后）

## 1. 结论先行

09-17 二测海选（t_65d538e905c5，M=10、Rounds=16、10 桌、160 轮）：
- 我方 `speedc151`：**-149 原始分、约 rank 100/201、place_points -8**
- 官方逐玩家审计：**胜率 22.5%**、8 巡听牌 **15.4%**、张|听 5.02、爆头|听 2.2%
- 延迟健康：draw p99 84.6ms / max 188.9ms，response p99 9.8ms / max 122.1ms，**0 超 3s、0 超 1s**；409=34 基本为已失效窗口

⇒ **c151 的 5 房 A/B +222.7/房没有迁移到强场。** 不能把 c151/c159/c155/c188 等 c151 家族继续当生产基线或主候选。
⇒ 立即回退到真机证据最厚的 `speedtugc`，下一役只测**独立于 c151 的机制**。

## 2. 下一役臂集

| 策略 | 角色 | 机制 |
|---|---|---|
| `speedtugc` | 恢复基线 | 历史真机 141 房 / +0.214 分每轮 / 胡率 25.4% |
| `speedc191` | 学习副露 | speedtugc + c121 学习副露 p=0.70 + 无白坏窗口否决 |
| `speedc146` | 首副露/价值大束 | 明杠 + 自杠 + c136 质量副露 + c141 财飘 |
| `speedc193` | 庄位路线 | speedtugc + 仅做庄启用 c151 压对数路线 |
| `speedc194` | 中盘拆对 | speedtugc + 中盘 3+ 对子、进张损失≤4 的外科拆对 |

启动（新 started）：

```powershell
python -X utf8 tools/ab_ctl.py start speedtugc,speedc146,speedc191,speedc193,speedc194 --bundles=speedc146,speedc191,speedc193,speedc194
```

## 2b. 平台关闭匹配时的暂停模式（2026-09-17 新增）

二测后 `/portal/api/features` 返回 `match_enabled=false`，自由匹配会持续 403 FEATURE_DISABLED。
为避免 watchdog/keeper/A-B 驱动空转重试，已加入：

- 哨兵 `var/.pause_mode`：存在时 `_ensure_all` / `_watchdog` / `_keeper` / `_ab_driver` 全部不排批；
- `var/_feature_mode.py`：每轮检查 `/portal/api/features`；`match_enabled=true` 时自动删除暂停哨兵；
- `.ab_mode` 已保留强场恢复战役配置，平台恢复后 watchdog/自愈链会自动启动 `speedtugc,speedc146,speedc191,speedc193,speedc194`；
- 管理命令：`python -X utf8 tools/feature_pause.py status|pause|resume`。

回归：全量 **675 项 OK**；暂停期间只保留 watchdog，不起 keeper/match_super/run_bot。

### 2c. c190/c191 上臂证据（2026-09-17 20:5x）

- 1,600 个真实有效窗口 vs speedtugc：只改 **38 个（2.4%）**；其中 **32 个为“真进张变好”**，无白坏窗口仅漏 1 个。
- 阈值校准：254 个有资格真实窗口，speedtugc 基线 49.2%；p=0.75 总接受 57.5%（贴 top32 58.4%），p=0.70 总接受 65.0%（强者区间上界 1.32×）。
- 冒烟/延迟：c190 draw p99 56.1ms、max 129ms；window p99 1.9ms、max 115ms；均 0 超预算。
- 设计边界：学习层只叠在 speedtugc；不再继承 c151 的成对压对数路线。

## 2d. 强场指纹复核（2026-09-17 21:0x）

- 番型（3,474 文件）：我方 1.293 番/胡、番≥2 23.4%、爆头 22.1%、杠开 0.3%、财飘 0.0%；top32 为 **1.362、28.1%、26.3%、1.6%、0.9%**。
- 听牌时序（1,200 文件 / 9,672 局）：起手向听3 的累计听牌差距从 k4 的 +3.5pp 扩大到 k8 的 **+10.2pp**；起手向听4 同样到 k8 +7.1pp。
- ⇒ 缺口是**中盘速度与价值事件**，不是起手塑造。`c191` 攻中盘副露覆盖，`c146` 攻杠/财飘，`c193/c194` 攻庄位与中盘拆对；与第一波五臂一致。
- `ukeire_actu_compare` 样本小且结果混合（向听2 少 4.05、向听3 多 10.24 张）⇒ 只作画像，不作优化靶标。

### 2e. sim 灾难门禁（仅安全检查）

20 房旋转座位，对 3×speedtugc：c190 **+9.73/房（t=0.68）**、c191 **+9.33（t=0.68）**、c146 **+8.40（t=0.43）**。  
⇒ 三臂均无明显崩盘；sim 不作为采用/排序证据，真机强场仍是唯一裁决。

### 2f. 官方强场决策脚印（2026-09-17 21:1x）

在二测 160 轮的真实 dec 决策上回放：

| 策略 | 有效窗口 | 收副露 | 接受率 | 相对 speedtugc 改判 |
|---|---:|---:|---:|---:|
| speedtugc | 267 | 131 | 49.1% | — |
| c190 | 267 | 145 | **54.3%** | 14 (5.24%) |
| c191 | 267 | 168 | **62.9%** | 37 (13.86%) |
| c146 | 267 | 169 | **63.3%** | 38 (14.23%) |

- top32 参照接受率 **58.4%**；c190 在下侧、c191 在上侧，正好组成剂量夹逼。
- c146 另在 1,432 个出牌决策中产生 4 次差异，并把 8 次决策改成杠；与它攻杠/财飘的目标一致。
- ⇒ 四臂在真实强场决策分布上都有足够脚印，不是 no-op。

### 2g. 巡目映射更正（2026-09-17 21:2x）

⚠ 旧读法“全部在 15–19 手”作废：最初 `seq → 弃牌数` 没有按 `round_no` 重置，跨局累计污染了标签。按局内全局弃牌数重算后：

| 策略 | 新增接受 | 早盘 ≤9 | 中盘 10–19 | 后盘 20–27 |
|---|---:|---:|---:|---:|
| c190 p=0.75 | 14 | 2 | 1 | 11 |
| c191 p=0.70 | 37 | 5 | 8 | 24 |

⇒ 两档主要补后盘，但 **c191 有 8 次中盘、5 次早盘**，不是纯后盘模型；c190 则明显偏后盘。

### 2h. 学习模型的中盘可用性

对官方强场 136 个“严格门控拒绝但合法”的窗口，按**局内全局弃牌数**分解模型概率：

| 局内弃牌数 | 候选窗口 | p≥0.70 | p≥0.75 |
|---|---:|---:|---:|
| 0–4 | 10 | 4 | 3 |
| 5–9 | 15 | 3 | 1 |
| 10–14 | 11 | 3 | 0 |
| 15–19 | 14 | 5 | 1 |
| 20–27 | 34 | 7 | 4 |
| 28+ | 52 | 25 | 9 |

⇒ 中盘 10–19 确实存在 8 个 `p≥0.70` 窗口，但只有 1 个达到 `p≥0.75`；这解释了 c191 有中盘覆盖而 c190 几乎没有。  
⇒ 若 c191 的增量和分/房仍不成立，下一步再建**中盘专用模型**；当前先用 c191的主候选剂量回答“学习副露到底值不值”。

### 2i. 中盘专用模型尝试：否证（2026-09-17 21:3x）

从 c121v3 语料只取局内弃牌 10–19 的中盘窗口，训练 141 维同构网络：

- 样本 4,553、16 位教师、正例 56.8%；留出教师 AUC **0.587**；
- 分 kind：碰 AUC 0.539、吃 AUC 0.539、明杠 0.571。

⇒ 现有特征下“中盘是否副露”几乎不可学，**c192 不部署**。中盘速度差不能靠继续训练同构副露门控解决；需要换特征/机制（路线结构、对手信息或牌效前瞻），这留给下一轮。

### 2j. 路线增广的中盘模型仍然不合格（2026-09-17 21:4x）

在 141 维窗口特征上加入 22 维路线特征（向听/真进张代理/七对/白/爆头/清一色），原中盘 4,553 样本重训：

- 留出教师 AUC：**0.639**（原 0.587，确有提升）；
- 高阈值 p≥0.80：精确率 0.545、召回 0.545；p≥0.90：精确率 0.585、召回 0.448；
- 验证集正例基准 0.434 ⇒ 只是略高于乱猜，不能承担真机决策。

⇒ **c192 继续不部署**。中盘缺口不靠“再给副露分类器加特征”解决；下一机制必须换问题定义（路线/牌效前瞻/对手信息），不要再做同类二分类器。

### 2k. 下一轮预备候选：c193（庄位条件化路线）

`c193 = speedtugc + 仅做庄时 c151 压对数路线`：

- 做闲严格回退 speedtugc；不继承 c151 其它层。
- 20 房旋转 sim 对 3×speedtugc：**-3.00/房（t=-0.34）**，中性，无崩盘。
- 官方强场 1,432 个出牌决策：**56 次改判**；其中庄位决策 285 个 ⇒ 约 20% 庄位出牌被条件化。
- draw p99 40.2ms、window p99 1.1ms，规则一致。
- **已进入第一波五臂**；与 c194 一起验证庄位提速和中盘拆对。

### 2l. 首副露覆盖分解（2026-09-17 21:0x）

官方强场 267 个有效窗口中，候选相对 speedtugc 新增的接受：

| 策略 | 第一次副露 | 已有副露后的追加 |
|---|---:|---:|
| c190 | 6 | 8 |
| c191 | **15** | 22 |
| c146 | **26** | 12 |

- 强者的覆盖差距主要是“有副露局占比”，所以 c146 的 26 次首副露最直接；c191 兼顾首副露和追加；c190 偏追加。
- 第一波五臂：c146 回答“首副露覆盖”，c191 回答“学习副露”，c193/c194 回答“庄位/中盘速度”；c190 作为剂量回退。
- ⚠ 门户对非我方 gid 返回 **403**：无法直接拉榜前玩家的锦标赛复盘；强手学习只能使用已有 `server/recent` 的 top32 练习房语料。

### 2m. 下一轮预备候选：c194（中盘 3+ 对子外科式拆对）

`c194 = speedtugc + 中盘（全局弃牌 12–28）且基线弃后仍 ≥3 对时，只接受同向听、减对子、真进张损失 ≤4 的替代。`

- 官方强场 1,432 个出牌决策：**40 次改判**；493 个中盘决策中 85 次基线留 3+ 对，40 次安全拆对，45 次因进张损失>4 被挡。
- 普通语料足迹：300 文件 / 65 个中盘决策 / 17 次基线 3+ 对 / 3 次安全拆对。
- 冒烟/延迟：draw p99 51.7ms、window p99 1.4ms，0 超预算。
- 20 房旋转 sim 对 3×speedtugc：**-5.80/房（t=-0.37）**，中性无崩盘。
- **已进入第一波五臂**；验证外科式中盘拆对是否把中盘听牌差转成分数。

### 2n. 下一轮优先级（官方强场代理）

对官方强场中实际改判的手牌，比较向听 / 真进张 / 弃后对数：

| 候选 | 改判 | 向听变差 | 真进张均值 | 真进张中位 | 对数变化 |
|---|---:|---:|---:|---:|---:|
| c193 | 56 | **0** | **+2.00** | +1.0 | -0.48 |
| c194 | 40 | **0** | **+0.88** | 0.0 | -1.00 |

⇒ 两者不是“拿速度换价值”的高风险改动，已纳入第一波五臂：
1. **c193 庄位条件化路线**
2. **c194 中盘 3+ 对子外科拆对**

第一波五臂为 `speedtugc,c146,c191,c193,c194`；c190 暂作剂量回退，不占首轮工位。

### 2o. 首副露质量分解（2026-09-17 21:1x）

对官方强场中相对 speedtugc 新增的接受窗口，按真实进张变化分类：

| 策略 | first/up | add/up | add/flat | first/flat | first/dsh>0 | err |
|---|---:|---:|---:|---:|---:|---:|
| c146 | **26** | **12** | 0 | 0 | 0 | 0 |
| c191 | 12 | 17 | 5 | 1 | 1 | 1 |

⇒ **c146 的 38 次新增接受 100% 是 real-ukeire up**，26 次还是第一次副露；这不是放宽门控，而是纯质量提升。  
⇒ c191 也有 29/37 up，但含少量 flat/dsh>0；作为学习副露的次级候选。  
⇒ 第一波判定时，c146 优先看“有副露局占比 + 分/房”，c191 看“学习覆盖是否带来额外增量”。

### 2p. 100 房 sim 复测：主候选回到中性（2026-09-17 21:2x）

扩大到 25 seeds × 4 座位 = 100 房，对 3×speedtugc：

| 策略 | 净胜/房 | t | 胡/桌 | 爆头/桌 |
|---|---:|---:|---:|---:|
| c191 | **+0.12** | +0.02 | 2.03 | 0.85 |
| c146 | **-1.31** | -0.24 | 2.07 | 0.91 |

⇒ 20 房的小正效应没有复现；两者在 sim 上都回到中性。  
⇒ **c146/c191 的采用依据只能是真机强场 A/B + 机制端点，不能拿 sim 当利好**；当前五臂设计不变。

### 2q. 质量门控完整性最终核对（2026-09-17 21:2x）

修正此前分类器的错误（把“更差向听选项的更多进张”误算为 quality-up）后：

- 合法 quality-up：**38 接受 / 3 边缘漏收**；
- 18 个表面 sh_down 漏收：全部是已有 **2 组吃**、第三次吃会被规则禁止的窗口；
- flat/down 合法窗口：正确拒绝。

⇒ **c146 对合法的“向听下降或同向听进张变好”窗口几乎满覆盖**。之前“62 个漏收”是分类器 bug，不再引用。  
⇒ 第一波 c146 的看点是：这份接近满覆盖的质量副露，能否把 coverage 提升转成真机分数。

## 3. 端点与强场要求

主端点：
- 原始分/房（服务器 total_score）
- 强场胜率/第一名率：必须优先看与榜单 top32 同房或官方锦标赛的样本
- 候选终点仍按组合臂 150 房/臂、t>=1.50；同时报告 12 房机制止损

机制端点：
- `c190`：学习副露接受率、无白坏窗口否决率、8 巡听牌
- `c146`：有副露局占比、质量副露接受率、杠与财飘
- `c146`：杠/轮、杠开胡占比
- `c141/c146`：财飘触发与财飘胡（频率低，不作单独主判据）

红线：
- 不再用随机练习池的正 A/B 点估计直接替代强场验证。
- 任何 c151 家族策略若要复活，必须在官方强场或 top32 同房中重新证明。
- C036 弃胡换爆头保留；延迟门禁与规则闸门不变。

## 4. 本次二测的可复用证据

- 权威复盘目录：`var/replays/official_20260917/`（10 个门户格式 gid）
- 本地决策/事件：`var/replays/official_1024_20260917_190853/`
- 延迟审计：`tools/official_latency_audit.py --lowprio`
- 逐玩家统计：`tools/real_game_audit.py --dirs official_20260917 --top 40 --min-rounds 8`

## 5. 主攻轴定案（2026-09-17 23:0x 新增证据）

### 5a. 胡率缺口两段分解 ⇒ **主战场 = 听牌率**（R423）

新工具 `tools/hu_gap_split.py`（门户四家全信息重建，无 dec 相位坑），2,444 文件 / 我方 19,544 席局 vs top32 18,664 席局：

| 组 | 胡率 | 听牌率 | 均听巡 | 听牌局胡率 | 爆头态率 |
|---|---:|---:|---:|---:|---:|
| 我方 | 24.0% | **53.0%** | 6.66 | 45.3% | 5.0% |
| top32 | 28.7% | **59.8%** | 6.20 | 47.1% | 7.3% |

算术分解（闭合到 −4.76pp vs 实测 −4.69pp）：

| 项 | 贡献 | 占比 |
|---|---:|---:|
| **听牌率（速度）** | **−3.20pp** | **68%** |
| 听牌后兑现（质量） | −1.05pp | 22% |
| 未听局胡率 | −0.51pp | 11% |

⇒ **胡率那一半缺口的主体是"到不了听牌"**。听张宽度 / 弃牌安全类机制优先级下调；**副露 · 牌效 · 路线 = 听牌率**是主战场。
⇒ 第一波五臂里 `c146`（首副露覆盖）/ `c191`（学习副露）方向被独立证实；`c193`（庄位路线）/`c194`（中盘拆对）是同一轴的次级入口。

### 5b. 杠缺口定案 ⇒ 机制已够，价值有限（R424）

`tools/gang_gap.py`，3,504 文件同房头对头：

| | 杠/轮 | 明杠 | 补杠 | 暗杠 | 杠开/胡 | 杠后胡/杠 |
|---|---:|---:|---:|---:|---:|---:|
| 我方 | **0.005** | 0.004 | 0.001 | 0.000 | 0.34% | 12.2% |
| top32 | **0.026** | 0.012 | 0.010 | 0.004 | 1.58% | 15.8% |

- 相对差 **5.2×**（补杠 10×、暗杠 8×），top32 最低者 0.006 ⇒ 我方是**全榜外离群点**；
- 但**机制打开即达标**：`speedc146` 0.0278、`speedc156` 0.0331、`speedc151` 0.0235；而 `speedtugc` 0.0025 / `speedc193` 0.0000 / `speedc194` 0.0059。
- **价值定界 −5~−7 分/房**（R313/R319 定价）⇒ 属于"顺手补差"，**不足以翻盘**；不得为刷杠率牺牲胡率/七对。

### 5c. 下一波（第二波）候选池 —— 只收"抬听牌率"的机制

第一波五臂到 12 房后（`tools/ab_mech_verdict.py`），若 c146/c191 未把"有副露局占比"转成分数，
第二波按下列**优先序**建臂（每条都必须先在官方强场 dec 上证明"改判存在且不降向听"）：

1. **c193+**：把庄位条件化路线从"仅庄"扩到"庄 + 副露后"（副露局是听牌率主通道，且庄位机制已证 0 向听恶化）。
2. **对子作为速度通道的估值修正**：`真进张` 只数摸牌改善、**不计碰**，疑似系统性低估对子（R314 提出、R318 因相位坑撤回）。
   现在有了可信重建（R423 工具链），**必须先重做该对照**再决定是否建臂。
3. **副露后的听牌最大化**：给"已副露"状态单独优化弃牌（现有出牌逻辑是门清口径），这是 c146/c191 覆盖提升后最可能的下一个瓶颈。

**不做**：再训中盘副露二分类器（R405/R406 已否）；复活 c151 家族（强场已否）；为刷杠率单独开臂（R424）。

### 5d. 弃牌层：未听牌不用真进张（R425）⇒ 第二波头号候选

- 重建精度已用 bot 自己的 dec 校验：**124/124 手牌逐张一致**。
- 我方未听牌弃牌的"真进张最优命中率" **~74%** vs top32 **~92%**；均进张损失 **1.16~1.55** vs **0.22~0.50**。
- 代码根因：`bot/speedt.py::_best_discard_t` 的主键只在 `s==0` 时算 `wcnt`，未听牌直接落到硬编码 `_pref`。
- ⚠ 反证：C038（全向听级 ukeire）当年 sim 中性、C039（照搬顶级牌性偏好）更负 ⇒ **不得用 sim 裁定**。
- **第二波候选 c195**（唯一变量）：把 `_best_discard_t` 的次键从 `-wcnt(仅听牌)` 改为
  `(-wcnt_all_shanten)`（未听牌用 `real_ukeire` 的剩余张数），`_pref` 降为第三键。
  必须先过：延迟门禁（`real_ukeire` 的 p99）、M=10 并发门禁、以及**官方强场 1,432 决策的改判审计**（改判次数、向听不变率、进张变化）。

### 5e. 执行层缺陷：代打率 3.48% vs 0.48%（R426）⇒ 与策略无关的第一优先修复

- 我方 3.48% 的弃牌被判为服务端代打（交叉验证：这些弃牌与我们策略一致率仅 9.1%，正常弃牌 89.9%）。
- 每房约 0.5 次随机弃牌，且**等量污染五个 A/B 臂**。
- 修复方向（不改策略）：查 `run_bot.py` 的窗口轮询/回包路径、心跳与进程停顿、以及 M=10 并发下的 GIL/GC 停顿；
  目标是先把代打率压到 top32 量级（≤1%）。

### 5f. ★第一优先（压过一切策略候选）：**庄家开局首弃代打 48%**（R429~R432）

- **现象**：我方做庄、且本局第一个动作就是我们的弃牌时，**48.05%** 被服务端代打（top32 9.35%、其他 14.55%）；**8 个局号全都 ~45%**，非冷启动。
- **是回归**：09-10 我们 **9.22%**（与 top32 齐平）→ **09-12 起 67~75%** 直到今天。09-17 二测全程 66.55%。
- **代价**：代打那张平均白丢 **5.67 张真进张**（我们自己只有 0.185），31 倍；32.1% 的代打造成实质损失。
- **机制证据**：bot 自己的事件录制在 `round_ended` 后**整段丢事件**（764 round_ended → 767；765=我方首弃、766=超时标记从未收到）。
- **修复方向（不改策略）**：
  1. 先做**只加日志**的探针：在 `round_ended` 分支打印 `seq/turn/phase/res_kind/my_hand` 长度，并在"轮到本人但没产生动作"时打印
     `phase/turn/seat/my_hand/drawn/res_kind/seq` ⇒ 下一场就能区分"没收到事件" vs "收到但 `my_turn` 判假"。
  2. 候选修复：`round_ended` 分支**把 `seq` 归零**（强制下一轮走全量快照，绝不可能跳过新局首事件）；
     `_state(0)` 与 notify 帧的交互需要先确认是否会"吃掉"事件。
  3. 兜底：**"轮到本人 + 无 drawn_tile + 手牌 14 张"必须是一等公民路径**，绝不能因为缺摸牌事件就放弃本回合。
- **验收**：`python -X utf8 tools/dealopen_audit.py` 的"我方庄家首弃代打率"从 48% 降到 ≤12%（top32 量级）；
  同时全局弃牌代打率 2.40% → ≤1.0%。**在这条修好之前，新策略候选的 A/B 都带着等量噪声。**

### 5f. ✅ 已修复并验证：庄家开局首弃代打 48% → 4.7%（R438~R440）

- **根因**：bot 事件驱动；**庄家开局首弃是全桌唯一没有前置事件的动作**，没有任何唤醒源 ⇒ 只能干等最长 30s 的长轮询，
  而窗口只有 ~3s。探针实测新局后服务端持续返回 `phase=settled`，第一版 3s watch 在"新局真正开始"前就到期。
- **改动**（唯一行为改动，`bot/game.py`）：`round_ended` 后有界主动短轮询（每 0.5s `_state(0)`）**直到 `phase != settled`**，上限 8s；
  离开 settled 立即交回正常路径；异常回退长轮询。诊断日志已收口（`HM_TRACE_STATE`）。
- **验证（门户口径，独立于探针）**：
  - 庄家首弃代打率 **48.05% → 4.7%**（0/22 + 2/21，n=43；top32 为 9.35%）
  - 全局我方弃牌代打率 **2.40% → 0.41%**（0/759 + 6/692，n=1451；top32 为 0.48%）
  - 修复后两场 **+117（rank1）/ +104（rank2）**；全量回归 **689 项 OK**
- **纪律**：A/B 期间禁跑重活（本机负载曾把该率推到 86%）。
- 后续监控：每 2~4 小时跑一次 `python -X utf8 tools/fetch_room_replays.py --since <上次> --rooms 20`
  再 `python -X utf8 tools/dealopen_audit.py --since <上次> --no-cost`，**门禁：庄家首弃代打率 ≤12%、全局弃牌代打率 ≤1.0%**。

### 5g. ✅ 修复第二版达标（R443/R444）

- 实测结算空档 0~8s（中位 5s、20% 恰好 8s）⇒ 第一版 8s 上限刚好卡住；第二版改为 **12s 上限 + 只在"下一局庄家是我"时开 watch**。
- 门禁（`python -X utf8 tools/exec_leak_check.py --since <t>`）：
  - **新局首动作代打 39.11% → 4.55%**（阈值 ≤12%，PASS；top32 为 9.35%）
  - 整体我方弃牌代打 3.11% → 1.01%（阈值 ≤1.0%，临界；其中"自己摸牌后"随本机负载起伏）
- ⇒ **结构性执行缺陷已清零**；后续该项作为常驻门禁，每次心跳复核一次即可。

### 5h. ★下一波的头号机制候选：**弃牌层残差**（R445）

- 同房对照（可信重建 + `real_ukeire` 尺子）：基线 **76.2%** 最优命中 → c135 族 **87.4%** → **top32 95.4%**；
  均进张损失 1.163 → 0.422 → **0.195**。
- ⇒ **c135 只补了一半**（旧结论夸大）；弃牌层仍有 8pp / 2.2 倍的差距，且落在听牌率这条 68% 主轴上。
- 但 c135 的零结果是**欠功效**的（~26 房，判出力 ±40 分/房）⇒ 不能据此否定该轴。
- **候选 c196（待建，唯一变量）**：在 c135 的真进张次键之上，把**可见张数的输入口径**与度量口径对齐
  （自家手牌 + 四家弃牌河 + **四家副露**），并只在**同一次会话**的房上先做 `river/seq` 一致性验证
  （这是唯一未验证的输入项，见 R446 ④）。上线门槛同 c135：延迟门禁 + M=10 并发门禁 + 官方强场改判审计。
- 若要判"这条轴值不值"，必须给到 **≥100 房/臂**（单变量终点 t≥1.96）。

### 5i. ✅ 修复 #3（取态层水位跳跃）：河保真度 ×10，执行层零代打（R447/R448）

- 根因：`bot/game.py` 每批事件后**再拉一次全量快照**并把水位推到 `auth.seq` ⇒ 两次请求之间到达的事件被永久跳过
  （别人的弃牌 + **`round_ended`**）⇒ 河既偏短又跨局不清零 ⇒ 所有 `real_ukeire` 活张数失真。
- 修复：保持 `seq = 最后一条已处理事件`。全量回归 689 项 OK，无重复处理。
- 端点：**原始口径平均缺口 0.97 张**（修复前 少1.1~4.4/多6.0~15.1）；同房执行层 **0.00% 代打**（726 决策）。
- 常驻门禁：`python -X utf8 tools/river_fidelity.py --since <t>`（阈值 ≤1.5 张，抓数量级回归）。

### 5j. ★下一波预登记：用 c135 族臂**伪证**"残差=输入问题"（R449）

- 推断：c135 的排序目标与度量目标逐位一致（`s>0` 段），因此它 87.5% ↔ top32 95.3% 的差距只能来自**输入（河）**；
  河已在 R447/R448 修好。
- **可伪证预测**：下一波放一个 c135 族臂（建议 `c135` 本身做单变量，或 `c146+c135` 束），
  在修复后的房里其"最优命中率"**≥93%**。若不成立 ⇒ 推断错，回到"输入差异"清单里找别的项。
- 端点优先级：机制端点 = `tools/discard_eff_audit.py` 的该臂最优命中率/均进张损失（干净、可复算）；
  分数端仍按 ≥100 房/臂（单变量终点 t≥1.96）。

### 5k. ✅ 弃牌层收口（R450）：算法已最优，输入已修复

- 离线验证：c135 的弃牌函数**喂正确可见集时 100%（1301/1301）落在最优集合**；生产口径 87.5% ⇒ 差距 100% 来自输入（河）。
- 河已修复（R447/R448），门禁 `tools/river_fidelity.py` 通过（平均缺口 1.05/0.00 张）。
- **下一步只剩"换算成分数"这一问**：下一波放一个 c135 族臂，端点 = ①机制（`discard_eff_audit` 该臂最优命中率，
  预期 ~100%）②分数（≥100 房/臂，单变量终点 t≥1.96）。
- ⚠ 不要把 100% 读成"强于 top32"：他们 95.3%，但可能在优化更丰富的目标（ukeire↔打点/安全的取舍）。

### 5l. ★第二波预登记（2026-09-18，按本轮 12 房机制止损自动决定臂集）

**共同要求（无论本轮结果如何）**
1. **必须包含一个 c135 族臂**，用于伪证/证实 R450 的可伪证预测：
   c135 的弃牌函数喂正确可见集时离线 100% 落在最优集合；预测真机同房该臂"最优命中率"应 ≥95%
   （对照：修复前生产口径 87.5%、top32 95.3%）。
2. 臂数尽量少（2~3），把判出力换到**每臂房数**上（`docs/候选池-下一个月.md` §0★.4：一位仅能判 ≥33 分/房）。

**臂集决策表（本轮 12 房机制止损后照此执行）**

| 本轮观察 | 第二波臂集 | 理由 |
|---|---|---|
| `c191` 有副露局占比↑ **且** 分数同向 | `speedtugc` vs **`c191+ c135`**（束） vs `speedc135` | 学习副露 + 真进张弃牌分别已被本轮/离线证实"机制在动"，用束换功率 |
| `c146` 覆盖↑ 但分数不动 | `speedtugc` vs **`speedc135`**（单变量） | 先把"弃牌层"这条干净轴的分数价值钉死，再谈副露后听牌 |
| `c193`/`c194` 机制端点未动 | 直接淘汰，不占第二波工位 | 机制没动=不是它们没用，而是**干预没发生**，先修实现 |
| 出现臂崩坏（机制反向后恶化） | 立即停该臂，只保留 `speedtugc` + `speedc135` | 保功率 |

**上线前门禁（缺一不可）**
```powershell
python -X utf8 var/_concurrency_bench.py speedc135 --threads 10 --per-thread 100 --warm 0
python -X utf8 var/_concurrency_bench.py speedc135 --threads 10 --per-thread 100 --warm 0 --kind window
python -X utf8 tools/rules_guard.py --strategy speedc135
python -X utf8 -m unittest discover -s tests
```
- c135 的历史尾延迟是 **p99 ≈ 1.0s / max ≈ 2.8s**（3s 预算内但接近）⇒ M=10 实测必须 0 次 >3s / 0 次 >1s（window）。
- 若并发门禁不过 ⇒ 用 `c136`（同为质量感知、延迟 0/2000）或先做延迟优化再上。

**端点与判据**
- 机制：`tools/discard_eff_audit.py` 该臂最优命中率（预期 ≥95%）；`ab_mech_verdict` 的胡率/番/爆头/有副露局占比。
- 分数：**≥100 房/臂**（单变量终点 t≥1.96）；组合臂按 150 房/臂、t≥1.50。
- 每一读数**必须同时报**代打率（`exec_leak_check.py`）与河保真度（`river_fidelity.py`）。

### 5m. ★听牌率缺口的钱花在哪（R452）：3.8pp 给副露覆盖，5.9pp 给"同档内路线"

- 8 巡听牌率缺口 −9.7pp 分解 ⇒ **39% 副露覆盖面 / 61% 同档内速度**（门清 −6.7pp、副露 −5.3pp）。
- 因此第二波之后若还要再上一个机制，**必须针对"同档内更快的路线选择"**；副露覆盖类机制的天花板约 +3.8pp。
- 已知不可用路径（避免重走）：弃牌层算法已最优（R450）；中盘副露二分类器已否（R405/R406）；
  c151 全局压对数强场已否；C039 照搬顶级偏好更负。
- 尚未试过、且与"同档内路线"直接相关的方向（按可行性排序）：
  1. **二步前瞻弃牌**（不只最大一步真进张，还要看第二步的可改善面）——C048 rollout 已否（8s/局太慢），
     但**有界二步**（只对最小向听组的前 k 个候选、限时 300ms）没试过；
  2. **对子/搭子的"碰通道"定价**：真进张只数摸牌，把"可碰"折算进候选价值（R314 提出、从未用可信重建验证过）；
  3. **副露后的听牌最大化**（现有出牌逻辑是门清口径）。

**纪律**：以上任何一条都必须先在**离线同房对照**里证明"能提高同档内 8 巡听牌率"，再进真机 A/B；不要直接上臂。

### 5n. ★修正 §5l 的臂集与"二测判决可沿用性"（R453）

- **必须重审**：09-17 二测对 c151 家族的强场判决，是在**弃牌河错 + 庄家首弃 48% 代打**两个 bug 都在场时做的
  ⇒ 与 E001 后"C010/C013/C017 作废待重测"同理，**该判决不能直接沿用**。
- **1 步进张轴已追平/超过强手**（强手张数口径 92.5%，c135 喂对输入 100%）⇒ 不要再在"一步真进张"上做文章。
- 下一波臂集（修正）：
  - 首选 **`speedc135`**：干净单变量（只改弃牌次键），验证 R450 的可伪证预测；
  - 延迟安全兜底 **`speedc156`**（同含 c135 弃牌路径，全时段 max 1992ms / 0 次 >3s）；
  - 若本轮 `c191` 也正向，则扩成 3 臂 `speedtugc` / `speedc135` / `speedc191`。
- 剩余真问题（5.9pp 同档内速度）**不在这条轴上**，按 §5m 的三个方向另开离线验证，不要指望 c135 解决它。

### 5o. ★本轮机制止损的**精确端点**（R454）

副露覆盖面这一类机制的端点是**一个分布**，不是"覆盖率"一个数：

| 指标 | 我方当前 | top32 | 目标 |
|---|---:|---:|---|
| 无副露局占比 | **40%** | 27% | ≤30% |
| 首次副露均值（第几次摸牌） | **3.77** | 3.37 | ≤3.4 |
| 副露@0-1 占比 | 13% | 19% | ≥17% |

- 价值评估：只做组成（不动档内），上限 **+4.8pp / 8 巡听牌率**；档内那 **3.4pp** 需 §5m 的二步路线机制。
- 判定顺序（12 房机制止损时照此读）：**先看分布有没有动**（没动=干预没生效，停臂）；再看 8 巡听牌率有没有同向；最后才看分数。

### 5p. ★领先指标出炉（R455）与两条待办

- **c146 机制达标**（无副露 25.6%、首副露 3.41 ≈ top32 的 27% / 3.37）⇒ 12 房时重点看它能否把分布转成 8 巡听牌率（价值上限 +4.8pp）。
- **c191 机制未动**（36.9% / 4.26 vs 基线 37.5% / 4.51）⇒ 12 房仍不动即停臂。
- **一级重审候选（新）**：**把副露门控从 strict 放宽回 `allow_equal`**（= speedtu 族门控）。
  依据：全量门户口径 top32 无副露 **27%**（比我们少），而放宽门控的 speedtu/speedtugh 实测无副露 **18~22%**、首副露 3.46~3.54、
  8 巡听牌 **47~48%**，三项都优于严格门控基线（39% / 3.86 / 43.2%）。这与 09-11 的旧判断（据小样本改严）方向相反。
  ⚠ 不同期/不同池 ⇒ 必须进真机 A/B 裁决（建议与 `speedc135` 同役，2~3 臂）。

### 5q. 候选收敛（R456）

- **不排"无条件 allow_equal"臂**：`speedtu` 无副露 18~22% **低于** top32 的 27%，属于**副露过多**；
  `c146`（c136 质量门控）实测 **25.6%** 最贴合。⇒ 门控这条线**以 c146 为准**，若要再动只在 c136 的质量阈值上微调。
- **二测污染已量化**（官方复盘 10 局）：新局首动作代打 **13.88%**、整体 **3.13%**，且全时段弃牌河失真。
  c151 是重度 `real_ukeire` 策略 ⇒ **其强场判决作废待重测**；重测首选 `speedc135`。

### 5r. E2（二步期望进张）——**未找到证据**，需要重测（R458）

- 首测（n=70/组、早盘偏采样）：E2 比 E1 略好（我方 71.4→74.3%、top32 80.0→85.7%），但差距未解释掉、且样本不足。
- **不许据此外推**；若还要走这条路，先做**随机采样 + n≥500/组**的复测（脚本 `var/_e2.py` 可直接改采样）。
- 优先级判定：**低于** 本轮 c146 的 12 房读数（c146 已证实能同时改善"副露组成"与"首次副露时点"，价值上限 +4.8pp）。

### 5s. 残余 3.4pp 仍未定位（R458/R459）——下一轮排查顺序

已被否证的候选：弃牌层算法（R450 最优）、二步期望进张（R458 未支持）、桶内副露时点（R459 一致）、
"低估对子"（R453 方向相反）、中盘副露二分类器（R405/R406）。

尚未测、按成本排序：
1. **副露质量**（不是时点）：同桶内我们接受的那组副露，其"after 手牌质量"分布是否比强手差？——离线同房对照即可，最便宜。
2. **副露后的打法**：现有出牌逻辑是门清口径，melded-hand 的进张/听形未必最优。
3. **白/财神用法**差异。
4. 口径内生性：桶按"已实现的副露数"分层，"有副露机会却没副露成"的局会落进无副露桶。

### 5t. ★归因收敛（R460）：下一波两臂各自负责一块，且各有可伪证预测

| 缺口 | 归因 | 负责臂 | 可伪证预测 |
|---|---|---|---|
| 组成 **4.8pp** | 副露占比 40%→27%、首副露 3.77→3.37 | **`c146`** | 副露分布达到 top32 水平（已实测 25.6%/3.41 ✓）；8 巡听牌率应同向 |
| 同档内 **3.4pp** | **≈ 弃牌层差距本身**（基线 75.9% vs 强手 92.5% 最优） | **`speedc135`** | 修复后的房里，c135 的"同档内 8 巡听牌率"**高于** `speedtugc`；其最优命中率 ≥95% |

- 已连续否证的解释（不要再走）：弃牌算法（R450）、二步期望进张（R458）、桶内副露时点（R459）、副露质量（R460）、低估对子（R453）。
- 若两条预测**都不成立**，才回到"未定位"清单（副露后打法 / 白用法 / 口径内生性）。

### 5u. 三项执行修复的分数效果（R463）——报数时必须带混杂警告

- 修复前 16 房 **−55.2**、修复后 15 房 **+41.7** ⇒ **+96.9 分/房，t=2.42**；正分率 31%→80%。
- ⚠ 修复前窗口同时是本机重负载期 ⇒ 其中一部分是"负载恢复"效应；**只能把机制端点算作干净归因**。
- 对外/对内报数统一口径：**"分数 +96.9/房 (t=2.42，含负载混杂)" + "机制端点 48%→2.7%、河缺口 15→1.1"**。

### 5v. 分数效果的第三重证据（R465）

- **战役内回归**（23 房，不依赖前后切分）：首动作代打率 **−12.0 分/pp（t=−3.34）**；整体代打率 −33.1 分/pp（t=−1.70）。
- 因果量级区间：由观测前后差反推 **≈3.6 分/pp**，由回归斜率给 **12 分/pp** ⇒ 真实值大概率落在 **3.6~12 分/房每 pp**。
- 三重证据合并报数：①机制端点（干净因果）②前后差 +96.9/房 t=2.42（含负载混杂）③战役内回归（方向独立确认，t=−3.34）。

### 5w. 第三波预备：价值/爆头轴（R466）

- 修复后重测：番/胡 **1.297 vs 1.368**、爆头占胡 **22.4% vs 27.0%**、杠开 **0.4% vs 1.6%** —— 与修复前指纹逐项一致
  ⇒ **执行修复不改变价值取向**，该轴缺口独立存在。
- 二测对该轴（c151 家族）的判决同样被污染（R456）⇒ **需要重测**。
- 定价（实测斜率）：爆头 ≈ −12 分/房、杠开+财飘 ≈ −5~7 分/房 ⇒ 合计 **−17~19 分/房**（小于听牌率轴）。
- **排序**：第二波（速度轴 `speedc135` + 组成 `c146`）→ **第三波（价值/爆头轴）**。

### 5x. ⚠ 正式赛选型的**前置校验**（R467，优先级等同门禁）

- 实测推翻"有财必拷响会拒 64% 胡牌"：二测 **35 次 hu 决策全部被接受**；"平胡+有白"在二测/阶梯分别占 71%/86%。
- ⇒ **不许凭 config 标签选臂**。赛前必须：①读 `/rules` 记 `YouCaiBiKao`；②**用候选策略实测 1 房**并查 `hu` 决策的 `sub`：
  **出现 409 才切闸门臂（c148/c153），0 次 409 则按默认非闸门策略选型**。
- 影响面：若正式赛真的开始拒，则 **c135/c146 也必须加同一闸门**，否则第二波/第三波的结论在正式赛不可用
  —— 这一点写进 T-24h 清单（已落到 `docs/正式赛执行清单-一个月后.md` §1.1）。

### 5y. 裁决用换算尺子（R469）

- **+1pp 8巡听牌率 ≈ +13.5 分/房**（修复后 23 房回归，t=3.00，R²=0.27）。
- 12 房/臂可辨 ±2.5pp ⇒ ≈ **±34 分/房**（与 §0★.4 功效表一致）。
- ⚠ 修复的分数效果里约 **70% 不在听牌率上**（推测是被迫弃牌导致的放铳/送分）⇒
  以后评估执行层修复时，除听牌率外要同时报**我方放铳率 / 被爆头率**。

### 5z. 放铳率复核（R470）——不是缺口，也不是修复的收益来源

- 我方放铳率 **18.2%** vs top32 **19.3%**（全时段）⇒ 我们**更少放铳**。
- 本战役前后：18.57% → **18.74%（不变）**；胡率 23.18% → **25.15%（+1.97pp）**。
- ⇒ 报数口径：执行层修复的收益走**胡率/听牌率**；把"少放铳"从解释里删掉。
  分数提升约一半可由观测量解释，其余在噪声内（**不要写 70%**）。
- 常驻监控建议再加一项：**放铳率**（`var/_deal_in.py`），用于发现"修执行反而多放铳"这类反向副作用。

### 6. ★12 房机制止损判定表（R471，已按方差算准）

- 阈值：**8巡听牌率 ±3.1pp**（≈42 分/房）；**无副露占比 ±4.9pp**（1.5σ @ 12 房/臂）。
- 判定：分布动 ≥5pp 且听牌率 ≥+3.1pp ⇒ 保留；分布动但听牌率不动 ⇒ 停臂并记"覆盖≠速度"；
  分布不动 ⇒ 干预没生效，立即停臂；听牌率 ≤−3.1pp ⇒ 反向伤害，立即停臂。
- **分数不在 12 房裁决**（可辨 ±88 分/房，分辨率只有机制端点的一半）。

### 6.1 第四项执行改善：吃/碰 409 降 3~5 倍（R472）

- 修复后 claim 409：`response_peng` **0.64%**、`response_chi` **1.38%**（修复前 3.15% / 3.65%）。
- 与 R454 的关联：这等于**在已修好的执行器上又少丢 2.3~2.5pp 的副露机会**，正落在"副露组成"轴（听牌率缺口的 58%）上。
- 常驻监控：建议 `var/_claim_health.py` 与放铳率一起纳入每次心跳的复核清单。

### 6.2 数据卫生纪律（R473）

- **门户补拉一律 `--rooms 20`（或更大）+ `--since`**；小 N 只取"最近 N 房"且**不会报告漏了哪几房**。
- 拉到后必做**三步自检**：①`tools/ab_integrity.py`；②`var/_arm_endpoints.py` 中各臂**席局数 ÷ 80 = 房数**是否与 `auto_ranking` 一致；
  ③**`python -X utf8 var/_portal_completeness.py`**（逐房应恰好 10 局；本次靠它发现 6 场整房缺失，全在修复前）。

### 7.0 ★★★口径更正（R519，2026-09-18）——**目标单位是"分/房"，不是"总分"**

**下面 §7 的"+37/房"作废**：它把"全史榜缺口 ÷ 一个月"当成目标，但**全史榜是沉没成本**（我们的负分主要来自早期实验）；
**平台每周一重置周榜**（`period=week`），所以每一周都是**从零开始的新比赛**。真正的对标线是**周榜 top-32 的速率**：

| 周期 | 我方 rank | 我方房 | 我方分 | **分/房** | top1 | **top32 门槛** |
|---|---:|---:|---:|---:|---:|---:|
| all | 584 | 552 | −9,132 | −16.5 | +112.3/房 | +26.2/房 |
| **week** | **207** | 326 | −5,568 | **−17.1** | +111.1/房 | **+48.3/房**（13,233） |
| today | 106 | 49 | −1,065 | −21.7 | +109.4/房 | +50.7/房 |

- first%：我方 **16.9%** vs top32 **33.6%**。
- **目标 = 把速率从 −17.1 抬到 ≥ +48.3 分/房（+65）、first% 从 16.9% 抬到 ≥33.6%（+17pp）。**
- 已知杠杆换算：执行层（R488+R508）≈ **+6~7 分/房**；听牌率 **+1pp ≈ +13.5 分/房** ⇒
  执行 + c135（配对 +2~3pp）≈ **+34~48** ⇒ 预计落在 **+17~+31 分/房**，
  **仍差 +17~+31** ⇒ 一个月内**必须再上一个 ≥+1.5pp 听牌率的机制**（第三波价值/爆头轴，或 §5m① 有界二步前瞻）。
- 工具：`python -X utf8 var/_ladder_rates.py`（三张榜 + 本周剩余预算 + 需要速率）。
- 纪律：**多打不能救负分/房**（房间零和，−15.3/房 ⇔ 三个对手各 +5.1）⇒ 任何"提高产出"的想法必须先提高分/房。

### 7. ★目标级算术（R475）——距离 rank 32 只差约 +5 分/房

| | 数值 |
|---|---|
| 我方 | rank **579** / 529 房 / **−8,708** / −16.5 分/房（R482 复测） |
| rank 32 | +26,033（+25.4/房，first 26.0%） |
| 差距 | +34,304 分 |
| **追上 rank 32 所需** | **≈ +37/房**（30 天 × 4.1 房/h） |
| **我方修复后读数（R482/R483 更正）** | 原始 **+4.3/房**（19 房）；**桌强调整后 +13.0/房** ⇒ **差约 +24/房** |

- 待补的 ~5 分/房 由两条已被证据锁定的机制去争：**速度轴 c135**（同档内 3.4pp）+**组成轴 c146**（4.8pp，×13.5 ≈ 65 分/房的理论上限，实际按比例折算）。
- 每月重算本表（`var/_ladder_gap.py`）；若 rank 32 加速，门槛会上升。

### 7.1 引用 R454 分解时必须带的口径（R476）

- "组成效应 4.8pp" 是**上界**：它假设桶内率不随构成变化。把局从"无副露"搬到"副露@2-3"时，
  这批局本身可能更差（"能副露却一直没副露成"）⇒ 实际收益 < 4.8pp。
- 12 房判定时看**两件事**：构成位移 ≥5pp **且** 总听牌率 ≥+3.1pp。
  只动了构成而总率没动 ⇒ 证伪"副露覆盖→速度"，**停 c146**，工位让给 c135 与第三波。
- 桶级读数（单桶 n≈50~100）在 3 房时误差 ±7~10pp，**不可用于裁决**。

### 8. ★★路线修正（R477）：第二波改为**两臂**，组成轴降级

**理由（既有反证 + 新数据一致）**：STATUS §9.21 用**自体内自然实验**证明"副露多 ↔ 听牌率高"是**选择而非杠杆**
（09-10 历史副露最多那天 ex=0 仅 58.1%，总听牌率 **15.9%**，反而低于副露少得多的 09-11 的 **17.1%** ⇒ **无剂量反应**）；
"多吃碰"已被三份自体内证据否掉（§9.11/§4c-48/§9.21）。
我的新数据与之一致：**c146 组成位移 13.7pp（过阈），总听牌率只 +1.2pp（未过阈）**。

**修正后的第二波**：

| 臂 | 角色 | 可伪证预测 |
|---|---|---|
| `speedtugc` | 基线 | — |
| **`speedc135`** | **唯一候选** | 修复后的房里：最优命中率 ≥95%、**同档内 8 巡听牌率 ≥ 基线 +3.1pp** |

- **c146 撤下第二波**（不再用决策位重测已被否掉的轴）；若 12 房读数出现"组成动 + 总率也动 ≥3.1pp"的反常，
  再把它作为候选重新排队（届时有直接证据）。
- 两臂 ⇒ 每臂房数翻倍，功效更高（100 房/臂仅需 200 房 ≈ 2 天）。
- **价值/爆头轴**仍排第三波。

### 8.1 新纪律（R477）：跨截面对比不得直接当候选

推任何"跨组分层差"（如"强手在 X 桶更强 ⇒ 我们补 X"）之前，**必须先 grep 状态文档确认该方向有没有"自体内剂量反应"检验**。
本项目已连续否掉 4 条此类外推（§9.11 / §9.21 / §9.25 / §9.34）——跨截面差异在这些案例里全部是**选择效应**。
本役的 R454 分解（组成 4.8pp / 档内 3.4pp）**只作描述**，其中**组成部分不可当作可达收益**。

### 8.2 12 房判定的最终版（含 R477 修正）

| 臂 | 看什么 | 通过条件 | 不通过 |
|---|---|---|---|
| `c135`（第二波） | 最优命中率 + 同档内 8巡听牌率 | 命中率 ≥95% **且** 听牌率 ≥基线+3.1pp | 停臂 |
| `c146`（本轮） | 组成位移 **且** 总听牌率 | 两者都过阈才保留 | 只动组成 ⇒ 停臂（R477 预测） |
| `c191`（本轮） | 组成位移 | 位移 ≥9.7pp(3房)/4.9pp(12房) | 未动 ⇒ 干预没生效 ⇒ 停臂 |
| `c193`/`c194`（本轮） | 干预脚印 | 脚印 ≥5% 才有判断价值 | 当前 +5.3pp/+1.8pp ⇒ **不建议延长到 150 房** |

### 9. ★★★候选供给重开（R479）：坏河跨两个时代 ⇒ 读河候选的旧判决不干净

- 实测：09-09~09-11 河"从不跨局重置"（比门户长 48~65 张）；09-12 起换成另一种错法；**两个时代都错**。
- `bot/` 里 **34 个策略读河**（基线 `speedtugc` 不读河）⇒ 历史上**读河候选被系统性削弱**，其"已排除"结论方向保守。
- ⚠ **2026-09-18 更正（R480）**：R479 用文件级 grep 判"读河"**是错的**（命中了文档字符串）。
  按**实际代码 + 继承**重做后，**纯 BC 模型 `c067`/`c068`/`c073w4` 并不读河**
  （`C067Policy._visible` 直接 `return None`；`C068Policy` 只换模型与候选区间、未覆盖 `_visible`）
  ⇒ **它们的"−19.8/房"旧判决不受河污染，不因河缺陷重审**。"重审 BC 模型"的建议**撤回**。
- **修正后的重审优先级**：
  1. **`c151/c152/c156/c159/c162` 等读河家族** —— 二测强场判决已由 R456 判定受两个执行 bug 污染 ⇒ **仍需重审**；
  2. `c068b/c076/c077/c089/c118/c121`（**BC + 可见特征的变体**，确实读河）—— 旧判决不干净，但当年多为 n=1~2 的早期筛选，价值有限；
  3. `c130` 不重审（独立死因 p99 33.6s）。
- **不重审**（有独立死因）：`speedc130`（p99 33.6s 超时）、`speedc117`/`speedc132`（用邻接代理而非真进张）。
- **纪律**：判"某策略是否用某输入"**不能用文件级 grep**——要看①是否在实际执行路径上；②**继承/覆盖**（父类 return None、子类未覆盖 ⇒ 实际不用）。
  跨文件继承会让 grep 名单**系统性偏大**（本次即由此误判）。

### 10. 第三波预登记（R481）

- **定位**：c151 家族的重审 = "**旧判决被污染、必须重判**"，**不是**"相信压对数有用"（其前提是跨截面对照，无自体内剂量反应）。
- **臂选择（延迟口径）**：优先 **`c156`**（p99 340ms / max 1992ms / **0 次 >3s**）或 **`c159`**（381ms / 2101ms / **0 次**）；
  **不直接用 `c151`**（p99 1115ms、8 次 >3s）。
- **可伪证预测**：修复后的房里，该臂的**拆对率**应高于基线（机制动）**且** 8 巡听牌率 ≥基线 +3.1pp；两者都过才保留。
- **量级参考**：二测 −149 可被当时的 13.88% 首动作代打按 −12 分/pp 整额解释（−167）⇒ 该判决证据强度很低。

### 11. ★12 房裁决的分数端点口径（R484）

- **必须用桌强调整后的净胜/房**（原始分会翻转排名）：本役修复后各臂桌强从 **−6.1 到 +57.2**（全轮 +22.6）。
- 工具：`tools/ab_readout.py`（按臂自动给"桌强调整后净胜/房"）+ `var/_arm_tablestrength.py`（按臂看桌强均值）。
- 阈值：见 §6（听牌率 ±3.1pp、无副露 ±4.9pp）；**分数端点不做 12 房裁决**（可辨 ±88 分/房）。
- 纪律：<30 房/臂的任何分数读数，**必须同时报桌强均值与调整后值**。

### 12. ★12 房裁决操作单（R485，到 12 房/臂时照此机械执行）

**Step 0 — 补拉（大 N，勿用小 N）**
```powershell
python -X utf8 tools/fetch_room_replays.py --since "2026-09-17 21:14:57" --rooms 60 --gap 0.8
```

**Step 1 — 三步完整性自检（任一不过就先修数据，不要读结论）**
```powershell
python -X utf8 tools/ab_integrity.py
python -X utf8 var/_portal_completeness.py      # 每房应恰好 10 局
python -X utf8 var/_arm_endpoints.py            # 各臂席局数 ÷ 80 = 房数
```

**Step 2 — 机制端点（主判据）**
```powershell
python -X utf8 var/_arm_endpoints.py
```
看三列：`无副露%`、`首次副露`、`8巡听牌%`（含分桶）。按 §6 阈值：听牌率 ±3.1pp、无副露 ±4.9pp。

**Step 3 — 分数端点（只作辅助，必须带桌强校正）**
```powershell
python -X utf8 tools/ab_readout.py
python -X utf8 var/_arm_tablestrength.py
```

**Step 4 — 执行层门禁（保证读数不是执行噪声）**
```powershell
# ⚠ 门禁一律按**当前代码版本的窗口**跑（切臂/改码后重设 since）。
#   用整役 since 会把 05:00 之前"跨局不清零"时代的房算进来 ⇒ river_fidelity 必然 FAIL（R504 实测）。
WINDOW="2026-09-18 05:00:00"      # ← 每次改码后改成新窗口起点
python -X utf8 tools/exec_leak_check.py --since "$WINDOW"
python -X utf8 tools/river_fidelity.py  --since "$WINDOW"
python -X utf8 tools/melds_fidelity.py  --since "$WINDOW"
```
阈值：新局首动作 ≤12%、整体弃牌代打 ≤1.0%、河缺口 ≤1.5 张、副露差 ≥−1.0 张。

**Step 5 — 按 §8.2 判定表裁决**（逐臂：保留 / 停臂 / 不延长）

---

### 13. 第二波设计说明（R485）：两臂交替**天然规避** R484 的桌强问题

- R484 显示 **5 臂固定轮转**在短窗口内会出现 **−6 ~ +57** 的桌强差异（相位效应：每个臂固定在 ~75 分钟周期里的同一个相位）。
- **第二波改为两臂交替**（`speedtugc` vs `speedc135`）⇒ 相邻房直接对比，**桌强按构造均衡**，
  且配对差分的方差远低于跨臂均值差 ⇒ 同样的房数下功效更高。
- 这是**支持"第二波只用两臂"的第三个理由**（前两个：R477 组成轴撤下、R450 速度轴证据最强）。

### 14. 第二波门禁状态（R486）

| 门禁 | 状态 | 证据 |
|---|---|---|
| **延迟（M=10）** | ✅ **已闭合** | 09-17 同族 12,814 次 draw 决策：**0 次 >3s**、p99 ≤ 389ms、max ≤ 2100ms（c151 的 8 次 >3s 全在去重前的 09-16） |
| 预热名单 | ✅ 已含 c135/c136/c146 | `HEAVY_WARMUP`（R468） |
| 规则 guard | 待正式赛 token | T-24h 流程 |
| 全量回归 | ✅ 689 项 OK | 最近一次 |

⚠ 若正式赛 M>10，须按实际 M 复跑并发压测（本门禁只覆盖 M=10）。

### 15. 执行层收口总表（R488）

| 通道 | 修复前 | 修复后 | 判定 |
|---|---|---|---|
| 新局首动作代打 | 48.05% | **2.67%** | ✅ |
| 整体弃牌代打 | 2.40% | **0.57%** | ✅ |
| 弃牌河缺口 | 少1.1~4.4/多6.0~15.1 | **少1.11/多0.00** | ✅ |
| 四家副露缺口 | — | **−0.43 张** | ✅ |
| 吃/碰 409 | 3.15~3.65% | **0.64~1.38%**（残存=合法竞争） | ✅ |
| 响应时序（1s 窗） | — | **p99 29~56ms，0 超时** | ✅ |
| 丢胡（代打弃牌里本可胡） | — | **1/5879** | ✅ |
| 放铳率 | 18.57% | 18.74%（本就低于 top32 19.30%） | 持平 |

⇒ **执行层已无已知缺陷**；之后只做门禁复核。剩余分数差距必须靠**策略机制**（第二/三波）。

### 16. ★探索/产出排期纪律（R489）

- 事实：账号一次一房（E002）⇒ **探索与产出严格二选一**；本役 35 房净 **−802（−22.9/房）**，其中 2/5 房花在已判"不可判"的臂上。
- **纪律**：
  1. **探索段尽量短**：两臂交替、到机制阈值（听牌率 ±3.1pp / 分布 ±4.9pp）即停，**不追分数显著**；
  2. **产出段**：一旦有胜者，**立刻设为生产**（keeper）并跑 ≥1 天，再进下一个探索；
  3. 每役开始前先写清"这一段要多少房、判什么、什么时候停"。

### 17. ★裁决双尺子（R490）

- 比赛**晋级看名次分/first%**，阶梯看**累计原始分**；两把尺子可能背离 ⇒ 裁决必须**同时读**：
  `ab_readout.py` 的「桌强调整后净胜/房」+「第一名率 / top2率」。
- 判定：净胜↑且first%↑ ⇒ 优先保留；净胜↑但first%↓ ⇒ 阶梯可用、比赛慎用；净胜平但first%↑ ⇒ 比赛优先；两者都↓ ⇒ 停臂。
- **迁移风险**：阶梯 Rounds=8、正式赛 Rounds=16 ⇒ 赛前用真实赛事第一阶段实测校验一次。

### 18. 备选实验设计（R491）：自建房同桌配对 A/B（M/Rounds 可指定）

| 用途 | 工具 | 理由 |
|---|---|---|
| **强场判决** | 阶梯轮转 A/B（真实池） | 唯一"真对手"口径 |
| **机制快速筛选** | `room_ctl.py` + `l2_super.py` 自建房同桌 | 同配置（可 Rounds=16）、**配对**、功效高 |
| **决胜** | 自建房同桌 @Rounds=16 | 当阶梯判决含糊时，用配对 16 轮房拆"机制没用"vs"轮数不匹配" |

⚠ **自建房的对手是自博弈**（M 席都是我们的 token）⇒ 本项目已证自博弈对手强度会翻转结论（E021）⇒ **只作筛选/决胜，不作强场判决**。
⚠ 账号一次只跑一件事（E002）⇒ 与阶梯 A/B **互斥排期**，不能并行。

---

### 19. ★第二波**锁定**与切换操作单（R501/R502，2026-09-18 11:0x）

**背景（两条新证据，都指向同一结论）**
1. R501：残余首动作代打是**每局第一次弃牌的确定性缺口**（tugc 4.46% / c146 7.50% / c193 8.50%），
   且**与算力无关**（c135 冷启动 60 个真实局面：第 1 次 12ms、p95 32ms、max 36ms）。
2. R502：修复后的非听牌段**最优弃牌命中率**我方 **72~76%**、top32 **92%**（`DEA_SINCE=2026-09-18 01:44`，2480 局）
   ⇒ 这 16pp 是**目标函数差距**（基线不看 real_ukeire），**不是输入差距** ⇒ **c135 的上限比本轮任何臂都大**。

**锁定（不再依赖本轮 12 房结果）**

| 项 | 值 |
|---|---|
| 臂集 | **`speedtugc`（基线）vs `speedc135`（单变量）** |
| rooms | 1 |
| bundles | **不写**（单变量 ⇒ t≥1.96 / 100 房/臂） |
| 机制终点（可早看） | ① `discard_eff_audit` 该臂**最优命中率**（预测 ≫76%，向 92% 靠）② 8 巡听牌率 ≥ 基线 **+3.1pp** |
| 分数/名次终点 | ≥100 房/臂；双尺子（§17）：桌强调整净胜/房 **且** first% |
| **反向风险判据** | 若"最优命中率↑"但"tenpai/first 不动或下降" ⇒ 记**指标自证**，**不上生产**（R502 ③） |

**切换操作单（只在"无 run_bot 且无 match_super"的空档执行）**
```powershell
# 0) 12 房裁决（§12）——只在有空档时跑；若本轮四臂已全部触发 §6/§8.2 停臂条件，
#    裁决结论照写，不追分数显著。
python -X utf8 tools/fetch_room_replays.py --since "2026-09-17 21:14:57" --rooms 60 --gap 0.8
python -X utf8 tools/ab_integrity.py            # ↑ 补拉可以整役；**门禁不要用整役 since**（见 Step 4）
python -X utf8 var/_portal_completeness.py
python -X utf8 var/_arm_endpoints.py

# 1) 上线前门禁（必须在无对局的空档跑；§14 已闭合，这里是复验）
python -X utf8 var/_concurrency_bench.py speedc135 --threads 10 --per-thread 100 --warm 0
python -X utf8 var/_concurrency_bench.py speedc135 --threads 10 --per-thread 100 --warm 0 --kind window
python -X utf8 tools/rules_guard.py --strategy speedc135
python -X utf8 -m unittest discover -s tests

# 2) 停本轮 → 起第二波（**基线必须写在第 1 位**）
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/ab_ctl.py start speedtugc,speedc135 1
```

**★第二波"转生产"操作单（只有机制+听牌率双门槛都过才执行）**

门槛：① `var/_hitrate_live.py` / `discard_eff_audit.py` 该臂最优命中率 **≥95%**（实测已 99.1% ✅）
      ② `var/_arm_endpoints.py` 8 巡听牌率 **≥ 基线 +3.1pp**
      ③ first%（`tools/ab_readout.py`）**不降**
      若 ① 过而 ② 不动/下降 ⇒ 记"指标自证"，**不上生产**（R502③/R509）。

```powershell
# 0) 确认没有对局在跑（绝不能在对局中动手）
python -X utf8 tools/ab_ctl.py stop          # 会等驱动自然退出；退出时把基线写回策略文件
# 1) 把生产策略指向已过门槛的臂（必须显式写，因为 stop 会把文件恢复成基线）
"speedc135" | Out-File -Encoding utf8 var\_keeper_strategy.txt -NoNewline
# 2) 让 watchdog 按其策略拉起 keeper（≤120s；keeper 自带锁，幂等）
#    确认：Get-Content var\_keeper_strategy.txt  /  Get-Process（_keeper.py + match_super.py）
# 3) 生产段纪律：跑 ≥1 天再进下一个探索；期间每次心跳报
#    var/_hitrate_live.py（该臂命中率）、tools/exec_leak_check.py --since <生产段起点>、tools/river_fidelity.py
```

**第二波时间线（本役执行记录，用于门禁窗口口径）**

| 时刻 | 事件 |
|---|---|
| 11:33:11 | 第一波干净停止（空档内，未误拉新批） |
| 11:34:29 | 第二波启动 `speedtugc` vs `speedc135`；首房 11:34:50（**旧代码**） |
| **11:46** | **R508 落地**：`bot/protocol.py` 报名期状态轮询 15s → **1s**（修掉"M=10 同时开局时庄家首弃全被代打"）；689 项回归 OK |
| ~11:55 起 | 后续房全部带 R508 ⇒ **首动作代打应≈0**；门禁/读数窗口从 **11:55** 起算 |

⚠ 纪律：`tools/ab_ctl.py` **没有 --help**（任何第二参数都会执行）；
`stop` 的默认等待是 20 分钟（对局中会等到自然结束，**绝不杀 run_bot**）。

---

### 20. ★本役收口（2026-09-19，R559~R570）—— 方向地图 · 流水线 · 四条新纪律

**一、已在生产**
| 项 | 值 |
|---|---|
| 生产策略 | **`speedc152`**（= c135 真进张排序 + 分档对数奖励），keeper 只读锁定 |
| 上线依据 | 和了率配对 **+0.66pp**（与 c135 的 +0.84pp 噪声内等价）**且** 平均对数 2.33→**1.72**（价值腿机制在动） |
| 复核 | **100 房后**看：分/房 ≤ **−15** 或 **爆头占胡无改善** ⇒ 回退 speedtugc（`var/_prod_c152_monitor.py`） |

**二、方向地图（本役全部证据的收口）**
| 方向 | 状态 | 关键证据 |
|---|---|---|
| 执行层（首弃/河/409） | **✅ 已收口** | 首动作代打 48%→**0.00%**；河缺口 1.2/0.00；409 0.64~1.38% |
| 速度轴（一步真进张） | **✅ 已兑现** | c135：条件听牌 **+4.6pp** / 和了 **+0.84pp** ≈ **+12 分/房**；命中率 98.1%（优于 top32 的 94%） |
| 副露率（多吃碰） | **❌ 关闭** | c146 标定到 top32 同幅（4.29% vs 4.34%）**无收益** |
| 待ち质量 / 转化率 | **❌ 不是缺口** | 活等张我们**更好**（13.39 vs 13.13）；转化率仅差 1.5pp |
| 二步前瞻 | **❌ 无效** | 唯一真正跑到过的二阶项（11.8% 分歧），配对和了率无增益 |
| 朴素 rollout 搜索（C063） | **❌ 关闭（延迟）** | M=10 draw 19/1000 超 3s、max 12.8s |
| **价值轴（爆头占胡 +4.6pp ≈ +16 分/房）** | **⚠ 缺口真实、但三次都拿不到** | ① 规则/形状分解（分歧 0~1.4%）② 通用弃牌加权（−21 分/房 > +16 上限）③ 可胡点追加弃胡 c208（依据跨截面 + selfev n=1 为负 + 脚印 2.9% 太疏） |
| 学习型值函数 | **✅ 已造出** | 爆头可达性 V：**局级 AUC 0.788**、分位 0.9%→20%（去掉错位特征后还升了） |
| **下一步唯一在案的入场券** | **把 selfev 做密** | 需要覆盖 **50~100 个"弃胡"点**、纳入有副露的局；先量出"弃胡后仍和的比例"，再决定这条轴生死 |

**三、流水线（不越 §16 纪律）**
```
c152 生产跑 100 房（≈1 天） → 复核（分/房 · 爆头占胡 · 执行门禁）
   ├─ 通过 ⇒ 以 c152 为新基线；下一波**先做密 selfev**（价值腿入场券），再谈臂
   └─ 不通过 ⇒ 回退 speedtugc，并把结论写进方向地图
```

**四、本役新增的四条纪律（都用真金白银换来的）**
1. **分歧率前置**：任何"改并列键/加权/带宽"的候选，先报**与基线的选择分歧率**；<10% 一律算"**未跑到**"，不许写成"机制无效"。（R545：c200 1.1%、c201 2.6%、c203 5.0% 全属空转）
2. **同口径复算**：凡是"训练用 A 口径、推理用 B 口径"的模型接入，必须先做一次同口径复算。（R568/R569：`draw_idx` 训练真实、推理写死 0 ⇒ 去掉该特征后 AUC **0.771→0.788**）
3. **门禁按当前代码版本窗口跑**：用整役窗口做门禁会把历史代码版本的债算到当前版本上。（R504：整役 river_fidelity FAIL、按版本窗口 OK）
4. **模仿式差异必须先过自体内检验**：跨截面的行为差异（"强者这样打"）在本项目已翻转 5 次（§9.11/§9.21/§9.25/§9.34 + C039）；**行为对齐不算赢**。（R567/R570 据此否掉 c208）

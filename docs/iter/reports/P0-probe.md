# P0 真机规则语义探针报告

> 日期：2026-09-07 | 状态：进行中（补杠场景观察中）
> 房/会话：过胡房 t_e3aa16c92e37（已毕，11 局）；杠房 t_23b68d70ae8f（进行中）

## 1. 暗杠（自摸 4 张同码）——已触发，关键发现

**事件序列（probe_gang 座 zhuque，11:26:58，gid=t_23b68d70ae8f_r9_b0_t0）：**
```
提交: {'action': 'gang', 'tile': '1t'} phase=draw turn=0   ← 服务器未 409 拒绝
手牌形态暂不一致 len=11 expect=10 e=1 g=1 —— 跳过本轮     ← 客户端守卫
（后续多轮持续形态不一致，跳过决策至终局；本席 -8）
```

**结论：**
1. **服务器接受 draw 回合的暗杠提交**（无 INVALID_ACTION）——真机协议支持自杠；
2. **杠后服务器补牌语义与 game.py 长度模型不匹配** → 客户端卡守卫到终局。
   **已修复（T5b, 12acca8）**：实测事件流（var/replays/p0g）确认杠补牌以
   `tile_drawn data.gang_replenish=true` 送达、快照长度 = expect+1；game.py
   现按该形态退补牌回 13-3e-g 后正常决策（"手牌形态暂不一致"不再卡死）。
3. 白板禁杠未观测到（4 白场景罕见）——以 sim 规则与服务器无 409 为据暂从 sim 口径。

## 2. 补杠（碰组 + 第 4 张）——已触发（增强探针：窗口无条件碰）

110 局增强探针（房 t_5d9ac5a1942d，probe 两席共 ~190 碰）累计：**杠提交 24 次
（含补杠 15 例 + 暗杠样本）**，全部被服务器接受，行为与 §1 同构（补牌 → 长度
形态 → T5b 修复覆盖）。原始事件流 112 份存档 var/replays/p0g/。

## 3. 杠上花/连杠路径——未单独触发（依赖 1 的自杠流程打通后才有意义；T5b 后待 SpeedH 冒烟观察）

## 4. 过胡（胡形弃牌）——已实测：**服务器允许**

**证据（过胡房 t_e3aa16c92e37，11 局）：**
- probe_hupass 座（胡形必弃）：zhuque 0 胜、xuanwu 0 胜（各场多为 -1/-8/-2）；
- 对照 speedE 座（可胡即胡）：qinglong 3/11 胜、baihu 3/11 胜（~27%，与引擎
  胡率口径一致）；
- 探针座胡形弃牌均被接受（无 409 风暴、11 局完整结算）→ **服务器不强制自摸胡，
  允许过胡**。推论：打点/等番类候选（v2 范围）规则上可行。

## 5. 结论 → P1 修订点

- [ ] **修订（来自 §1）**：SpeedH 需先在 game.py/meldtrack 增加"自杠流程"适配
      （提交 gang 后的服务器事件处理与长度模型修正）——新子任务 T5b，P1 范围
      调整为 协议适配 + 门控函数 + decide 集成；
- [x] 过胡合法 → v2 打点候选的合法性前提成立（登记，后续候选可用）；
- [ ] 补杠/杠上花语义待杠房数据回填后补本节；
- [ ] sim 杠语义（v2 不补牌）与真机（补牌）差异已实证 → SpeedH 强度判据
      绝不可用 sim，只走真机同桌/窗口 A/B。

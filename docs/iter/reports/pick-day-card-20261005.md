# 10/5 选臂提案 · **当天操作单**（一屏版）

> 今天 09:00 你的最终臂被提名；**真正写入是 10/7 08:30 的自动确认**。完整口径见 **§V.66/§V.67**、**§V.161**、**§V.279**；本卡只列你要做的事。

## 1. 自动发生的

- `HangzhouMajFinalPickProposal` @ 10/5 09:00 ⇒ 写 `var/_final_pick_proposal.txt`。
  内容：强手房分/房主序列（附 SE 与 2×SE）、强手房两层否决预检、**逐臂可部署性**、两半 Pareto（真破平依据）、若 B2 存在则附旁证窗口。
- 10/7 08:30 `HangzhouMajFinalArmConfirm`：**只叠已判正的层** → 自动写 `var/.final_arm.txt`（人工已写 ⇒ no-op、绝不抢）。

## 2. 你要做的（一句话）

**读 `var/_final_pick_proposal.txt`，然后三选一**：

| 情形 | 你该做什么 |
|---|---|
| 提案的结论你认同 | **什么都不用做** —— 10/7 08:30 会自动写 `.final_arm.txt` |
| 你想**改选另一根已判正**的臂 | `echo <arm> > var/.final_arm.txt`（人工裁决优先，自动步骤不会覆盖） |
| 你想用**未判正**的臂（如 B2） | **不行**（§V.161 A.1：没过阈值的不进候选池、不能事后挑）—— 它只能在提案里作**旁证** |

## 3. 读提案时先看这四处

1. **`可部署性`** 段：只有 `✅ 已判正` 的臂能进最终臂；
2. **`前两名` + `|z|`**：若报“不可区分”，**不等于没差别**（MDE≈ 41 分）⇒ 必须看两半 Pareto；
3. **强手房两层**：正式赛对应的就是这两行（且层内 < 15 房/臂 只记录、不翻转）；
4. **回退口径** 行（若有）：`.ab_mode` 不在时窗口是“最近 12 天”，**跨多个役次**，不可当单一役的判词。

## 4. 红线

- **绝不手改** `var/_verdict_*.txt`；不把未判正的臂写进 `.final_arm.txt`；
- 不为了“多一层”去改判词口径。

## 5. 与 10/7 的衔接

今天之后，下一个动作就是 **10/7 08:30 定臂 → 09:00 换臂**（操作卡：`final-switch-card-20261007.md`）；
12:00 还有一次换臂重试（若判词在 09:00 之后才落地）。

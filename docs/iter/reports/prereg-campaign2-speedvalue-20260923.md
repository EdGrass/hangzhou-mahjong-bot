# 役 2 预登记（2026-09-23）：`speedvalue`（出牌并列取舍 / 拟合 V）

> 状态：**候选已备好、等役 1 门禁采用后起役**。本文件与计划 §A/§B/§P、手册 §7/§7.6 配套。
> 纪律：**判据在起役前一次性登记，役中不改**；本役同时应用**零行为变更**的 `age_ms` 插桩。

## 1. 臂

| 角色 | 臂 | 说明 |
|---|---|---|
| 基线 | **今晚门禁采用的臂**（预期 `speedc151`） | 若门禁判"未达"仍采用 c151（其两端口径与台子证据均优于 tugc），并在 journal 注明 |
| 候选 | `speedvalue`（`bot.speedvalue.SpeedValue`） | 唯一差别：弃牌改按**拟合 V**（听牌处"活等张 + 番"线性权衡，去掉从未标定的 `ROUTE_BONUS` 对子惩罚）；副露/杠/财飘 100% 继承 |

## 2. 判据（预登记）

| 档 | 端点 | 阈值 | 说明 |
|---|---|---|---|
| **主** | 复盘**和牌率/房** | **z ≥ 1.50**，各臂 **≥80 房有复盘**，且**覆盖 ≥70%** | `var/_gate2.py`；缺任一 ⇒ REFUSE（攒房/补复盘） |
| 副 | 复盘**番/房** | z ≥ 1.50 且**与主同号** | 同工具 |
| 护栏 | ledger **第一率** | 候选 ≥ 基线 | `tools/_first_rate_readout.py` 口径 |
| 护栏 | ledger 净分/房 | **同号**（不作阻塞） | 冻结端点只作旁证（其 MDE 太大，见 §P） |
| 熔断 | `var/_breaker_watch.py` | 两档不触发 | — |
| **机制** | 对子档案（`--mechanism pairs`） | **上升**（≥3 对占比 ↑，目标 ~33.5%）且和牌率不降 | `var/_gate2.py --mechanism pairs` |
| **机制（工程）** | 窗口捕获率 / `age_ms` | **记录基线**（捕获率现 78.0%；`age_ms` p50 预期 ≈1.2s） | 本役**只插桩不启用**修法，保持数据干净 |

## 3. 事前预测

- **方向**：`speedvalue` > 基线（台子 p9 胜局轴 t=−3.57；p3 −1.00；混池 −2.85）；
- **量级（主端点单位）**：台子和牌率效应 **+0.64 ~ +2.23pp**（p3/p9），合并 ≈ **+1.44pp**；台子绝对和牌率 ≈ 真机（24-26% vs 24.7%）⇒ **无水平偏移**；
  迁移灵敏度 `k_hu` **未知**，本役将首次测出（供后续役次推算）；若 `k_hu≈1` ⇒ 真机 ≈ **+1.4pp**，z=1.5 约需 **90 房/臂**；
- **机制预测（副）**：对子档案 **2.25 → ~2.31**（≥3 对 27.8% → 33.5%）——**与顶级方向一致但不越界**（顶级 2.48，见 §L）；
- **风险**：若对数**暴涨到 ≥2.7（tugc 水平）** ⇒ 说明解除惩罚过头，下一役改用部分惩罚档（`SpeedValuePair2/4`）。

## 4. 判词分支（与计划 §B 一致）

- **B1 z≥1.50** ⇒ 采用 `speedvalue`；**役 3 = 杠束 + 启用混合 `/state`**；
- **B2 |z|<1.50** ⇒ **不追加同轴改动**；仍按役 3 推进（杠/捕获率的证据来自真机而非台子）；
- **B3 z≤−1.50** ⇒ 回退基线；把 `speedvalue` 拆成"去对子惩罚 / 听牌重排"两项分别上真机定位。

## 5. 起役清单（手册 §7 + §7.6）

```powershell
# ① 停役 1（等当前房自然结束）
python -X utf8 tools/ab_ctl.py stop
# ② 注册 + 冒烟 + M=10（机器空闲才会执行；否则 REFUSE）
python -X utf8 var/_prep_next_campaign.py --arm speedvalue --module bot.speedvalue --class SpeedValue
# ③ 本役附加：应用零行为变更的 age_ms 插桩（幂等 + fail-closed）
python -X utf8 var/_prep_window_instrument.py
# ④ 起役 2
python -X utf8 tools/ab_ctl.py start <门禁采用的臂> speedvalue 1
# ⑤ 起役后 3 点核对 + 记录 age_ms 基线
python -X utf8 var/_window_age_report.py          # 期望：带 rnd/age_ms 的房数开始增长
```

## 6. 留档要求

- 本文件（预登记）+ `docs/iter/journal.md` 的起役/判词条目；
- 判词时的 `_gate2.py` 输出（两端口径 + 机制）；
- 采用动作的 `var/_ab_adopt_log.jsonl` 记录；
- `age_ms`/捕获率基线数值（供役 3 的修法对比）。


---

## 7. 修订（2026-09-23 08:4x）：客户端时代的混杂说明

本役 03:13:44 起役，期间客户端经历两个**全局**改动（对两臂同时生效）：

| 时段 | 客户端状态 | 影响 |
|---|---|---|
| 03:13–04:00 | 原版（每批都取快照） | 基线行为 |
| 04:00–08:01 | **R1092/R1098 回归**：跳过 pass/timeout 批次快照 ⇒ 看不到延迟登记的窗口 | 吃实现 -50%（4.36→2.2/file）、`response_chi` 决策行 -31%（49.5→34.0） |
| **08:01 起** | **R1113 修复**：`window_watch=6`（弃牌后 6 批必取快照） | 决策行 45.6/file、吃/file 3.7（回到基线逐房区间 3.7–5.0） |

**判词纪律**：
1. 本役主判据是**臂间差**（speedvalue − speedc151），两臂在同一时段、同一客户端下交替排批 ⇒ **差值仍然有效**（回归与修复对两臂同时生效）；
2. 但**绝对水平**（净胜/房、和牌率/房）不代表修复后配置，判词须注明"含 4 小时回归期"；
3. **二次读数**：`var/_gate2.py --since "2026-09-23 08:01:00"` 作为"干净客户端子集"的旁证（样本小、只作方向性参考，不作阻塞判据）；
4. 若本役判"平"，不得据此否定 speedvalue——须在**干净客户端**下重跑同轴对照。

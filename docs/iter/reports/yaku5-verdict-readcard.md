# 役 5 判词读卡（**叠层验证**：基线 = 役 4 的两层赢家 ｜ 候选 = 三层组合）

> **先决条件**：只在 §V.186 的"**meld ✓ 且 BC ✓ 且 V ✓**"那一格才起役。其余情形**不起役 5**，直接进 §V.66 选臂。
> 预登记：`prereg-campaign8-combo-20260925.md`（臂名、剂量口径在**起役前**填死）。
> 口径：`since = var/.ab_mode.started`；覆盖 ≥70%；各臂 ≥80 房起判；役盒 120 房/臂（到盒按 §V.66 破平，**不延长**）。

## 0. 这一役回答的是"**10/7 部署哪一个**"，不是"这一层有没有用"

役 3/役 4 各自回答"某一层有没有用"；役 5 只回答：**把已判正的层叠起来，是不是真的比最优单/双层更好**。

| 角色 | 臂 | 说明 |
|---|---|---|
| 基线 B | **役 4 的两层赢家**（例：`speedvaluebcmeldp45`） | `_bsegment` 起役时写进 `.ab_mode.bundles[0]` |
| 候选 C | `speedvaluebcvmeld` **或** 剂量对齐 `speedvaluebcvmeldp40` | **两根都已注册**；选哪根 = 起役前要填死的 `<…>` |

⚠ 组合臂是"多变量" ⇒ 按预登记走 **bundle 声明（|t|≥1.50）**，起役命令必须显式带 `--bundles=<B>`。

## 1. ★ 主端点不是 `_gate2` 打印的那个（最容易读错的一处）

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| **主** | **同席强手分/房** | **非劣于基线**（非劣 = 不显著劣） | `var/_pick_arm.py --since "<ts>" --min-rooms 30 --strong-top 32` |
| 副 | `_gate2` **和牌率/房 + 番/房** | **z≥1.50 且同向** | `var/_gate2.py --mechanism none --min-rooms 80` |

**⚠ 读法**：`var/_gate2.py` 把"和牌率/房"印成"主端点"——那是**本役的副端点之一**（通用模板的叫法）。
本役的**主端点判据是"强手房分/房非劣"**，必须用 `_pick_arm.py --strong-top 32` 读，**不能只看 `_gate2` 的第一行**。

## 2. 护栏

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| 护栏 1 | ledger **第一率** | 候选 ≥ 基线 | `_gate2` 内置 / `var/_first_rate_readout.py` |
| 护栏 2 | **三层机制各层在带内** | 新加层拉带；基线已含同层只看 `action=0` | `var/_mech_watch.py`（每 6h，自动跑并落 `.mech_warn`） |
| **否决** | 强手房 **两层**（≥1 名 / ≥2 名 top32）的「分/房」与「第1率」 | **任一层任一列 ≤ −2×SE合并 ⇒ 不采用** | `var/_strong_veto.py --since "<ts>" --baseline <B> --candidate <C>` |

- 某层 < 15 房/臂 ⇒ 该层**只记录、不据此翻转**；两层都不足 ⇒ UNKNOWN，**不阻塞**（按主/副端点结论走）。
- 分层原始读数：`var/_verdict_by_elite.py --since "<ts>" --arms <B>,<C>`（含 `强手房>=2` 行）。

## 3. 决定规则（**campaign8 §4 逐字**：这就是 10/7 部署哪根臂的规则）

1. 组合**不劣**（主端点非劣 + 副同向 + 护栏过）⇒ **10/7 部署组合臂**（"最屌"）；
2. 组合**显著劣** ⇒ 部署**最优单/双层**；
3. **不可判定** ⇒ 按 §V.66 破平序列（强手房分/房 → 两半 Pareto → 第1率 → 最近判词 → 基线）；
4. **任何情况下不部署"从未在任何役里判正过"的臂**。

## 4. 与 10/5–10/7 换臂链的衔接

| 时刻 | 动作 | 产物 |
|---|---|---|
| 役 5 判词落地 | `_verdict_watch` 落 `_verdict_役5<候选>.txt` + 哨兵（决定性/到盒） | 判词文件 |
| 10/5 09:00 | `HangzhouMajFinalPickProposal` → `var/_final_pick_proposal.py` | 选臂提案（只读台账，**只给读数与建议**） |
| 10/7 08:30 | `HangzhouMajFinalArmConfirm` → `var/_final_arm_confirm.py --go` | **自动写** `var/.final_arm.txt`（无已判正新层 ⇒ 保持基线；不赌未验证组合臂） |
| 10/7 09:00 | `HangzhouMajFinalSwitch` → `var/_switch_final.py --go` | 真换臂 + 关熔断（`.rate_guard_off`） |
| 10/7 10:30 / 12:00 | `_final_ready_check.py` / `_final_switch_retry.ps1` | 就绪复核 + 一次重试 |

**写死**：`.final_arm.txt` 是**故障关闭**的——文件不在，`_switch_final.py` 拒绝执行（宁可不换也不猜）。

## 5. 纪律

判据/阈值/役盒起役前一次性登记、役中不改；不为追 z 延长役盒；**强手房否决优先于主端点**；
**主端点用 `_pick_arm.py --strong-top 32` 读，不要拿 `_gate2` 的第一行当主端点**。

# Oracle v2 裁决卡 —— 09-04 正式赛复盘决策级重放（P3b 收尾，2026-09-08）

> 语料：var/replays/t_dee58824c308/（10 场 × 16 局 = 160 局，我方 = 蔡宇欣座，
> 对手 = 9 名真实玩家 —— 唯一真异构决策语料）。
> 工具：tools/replay_oracle.py（服务器复盘事件流重建我方每摸牌决策局面 →
> run_bot 各策略重决策 + 配对分歧 + 进度代理；状态机自检：弃牌必在手、
> 局终手牌长度合法、hu 与顶层 rounds 对照、fan-calc 合法性）。

## 数据质量（自检全部通过）

| 项 | 值 |
|---|---|
| 文件/局/决策点 | 10 场 / 160 局 / **1937 个我方摸牌决策** |
| 自检失败(跳局) | **0**（无弃牌不在手/长度违例/截断局） |
| 局终手牌合法 | 160/160 |
| 我方实际 hu | 25（顶层 rounds winner==我 25 —— **完全一致**） |
| hu 合法性（fan-calc） | 25/25；爆头 hu **0** |

## 结果

**1) 候选 vs E（同一真机局面的策略对差异 = 执行面宽度）**

| 候选 | 差异率 | 次数/1937 | 说明 |
|---|---|---|---|
| speedu（K1） | 3.4% | 66 | 全部 discard tie 分歧 |
| speedk（K1+K2） | 3.4% | 66 | draw 面与 speedu 同构（K2 只在窗口）——自洽 ✓ |
| speedv（river-aware） | 5.3% | 102 | 最大执行面 |
| speedp（财飘） | **0.0%** | **0** | 真人对局 1937 决策 **0 触发** |

**2) 频率裁决（打点/链族，真异构数据钉死）**
- 实盘 hu 25 次中爆头 hu = 0；各策略重放 1937 决策中弃白合计仅 2 次（0.1%，
  且无一次处于爆头态）→ **财飘入口在真实对局中的频率 ≈ 0**（与 sim 0/240、
  e4a 真机 0/36 一致）。C008/speedP 维持 paused：机制正确但入口频率为 0。

**3) 分歧的代理代价（实盘旧 bot vs E，1230 异选）**
- 弃后仍听：实际 51 次 vs E 56 次；两者都听的 49 对平均活等待 3.35 vs 3.61
  → E 在 ukeire 口径略优于 09-04 旧 bot（与旧 bot 0-胡 bug 期背景一致，方向自洽）。

## 结论（平台收敛性，真异构决策证据）

1. 现代 E 与 09-04 旧 bot 的全部行为差异集中在**弃牌 tie 层**（hu 点 25/25
   完全一致）——历代微调候选（K1/K2/river）同样只在此层面分歧（0-5.3%）；
2. 微调族分歧幅度（≤5.3%）与历史真机收敛结论一致：预期 |δ|<1/场，
   **低于真机可判线（σ≈9-14 下 δ=2 需 n≈85-190 场）** → 平衡设计复核价值
   有限（判定成本 > 期望收益）；
3. 大效应候选面（打点/链/财飘）入口频率 = 0（1937 决策 0 触发 + 爆头 hu
   0/25）→ 现行规则集（无 YouCaiBiKao）下该族关闭；
4. **平台收敛性结论成立**：SpeedE 在现规则集已近饱和；后续投入方向 =
   协议/稳定性域（已 v24 对齐）或等 YouCaiBiKao=true 类新规则环境。

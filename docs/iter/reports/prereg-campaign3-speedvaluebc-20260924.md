# 役 3 预登记（2026-09-24）：出牌质量 = `SpeedValue` + **BC 学到的排序**（`speedvaluebc`）

> 判据**起役前一次性登记**，役中不改。本文件取代作废的 `prereg-campaign3-meldpost-20260923.md`（R1179/R1186）。
>
> **⚠ 役次顺序（2026-09-24 05:1x，R1202 更正）**：本役**仍为役 3（先打）**。
> R1201 曾据"弃牌牌效审计：单步最优命中率已追平 top32"把本役降为役 4，**该降级理由已被 R1202 撤回**：
> 共同起点分析显示——**在同一中盘状态（同向听、同巡）下，我们到 k7 的听牌率仍差 −6.7pp（s0=1）/ −5.2pp（s0=2）**
> ⇒ 差距在**单步 ukeire 指标看不到的地方**（"同为最小向听"时的**并列取舍**正是本臂 15.9%/中盘 18.0% 的作用面）。
> 判据/阈值**不变**；预期仍按原文档（不预测正效应、最可能灰区），但**机制理由恢复成立**。

## 0. 目标缺口（为什么打这一轴）

| 证据 | 内容 |
|---|---|
| `var/_tenpai_gap_by_meld.py --by-start`（R1177，8997 房） | **听牌率缺口是真棋力缺口**：起手向听 1/2/3+ 分别 **−3.54 / −3.15 / −4.78pp**，且档位构成与 top32 几乎相同（3+ 占 80.0% vs 80.1%）⇒ 不是选择效应 |
| 同上（R1176/R1184 生态） | 缺口在**门清/通用听牌速度**上，不在副露层 ⇒ 正确的轴是**弃牌质量** |
| `tools/offline_replay`（R1185，保真口径） | **`speedvalue` → `speedvaluebc` 足迹 15.9%（3,415/21,424）、action 差异 0、异常 0** ⇒ 宽足迹 + 真单变量 |
| 历史（R698，**负结果但 n 不足**） | BC 弃牌族真机点估计曾 **+1.5/房（30 房）** 与 **−19.8/房（20 房）** ⇒ R250 判"**未决**"而非排除；CI 半宽 ±49~88 |

**机制假设**：`SpeedValue` 用**手写小特征 V**（活进张/番/白板）挑"弃后最小向听"里的张；
`BC` 是在**同一候选集**上用**行为克隆网络**（154 维：候选状态 + 动作 + 与基线之差）重排 ⇒
在"手写 V 说不清"的并列里可能更接近顶级选择。**只替换排序，不碰任何其它层。**

## 1. 臂（单变量证据）

| 项 | 内容 |
|---|---|
| 类 | `bot/speedvaluebc.SpeedValueBC(SpeedValue)`（**只新增文件**） |
| MRO | `SpeedValueBC → SpeedValue → SpeedC151 → SpeedC150 → …`（**同一族底**） |
| 唯一分叉点 | 只覆盖 `_pick_discard`（`SpeedTUGC.decide` 调它的那一处）；胡/自杠/抓打圈/副露窗口 **100% 沿用** |
| 双闸门 | ① 基线所选必须落在"弃后**最小向听**组"内；② 模型分数须**严格高过 `margin`** |
| 兜底 | 模型缺失 / 局面不符 / 任何异常 ⇒ **零行为变化** |
| 模型 | `var/c073_orig_w2_net.pt`（**起役前必须断言存在**，否则本臂会退化成 no-op ⇒ 白跑） |
| 就绪 | 注册 `run_bot.STRATEGY_FACTORIES["speedvaluebc"]` ✓；`arm_smoke` 四档 ✓（p50 11.9ms / max 166ms）；契约测试 5/5 ✓ |

## 2. 判据（预登记，役中不改）

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| **主** | 复盘**和牌率/房** | **z ≥ 1.50**，各臂 ≥80 房有复盘、**覆盖 ≥70%** | `var/_gate2.py` |
| 副 | 复盘**番/房** | z ≥ 1.50 且与主**同号** | 同上 |
| **机制（本役核心）** | **决策改动率**（BC 是否真在改牌） | **改动率 ∈ [10%, 20%]**（保真足迹 15.9% 的合理带）且 **action 差异 = 0** | `tools/offline_replay.py`（每 20 房复跑一次，抽样即可） |
| 护栏 1 | ledger **第一率** | 候选 ≥ 基线 | `var/_first_rate_readout.py` |
| 护栏 2 | ledger **净分/房** | 同号（不阻塞） | `tools/ab_readout.py` |
| 熔断 | `var/_breaker_watch.py` | 两档不触发 | — |

## 3. 事前预测（先写下来，避免事后解释）

- **机制**：决策改动率应稳定在 **~15.9%**（若 ≪10% ⇒ 说明模型未加载或闸门把改动吃掉了 ⇒ 走 B3 回查）；
- **主端点**：**不预测正效应**。理由：BC 的历史真机点估计正负并存；
- **量级（若走正向）**：和牌率 **+0.5 ~ +2.0pp**、番/房 同号略正或持平；
- **最可能的结果**：主端点落在 **z ∈ [1.0, 2.0] 的灰区**（80 房/臂的检出力有限），机制读数会是干净的；
- **延迟**：+1.2ms p50（已实测），**不构成风险**。

## 4. 判词分支

- **B1**：主端点 z ≥ 1.50 且机制达标 ⇒ **采用**，把"BC 排序"写入后续所有弃牌臂的底；
- **B2**：机制达标但主端点 z 不达（含灰区正向）⇒ **记录为"机制成立、主端点未证实"**；
  按 §V.16 的队列推进（役 4 = `speedvaluemeld`），**不重复投 BC 的剂量**；
- **B3**：机制**不达标**（改动率 ≪10% 或 action 差异 > 0）⇒ 本役**作废**并回查实现
  （模型是否加载、闸门是否过严、离线/真机视图是否又缺字段）。

## 5. 起役清单

```powershell
# 0) 起役前置断言：模型必须在场（否则本臂 = no-op，整役白跑）
python -X utf8 -c "import run_bot; p=run_bot.STRATEGY_FACTORIES['speedvaluebc'](); assert p.model is not None, 'BC 模型缺失！'; print('model ok', p.model_path)"
python -X utf8 tools/ab_ctl.py stop                                  # 等当前房自然结束
python -X utf8 tools/arm_smoke.py --class bot.speedvaluebc.SpeedValueBC --kind draw --n 400
python -X utf8 tools/ab_ctl.py start speedvalue,speedvaluebc 1 --bundles=speedvalue "--started=<切换时刻>"
```

注：`--bundles=` 与 `--started=` **必须显式传**（R1156：漏 `--bundles` 会把阈值从 1.50 悄悄改成 1.96）。

## 6. 已知风险

1. **BC 历史真机为负且 n 不足**（R250 判"未决"）⇒ 本役就是它的**第一次有检出力**的判决；
2. **模型文件依赖**：`var/c073_orig_w2_net.pt` 缺失 ⇒ 臂静默退化（已用 §5 的断言挡住）；
3. **离线足迹用的是 dec 语料**（历史策略产生的局面），不是本役自己的局面 ⇒ 机制端点按 §2 在役中**重抽样复核**。

---

## 7. 追加护栏（2026-09-24 09:1x 起役前预登记，R1287）：**强手房一致性**

**为什么**：正式赛（决赛）的对手密度 ≈ **强手房**（top32 在场），而 `_gate2` 主端点是**混合口径**。
⇒ 本役除主端点外，**必须**额外报**分层读数**：
`python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>`
（① **强手房 分/房** ② **强手房 第1率**），以及同席机制口径
`python -X utf8 var/_lowprio_run.py -- python -X utf8 var/_seat_h2h.py --since "<役起点>" --by-arm --top 32`
（**听牌率 / 赢分/轮**，与 `hu_gap_split --by-arm` 互补）。

**判定规则（写死）**
1. 主端点 z ≥ 1.50 且 **强手房两列均不劣于基线** ⇒ **采用**；
2. 主端点 z ≥ 1.50 但 **强手房两列任一显著劣于基线** ⇒ 记 **B2（主端点成立但强手房未确认）**：
   **不作为正式赛选臂**（正式赛几乎全是强手房），继续按役序测下一轴；
3. 强手房样本不足（< 15 房/臂）⇒ 只记录，**不据此翻转**主端点结论。

**口径注**：本节是**起役前追加**（非役中改阈值）；主端点与 z 阈值**一概不变**。
（役 2 的判词仍按它自己的原预登记口径，不适用本节。）

**「显著劣于基线」的钉死定义（R1288）**：强手房分/房的 `候选 − 基线` **< −2×SE合并**
（`SE合并 = sqrt(SE_候选² + SE_基线²)`，各自用强手房子样本的 `SD/√n`）⇒ 判为显著劣化；
若任一侧强手房 < 15 房 ⇒ **不判**（只记录）。本节与 §V.66 的噪声口径一致。

---

## 8. 追加读数列（2026-09-24 09:2x 起役前预登记，R1297）：**决赛相似层（≥2 名 top32）**

**为什么**：实测我方 `分/房` 随**同桌 top32 人数**单调下降 ——
0 个：**+61.0**（n=28）→ 1 个：**−19.4**（n=37）→ 2 个：**−55.6**（n=22）→ **3 个：−95.1（n=8）**；
第 1 率同向（28.6% → 21.6% → 18.2% → **0.0%**）。
而 9/23 三测的**赛事累计**是 `rank=7 / total_score=-277 / games_played=160`（≈ **−92~−138 分/房**，按 2~3 房折算）
⇒ 与我们"3 个 top32"层（−95）**同量级**。
⚠ **勘误（R1298）**：那句"决赛 −138.5/房"的**归因是错的** —— 决赛阶段（21:49–21:55）只有重复的"已确认出席本阶段"，
`status=closed` 时**并没有打到房**（最后一条"本场结束"是 21:45:07，属前一阶段）
⇒ 该数字是**赛事累计**，不是"那一场决赛"的难度。
本节的依据是**难度曲线本身**（+61 → −19 → −56 → −95），与决赛数字无关。

**新增读数（必报，不替代主端点）**
```powershell
# 在 §V.70 的强手房（≥1）之外，再报"≥2 名 top32"层：房数 / 分/房 / 第1率
python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>     # 现有工具（分层列）
```
判定：若该层**样本 ≥ 15 房/臂**，且 `候选 − 基线 < −2×SE合并` ⇒ **按 §V.70 的 B2 处理**（不采用进正式赛）；
样本 < 15 房/臂 ⇒ **只记录**（当前 3 强手层只有 ~5% 的房，约 4 房/臂/役 ⇒ 不可判，必须靠 ≥2 层与 强手房 层叠起来看）。

**参考量级**：本役 95 房里，0/1/2/3 名强手的分布是 **28/37/22/8** ⇒ ≥2 强手占 **31.6%** ⇒ 80 房/臂时可预期 ~25 房/臂，**足够进入判定**。


# 会话交接 — 2026-09-23（覆盖 R1000~R1026）

## 0. 红线与环境（不变）

- 权威目录 **`D:\hangzhouMaj`**；Python 一律 `python -X utf8`；重活走 `_lowprio`。
- 战役期间**只新增文件**，不改 `bot/` 既有文件、不改阈值/护栏/熔断。
- **战役进程是 `pythonw.exe`**（不是 `python.exe`）；用 `CommandLine` 关键字过滤，
  且**过滤会匹配到执行命令的 shell 自身** ⇒ 停进程前必须人工确认 PID→角色（R1003）。
- **只停自建后台批次，绝不碰** `run_bot`/`match_super`/`_ab_driver`/`_watchdog`。
- 临时补丁脚本用完改 no-op（本会话共 neutralise 32 个）。

## 1. 当前战役（`speedtugc` vs `speedc151`）

| 项 | 值 |
|---|---|
| 房数 | **107 / 107**（本役 started 2026-09-20 14:54:07） |
| 净分 | **b−a = +47.5 净胜/房，t=1.81**（波动区间 46~54） |
| 第一率 | 16.8% vs 15.0%（桌强调整后略低） |
| 完整性 | 混臂 0 / 非完成 0（唯一"非 finished"是**正在打的房**） |
| 100 房门禁 | **provisional + noninf 双档通过**、第一率护栏通过（R993） |
| 150 房终点 | **ETA 2026-09-23 ~20:00–21:40**；到点跑 `var/_gate_report.py` |

## 2. 下役候选：`speedvalue`（**门禁全过**，只剩注册与真机 A/B）

- 定义：`bot/speedvalue.py` —— 弃牌主判据 = **拟合 V(state)**（听牌处按"活等张 + 番"**线性**权衡，1 番 ≈ 20.5 张活等张；去掉从未标定的 `ROUTE_BONUS` 对子惩罚）。
- **证据**：自对弈台（**双重标定**）三批方向一致，合并 **n≈2400**，分数轴 t≈**−2.5**、胜局轴 t≈**−3.7**。
- **双重标定**：① 已知效应 `c151−tugc` 真机 +39.75 vs 台子 +81 ⇒ **k=0.491**（R1008）；② **绝对水平** c151 台子 −0.15 vs 真机 −0.525（t=1.10，R1020）。
- **投影**：+2.14/席·8局 ⇒ 真机 **≈ +11 原始分/房**（下役水平 **+7**）。
- **门禁**：`tests/test_speedvalue.py` 7/7；M=10 分歧 **0.00%**；冷态 p99 **196ms**/max 511ms；三档冒烟 n=100 全过（R1021）。
- **T-6h**：`var/_prep_next_campaign_speedvalue.py`（**fail-closed** 护栏 + 命令形状已预检）。

## 3. 本会话新增的"可复用知识"

### 3.1 游戏结构（改变问题形状）
- **只能自摸**（54,581 局**无 `hu` 事件**，分数形态全为自摸赔付；荒庄 789 局/1.4%）⇒ **防守/危险牌无意义**（R996）。
- **番是乘积**：`分支 × 2^链 × (4白×2) × (爆头×2)`，但 **`chain` 只在"活到胡牌"时算**（普通出牌断链）⇒ "每杠翻倍"是**误解**（R1006）。
- **分支倍率只有七对族**（门清）≠1，**平胡占赢局 97%**（R1011）。
- 番值差距的真实载体：**爆头**（我方 23.5% vs 顶级 27.9% 的赢局）+ **链**（0.71% vs 2.90%）（R1011）。

### 3.2 差距归因
- 胡率 −4.2pp 的分解：**听牌率 −6.44pp**（主导）+ 听牌后兑现 −1.64pp（R976）。
- 中盘落后的归因：**同副露数下我们并不慢**；差距是"太少进入副露状态"（巡8 0 副露 47.9% vs 34.2%）（R980）。
- 按开局白板数：0 白 −3.5pp / **1 白 −4.8pp（相对差最大）** / 2 白 −2.3pp / **3 白 +4.4pp（我们更好）**（R1010）。

### 3.3 **已封死的方向**（不要再占实验位）
| 方向 | 结论 | 出处 |
|---|---|---|
| 前瞻家族（s1/s2/anytime/TUGC） | deadline 静默失效 + 对 c151 ≈0 + 对 speedstack **更差** | R973/R975/R1001 |
| 放松副露门槛（tol） | **显著负**（z=−4.51） | R977 |
| `god_meld` 开关 / c131 | **无效**（爆头率反而更低） | R1012 |
| 爆头路径项 / 爆头窄副露规则 | **结构性惰性**（0.3% / 0.38%@5× / 0.09%） | R1014 |
| 最宽搭子（c230） | **零足迹**（0/29,348 窗口） | R1023 |
| 爆头分档路线加成（c152） | **更差**（胜局轴 t≈+1.9） | R1011/R1012 |
| 危险牌/防守类 | 本作无点炮 | R996 |

### 3.4 仍在跑/待判
- **积极杠**（`SpeedGangTakeFixed` = speedvalue + 多要明杠 + P0 杠修复）：机制已判定为**节奏**（胡率 +1.5pp、听牌率 +1.1pp、**番/胡不变**，R1025）；但**两个基座方向不一致**（c151 基座更好、speedvalue 基座略差）⇒ 待 p10/p11/p14 跑满。
- **`SpeedGangTakeSmart`**（只在该杠不抬高向听时吃）：足迹 **31% 的明杠窗口**，p17 判决中。
- **`speedstack`**（R962 合成臂）：对 c151 胜局轴 t=−1.85（800 局），对 speedvalue 目前 speedvalue 更好。

### 3.5 仪器纪律（重要）
- **自对弈台**是唯一通过双重标定的仪表；**主端点 = 逐局胜局数差**（比分数轴省 ~2× 样本，R990）。
- **复现必须按批拆开**看（`var/_batch_wint.py`）；`var/_arena_pergame2.py` 已打印两端均值防符号误读。
- **回放式 rollout 已降级**（对已知效应只复现 1/3）。
- **合并臂/新臂先量离线足迹再占批次**（R1023）；足迹 <1% 低于仪表分辨率。
- `parse_combo` 用 `x` 当重复分隔符 ⇒ **臂名不能含 `x`**（arena 里用别名，R1019）。
- `dec` 日志**不记录"已持杠"**（`m` 只有吃/碰），该口径须查门户复盘（R1016）。

## 4. 关键工具（本会话新增/常用）

| 用途 | 路径 |
|---|---|
| 自对弈台（运行时注册，不改既有文件） | `var/_arena_duel.py` |
| 两两配对统计（含两端均值） | `var/_arena_pergame.py` / `_arena_pergame2.py` |
| 逐批异质性（防"假复现"） | `var/_batch_wint.py` |
| N 臂同台排名 | `var/_arena_nway.py` |
| 台子绝对水平标定 | `var/_arena_abs_calib.py` |
| 迁移投影（k=0.491） | `var/_transfer_projection.py` |
| 机制/足迹探针 | `var/_gang_mechanism.py`、`_gangsmart_footprint.py`、`_svc230_footprint.py`、`_baotou_path.py`、`_single_white.py`、`_fan_detail_mix.py` |
| T-6h 上线 | `var/_prep_register_speedvalue.py` + `var/_prep_next_campaign_speedvalue.py` |
| 月度计划 | `docs/iter/reports/next-month-plan-20260923.md` |

## 5. 下一步（按序）

1. **150 房终点门禁** → 采用 c151；
2. 执行 T-6h 一条龙（注册 `speedvalue` + 三档冒烟 + M=10）；
3. **役 2：`speedc151` vs `speedvalue`**（150 房/臂；事前预测 **+11 原始分/房**）；
4. 役 2 判决后按 `next-month-plan-20260923.md` 的 **B1/B2/B3 分支**推进；
5. 待跑的批次（p9/p10/p11/p13/p14/p15/p17）跑满后，为**役 3**定序候选。

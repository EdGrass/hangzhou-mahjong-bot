# C001 报告卡 —— speedx1「tie 内弃刚摸优先」

> 日期：2026-09-06 | 状态：**L1 WIN → L2 待验（挂起等 HM_PORTAL_COOKIE）**
> 协议：docs/自迭代方案.md | 代码：bot/speedx1.py + tests/test_speedx1.py + arena 注册

## 假设

PROJECT.md §7.1 / 复盘差异#2：正式赛 TOP 玩家在同向听 tie 里倾向**打刚摸的新张**，
SpeedE 倾向**保留刚摸**。若"弃新张"在信息/牌河层面更优，应体现为场分收益。
改动 = SpeedE 弃牌 tie-break 末键翻转（唯一差异，其余继承）。

## 语义验证

- tests/test_speedx1.py：确定性 fixture（发/东 三键 tie → E 弃刚摸、X1 弃他牌）+ 随机
  40 手性质断言（差异 ⇔ E 弃刚摸 且 三键 (s, wcnt, 孤字) 全 tie）；0 反例。
- 探测：随机 400 手 ×63 处差异 100% 符合语义。
- 全量回归：80 tests OK（128s）；stability：speedx1 0 违规/守恒 PASS。

## L1 证据（同桌面 speedx1x2+speedEx2，8 巡本地）

| 项 | 值 |
|---|---|
| 批次（seed, delta） | 11: −0.8 / 12: +6.0 / 13: +3.0 / 14: −1.6 / 15: +3.8 / 16: +2.6 |
| history 口径（6 批 1200 局） | delta **+2.17/场**，σ=32.6（实测）→ 95%CI **[+0.33, +4.02]** |
| game-level（1387 局，含 seed1 partial 187） | delta **+2.32/场**，σ_d=32.7，SE=0.88 → CI **[+0.60, +4.05]** |
| 胡率 | speedx1 25.2% vs speedE 23.6%（6 批中 5 批 x1 更高） |
| 流局率 | ~2-3%（无口径偏移） |

命令：`python -m arena.runner --combo "speedx1x2+speedEx2" --games 200 --rounds 8 --seed {11..16}`
→ `python tools/ab_analyze.py speedx1x2` / `python tools/ab_gate.py --combo "speedx1x2+speedEx2" --sigma 32.6`

## 裁决（L1）

**WIN**：delta≈+2.2 ≥ δ_min=2.0 且 CI 排除 0 → **送 L2 真机同桌**。
谨慎项：CI 下界薄（+0.33~+0.60）；批间摆动大（batch sd≈3.0 > 期望 2.3）；
样本已超校准量（~1387 > ~1030）。L2 至少 2 房各 ≥200 局，跨时段重复才可确认。

## 教训（给后续候选）

1. **σ=10 旧假设作废**：本地 8 巡同桌面 σ_d≈33（tools/ab_analyze.py 实测）；
   δ=2/场 需 ~1030 局、δ=1 需 ~4100（放弃）。ab_gate 必须带实测 --sigma。
2. 批间波动提示：若只有 2 批 400 局就下结论会反复横跳（本候选 400 局时 CI 含 0、
   987 局时仍贴边，1387 局才稳定排除 0）——**L1 起步就按 σ_d 实测规划 N**。
3. 方向机制待证：x1 胡率稳定高 1.5-2pp，但场分差只在边界——弃新张可能提升
   自摸效率但引入放铳风险（真机才有对手防守差异）。

## L2 计划（待 cookie）

room_ctl create --m 1 --rounds 8 → 令牌 4 枚：speedx1×2 + speedE×2，keep_alive 托管，
≥2 房 × ≥200 局；判定同 L1 口径（σ 真机实测）。结果回填本节。

# C002 报告卡 —— speedx2「听牌等待扣已见」

> 日期：2026-09-06 | 状态：**L1 DRAW → dead-end（不上 L2）**
> 协议：docs/自迭代方案.md | 代码：bot/speedx2.py + mahjong/sim.py river + tests/test_speedx2.py

## 假设

PROJECT.md §7.5 / 理论缺口 #1（原 SpeedG git 3c269fd，当年与 F 一并被清、未获独立
判定）：waits 只扣手牌不扣牌河 → 死等（单钓北但河已见 3）仍计 1 等待。C002 在
SpeedE 上做最小改动：s==0 听牌的等待计数按「剩余 = 4 − 手牌 − view.river」修正，
河见满 4 剔除；空河完全等价 SpeedE（含孤字/保留刚摸 tie 序不变）。
配套基建：sim 注入 river（与真机 game.py 同口径，后续 C004 防守维度亦依赖）。

## 语义验证

- tests/test_speedx2.py ×3：空河等价（随机 80+ 场景）／确定性死等 fixture
  （7t 手 2+河 2 → E 弃 8t / x2 弃 7t）／随机构造 400 场景性质断言
  （分歧 ⇒ 同档、x2 活等待 ≥ E、E 候选含死等且 raw 优先）；发现并修正了
  "live 必严格更大"的错误断言（raw 差异同 live 也可分歧）。
- 结构化生成（赢形−1=必听）效率 ~11.5% 命中/次（初版随机探测超时作废）。
- 全量 83 tests OK；stability：speedx2 0 违规/守恒 PASS。

## L1 证据（同桌面 speedx2x2+speedEx2，8 巡本地，6 批并行）

| 项 | 值 |
|---|---|
| 批 delta（seed 21-26） | −3.22 / +0.82 / +1.66 / −0.60 / +1.96 / +0.46 |
| game-level（1200 局） | delta **+0.186/场**，σ_d=31.8，SE=0.92 → 95%CI **[−1.61, +1.99]** |
| 胡率 | speedx2 ~24% vs speedE ~24%（无系统性差异） |

命令：6× `python -m arena.runner --combo "speedx2x2+speedEx2" --games 200 --rounds 8 --seed {21..26}`
→ `python tools/ab_analyze.py speedx2x2`

## 裁决（L1）

**DRAW**：delta≈+0.2 ≪ δ_min=2.0，CI 大幅含 0 → 统计不可分 → **不上 L2**。
与历史 SpeedF（真机 408 局不可分）、SpeedG（未测即清）汇成同一结论族：
**纯评估精度类改进（更精的 waits/ukeire 口径）在本基准上无可测收益**——
多轮收敛于平台，符合 PROJECT.md §1 "收敛于平台" 的判读。

## 教训（给后续候选）

1. "计算更正确" ≠ "打得更好"：死等场景在真实对局占比太低，tie 修正无法
   改变场分分布（对照 C001 的行为类差异有 +2.2 可测效应）。
2. 同族防重：waits/ukeire 口径再精化的候选（扣副露已见/扣他家弃牌概率化等）
   应直接标记低预期，除非先改变评估对手或规则环境。
3. **sim 的 river 注入保留**（C004 防守维度需要公开弃牌河）；speedx2 代码
   留档待清理轮（与 F/G 同批清理时删除）。

## 附：C001 L2 进行中（见 reports/C001.md 尾节）

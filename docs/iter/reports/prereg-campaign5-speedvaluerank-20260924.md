# 役 5 预登记（2026-09-24）：中盘并列取舍 = `SpeedValue` + **81 维教师候选排序器**（`speedvaluerank`）

> 判据**起役前一次性登记**，役中不改。前置：役 3（`speedvaluebc`）与役 4（`speedvaluemeld`）之后。
> 本文件是 **R1202 定位（缺口在"并列取舍"层）** 的第二个直接打击点。

## 0. 目标缺口（为什么打这一轴）

| 证据 | 内容 |
|---|---|
| R1202（共同起点，400 房 + 本役 77 房） | **同一中盘状态（同向听、同巡）下，我们到 k7 的听牌率仍差 −5~−7pp**（s0=1: 58.0% vs top32 64.7%；本役房内 −7.4~−9.3pp）⇒ 缺口**不是**存活者筛选 |
| R1201（弃牌牌效审计，当前语料） | 我们的**单步最优命中率已追平 top32**（门清 89.8% vs 91.9%、有副露 94.3% vs 95.5%，打坏率更低）⇒ 差距**在单步指标看不到的地方** |
| R1206 / R1208（足迹体检） | `speedc152 → speedc215`（并列层换 81 维教师排序器）**23.3%**；把同一排序器接到现役基线 ⇒ `speedvalue → speedvaluerank` **37.0%（7,922/21,412）、action 差异 0、异常 0** |
| `bot/speedc215.py` 自述 | 动机原文即"**起手 2~3 向听的中等手牌能否越过听牌线**"⇒ 与实测缺口同靶 |

**机制假设**：我们与顶级的差别不在"选没选到最小向听/最大进张"，而在**同一最小向听组内的并列取舍**
（哪张留着对**后续**更有利）。教师排序器（81 维：弃后状态 + 牌 onehot + 巡目/河长/副露/杠）学的正是**"顶级玩家会弃哪张"**。

## 1. 臂（单变量证据）

| 项 | 内容 |
|---|---|
| 类 | `bot/speedvaluerank.SpeedValueRank(SpeedValue)`（**只新增文件**；R1207） |
| MRO | `SpeedValueRank → SpeedValue → SpeedC151 → SpeedC150 → …`（**同一族底**） |
| 唯一分叉点 | 只覆盖 `_pick_discard`；胡/自杠/抓打圈/副露窗口 **100% 沿用** |
| 双闸门 | ① 基线所选必须落在"**弃后最小向听组**"内；② 排序器分数须**严格高过**基线（`margin` 默认 0.0） |
| 复用 | 直接调用 `bot/c089rank.Ranker` 与 `cand_row`（与 `speedc215` **同源**，不复制特征逻辑、不改既有文件） |
| 模型 | `var/c089_ranker_net.pt`（**起役前必须断言存在**，否则臂退化成 no-op） |
| 就绪 | 注册 ✓；契约测试 **5/5** ✓；`arm_smoke` draw **p50 15.0ms / max 217.5ms** ✓ |

## 2. 判据（预登记，役中不改）

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| **主** | 复盘**和牌率/房** | **z ≥ 1.50**，各臂 ≥80 房有复盘、覆盖 ≥70% | `var/_gate2.py` |
| 副 | 复盘**番/房** | z ≥ 1.50 且与主**同号** | 同上 |
| **机制 1** | **决策改动率** | **∈ [10%, 50%]**（保真足迹 37.0%）且 **action 差异 = 0** | `var/_mech_gate.py` |
| **机制 2（本役新增）** | **同状态推进率**（s0=1，k4→k7） | **候选 ≥ 基线**（同房同巡） | `var/_advance_from_common_origin.py --by-arm` |
| 护栏 1 | ledger **第一率** | 候选 ≥ 基线 | `var/_first_rate_readout.py` |
| 护栏 2 | ledger **净分/房** | 同号（不阻塞） | `tools/ab_readout.py` |
| 熔断 | `var/_breaker_watch.py` | 两档不触发 | — |

## 3. 事前预测（先写下来）

- **机制 1**：改动率应在 **~37%**（若 ≪10% ⇒ 模型未加载/闸门吃掉了改动 ⇒ 走 B3 回查）；
- **机制 2**：候选的 s0=1 推进率**应上升**（这是本轴存在的理由）；**若不升 ⇒ 说明"并列取舍"不是缺口所在**，
  这是本役**最有价值的信息**（会把 §V.22 的定位推翻，方向应转向"非弃牌动作"如副露/听牌形态）；
- **主端点**：**不预测正效应**。族底（c152/c211 一系）的历史真机读数偏负，但**从未在现役基线上干净测过**；
- **量级（若走正向）**：和牌率 +0.5~+1.5pp、净分/房 +10~30；
- **最可能结果**：机制读数干净；主端点灰区（z ∈ [1.0, 2.0]）。

## 4. 判词分支

- **B1**：主端点 z ≥ 1.50 且机制 1/2 达标 ⇒ **采用**（并把"教师排序器"写入后续臂的并列层底）；
- **B2**：机制达标（含推进率上升）但主端点不达 ⇒ **记录"机制成立、主端点未证实"**；
  后续继续在"并列取舍"层加深（例如**更大/更新的教师数据集重训排序器**），不再换轴；
- **B3**：机制 1 不达标（改动率 ≪10%）⇒ 作废并回查实现；
  **机制 2 不达标（推进率没升）⇒ 把"缺口在并列取舍"这条定位撤回**，转向非弃牌动作。

## 5. 起役清单

```powershell
# 0) 模型在场断言（缺文件 ⇒ 臂 = no-op ⇒ 整役白跑）
python -X utf8 -c "import run_bot; p=run_bot.STRATEGY_FACTORIES['speedvaluerank'](); assert p.ranker is not None, '排序器缺失！'; print('ranker ok')"
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/arm_smoke.py --class bot.speedvaluerank.SpeedValueRank --kind draw --n 400
python -X utf8 tools/ab_ctl.py start <基线>,speedvaluerank 1 --bundles=<基线> "--started=<切换时刻>"
# 或直接：python -X utf8 var/_switch_campaign.py --baseline <基线> --candidate speedvaluerank --go
```

## 6. 已知风险

1. **族底历史偏负**：c215/c217 的祖先读数（c135 −0.418、c152 −0.356、c211 −0.312 分/席局）都来自**旧窗口与旧基线**，
   本役是它们在**现役基线**上的第一次干净对照；
2. **改动面宽（37%）**：下行有界（只在最小向听组内 + 严格更优 + 不动 action），但**不是零风险**；
3. **训练分布**：排序器学的是"教师候选排序"，若教师本身偏保守/激进，臂会继承该偏差（机制 2 会先暴露这一点）。

---

## 7. 追加护栏（2026-09-24 09:1x 起役前预登记，R1287）：**强手房一致性**

**为什么**：正式赛（决赛）的对手密度 ≈ **强手房**（top32 在场），而 `_gate2` 主端点是**混合口径**。
⇒ 本役除主端点外，**必须**额外报**分层读数**：
`python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>`
（① **强手房 分/房** ② **强手房 第1率**），以及同席机制口径
`python -X utf8 var/_lowprio_run.py -- python -X utf8 var/_seat_h2h.py --since "<役起点>" --by-arm --top 32`
（**听牌率 / 赢分/轮**，与 `hu_gap_split --by-arm` 互补）。

**判定规则（写死）**
1. 主端点 z ≥ 1.50 且 **强手房两列均不劣于基线** ⇒ **采用**；
2. 主端点 z ≥ 1.50 但 **强手房两列任一显著劣于基线** ⇒ 记 **B2（主端点成立但强手房未确认）**：
   **不作为正式赛选臂**（正式赛几乎全是强手房），继续按役序测下一轴；
3. 强手房样本不足（< 15 房/臂）⇒ 只记录，**不据此翻转**主端点结论。

**口径注**：本节是**起役前追加**（非役中改阈值）；主端点与 z 阈值**一概不变**。
（役 2 的判词仍按它自己的原预登记口径，不适用本节。）

**「显著劣于基线」的钉死定义（R1288）**：强手房分/房的 `候选 − 基线` **< −2×SE合并**
（`SE合并 = sqrt(SE_候选² + SE_基线²)`，各自用强手房子样本的 `SD/√n`）⇒ 判为显著劣化；
若任一侧强手房 < 15 房 ⇒ **不判**（只记录）。本节与 §V.66 的噪声口径一致。

---

## 8. 追加读数列（2026-09-24 09:2x 起役前预登记，R1297）：**决赛相似层（≥2 名 top32）**

**为什么**：实测我方 `分/房` 随**同桌 top32 人数**单调下降 ——
0 个：**+61.0**（n=28）→ 1 个：**−19.4**（n=37）→ 2 个：**−55.6**（n=22）→ **3 个：−95.1（n=8）**；
第 1 率同向（28.6% → 21.6% → 18.2% → **0.0%**）。
而 9/23 三测的**赛事累计**是 `rank=7 / total_score=-277 / games_played=160`（≈ **−92~−138 分/房**，按 2~3 房折算）
⇒ 与我们"3 个 top32"层（−95）**同量级**。
⚠ **勘误（R1298）**：那句"决赛 −138.5/房"的**归因是错的** —— 决赛阶段（21:49–21:55）只有重复的"已确认出席本阶段"，
`status=closed` 时**并没有打到房**（最后一条"本场结束"是 21:45:07，属前一阶段）
⇒ 该数字是**赛事累计**，不是"那一场决赛"的难度。
本节的依据是**难度曲线本身**（+61 → −19 → −56 → −95），与决赛数字无关。

**新增读数（必报，不替代主端点）**
```powershell
# 在 §V.70 的强手房（≥1）之外，再报"≥2 名 top32"层：房数 / 分/房 / 第1率
python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>     # 现有工具（分层列）
```
判定：若该层**样本 ≥ 15 房/臂**，且 `候选 − 基线 < −2×SE合并` ⇒ **按 §V.70 的 B2 处理**（不采用进正式赛）；
样本 < 15 房/臂 ⇒ **只记录**（当前 3 强手层只有 ~5% 的房，约 4 房/臂/役 ⇒ 不可判，必须靠 ≥2 层与 强手房 层叠起来看）。

**参考量级**：本役 95 房里，0/1/2/3 名强手的分布是 **28/37/22/8** ⇒ ≥2 强手占 **31.6%** ⇒ 80 房/臂时可预期 ~25 房/臂，**足够进入判定**。


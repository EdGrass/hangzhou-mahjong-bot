# 就绪度快照（2026-09-23 10:3x 刷新；权威一页版）

> 上游文档：`next-month-plan-20260923.md`（§W 役次队列）、`competition-runbook-20260923.md`（比赛当天）、`journal.md`（R1–R1132 全留档）。

## 1. 一句话状态

**客户端已定终态；主线在役 2（`speedc151 vs speedvalue`）攒房**（11 vs 11 房，目标各 >=80）；
**比赛当天的链路已逐项验证**（preflight READY / YCBK 双分支 / official 切换 DryRun 零副作用）。

## 2. 客户端终态（勿再改）

| 项 | 状态 |
|---|---|
| `window_watch=6`（弃牌后 6 批必取快照） | ✅ 生效（窗口捕获：`response_chi` 决策行 45.6/file ≈ 基线 49.5） |
| R1095a（吃碰杠后从本地河移除该牌） | ✅ 生效（河保真：扣吃碰口径 0.90 少 / 0.00 多，门禁 <=1.5） |
| R1096（响应 409 不整体重同步） | ✅ 生效（事件连续性；代价 ~4% 调用） |
| hybrid `/state` | ❌ 已否决并关闭（登记竞态 ⇒ p50 744→3768ms） |
| 缓存窗口决策 | ❌ 已否决并关闭（吃相位不可推导 ⇒ 91/73 条 409） |
| `calls_per_event` | 0.65-0.67 |

**已知天花板（不要再投入）**：客户端侧"省 /state 调用"（两次实测：换不来捕获、换不来延迟；延迟主因在服务端/送达侧）。

## 3. 役 2（进行中）

| 口径 | 效应 |
|---|---|
| 非配对（主，`_gate2`） | **+3.38pp**（z=1.39） |
| 配对（相邻 A/B 房） | +3.07pp（t=1.27） |
| 对手强度校正 | +2.74pp（t=1.18） |
| 机制（>=3 对占比） | +9.94pp（z=2.66 PASS） |
| 覆盖率 | 100%（11/11 两臂） |
| 半样本 | 前半 +7.5pp / 后半 -0.4pp（噪声；房间 sd=5.56pp、配对不降 SE） |

**判据（预登记）**：各 >=80 房 + z>=1.50（主）且原始/配对/校正三读数同号；异号 ⇒ 未定。

## 4. 役次队列

| 序 | 役 | 状态 |
|---|---|---|
| 3 | 杠束 `speedgangtakefixed` | 预登记 + 换役彩排（副本）通过 |
| 4 | 对子剂量档（6 档） | import + 冒烟通过 |
| 5+ | 组合臂/再动一档 | 待役 3/4 读数 |

## 5. 比赛当天链路（已逐项验证）

| 步骤 | 证据 |
|---|---|
| `tools/preflight.py` | **READY**（v34 一致 / fan-calc 6/6 / 引擎单测 / run_bot 单实例 / 决策 p99=47ms / 自愈链 Ready） |
| `tools/rules_guard.py` | 闸门在位；true ⇒ 用 `*YCBK` 双胞胎（4 个均可用，GOD_MELD=False）；rc=3 ⇒ fail-closed |
| `var/_switch_to_official.ps1 -DryRun` | **零副作用**（未写 `.official_mode`、未改 `.official_spec.json`、keeper 不变） |
| `_exit_official.py` | 在位 |
| 单测 | `test_official_mode.py` 10/10；`test_needs_snapshot` 13/13；`test_faststate` 7/7 |

## 6. 运维

磁盘 D: 余量 **3050 GB**；`var/replays` 4.3 GB/30k 文件；watchdog 与 fetch-loop 健在（判进程必须按 argv 基名精确匹配）。

## 7. 剩余工作与风险

1. 役 2 攒房至各 >=80（约 ~35 小时排批）→ 判词 → 采用/继续；
2. 役 3 杠束 → 役 4 剂量档（按读数定档）；
3. **赛前 7 天**：重跑 preflight + M=10 bench + `rules_guard`（换本场新令牌）+ 整链 official 实跑（需停当前役）；
4. 残留风险：窗口送达延迟（服务端侧，不可控）造成的 20-30% 高价值窗口丢失；已记录为"不可在本仓库解决"。

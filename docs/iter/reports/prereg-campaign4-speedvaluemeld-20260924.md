# 役 4 预登记（2026-09-24）：选择性副露 = `SpeedValue` + 学习副露网（`speedvaluemeld`）

> 判据**起役前一次性登记**，役中不改。
>
> **⚠ 役次顺序（2026-09-24 05:1x，R1202 更正）**：本役**为役 4（后打）**——R1201 的"提前到役 3"已被更正：
> 共同起点分析（R1202）证明中盘推进缺口是 **−5~−7pp**，而本轴按量化预测只能贡献 **+0.5~0.9pp 听牌率**（量级小一档），
> 且 BC 的作用面（中盘改动率 18.0%）与缺口**直接重叠** ⇒ **先打 BC（役 3）**。**判据/阈值不变。**

## 0. 目标缺口（为什么打这一轴）

| 证据 | 内容 |
|---|---|
| `var/_tenpai_gap_by_meld.py`（R1176，8997 房） | **副露构成差**：顶级 **28.0%** 的席位局处在 2+ 副露态，我们只有 **17.3%**；且顶级"有副露局"占比显著更高 |
| `var/_foot_sweep.py`（R1189，window 阶段） | `c151 → c156`：索取率 **67.6% → 74.7%**（仅候选收 294 / 仅基线收 0）⇒ 现存"只加不减"的选择性副露层 |
| `tools/offline_replay`（R1190，保真口径） | `speedvalue` → **`speedvaluemeld`**：**draw 层 0.0%**（21,424 决策 100% 一致）、**window 层索取率 67.6% → 74.7%**、仅候选收 294 / 仅基线收 0、异常 0 |

**机制假设**：我们"把多少局推进到有副露那一档"太少；而 `c151` 的质量门控偏严。
本臂在**基线说不要**的窗口上，用**学习到的副露网**（141 维窗口特征、阈值 0.60）补一部分"要" ——
**绝不否决**基线任何一次索取（单向增加、下行有界）。

## 1. 臂（单变量证据）

| 项 | 内容 |
|---|---|
| 类 | `bot/speedvaluemeld.SpeedValueMeld(SpeedValue)`（**只新增文件**） |
| MRO | `SpeedValueMeld → SpeedValue → SpeedC151 → …`（**同一族底**；`super()._want_claim` 与 c156 的基线门控同为 `SpeedC144`） |
| 唯一分叉点 | 只覆盖 `_want_claim`；`_pick_discard` 仍解析到 `SpeedValue`（**draw 层 0.0% 已实测**） |
| 判决逻辑 | 与 `bot/speedc156.py::SpeedC156._want_claim` **逐行等价**（照抄 12 行 + **等价性测试**防漂移） |
| 兜底 | 模型缺失 / 异常 ⇒ 退回纯 `SpeedValue`（零行为变化） |
| 模型 | `var/c121_meld_net.pt`（**起役前必须断言存在**，否则本臂 = no-op） |
| 就绪 | 注册 ✓；`arm_smoke` 四档 ✓（p50 ≤11.2ms / max ≤295.8ms）；契约测试 5/5 ✓ |

## 2. 判据（预登记，役中不改）

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| **主** | 复盘**和牌率/房** | **z ≥ 1.50**，各臂 ≥80 房有复盘、覆盖 ≥70% | `var/_gate2.py` |
| 副 | 复盘**番/房** | z ≥ 1.50 且与主**同号** | 同上 |
| **机制（本役核心）** | **副露/房** 与 **索取率** | 副露/房 **↑**（候选 > 基线）、索取率提升 ≈ **+7pp** 量级；`>1s 超窗 = 0` | `_gate2.py` 机制列 / `var/_submit_latency_audit.py` |
| 护栏 1 | ledger **第一率** | 候选 ≥ 基线 | `var/_first_rate_readout.py` |
| 护栏 2 | ledger **净分/房** | 同号（不阻塞） | `tools/ab_readout.py` |
| 熔断 | `var/_breaker_watch.py` | 两档不触发 | — |

## 3. 事前预测（先写下来）

**量化预期（按 R1176 的构成差直接折算，公式写明、便于事后对账）**：

- 现状（`speedvaluemeld` 的离线作用面）：索取率 **67.6% → 74.7%**（+7.1pp，仅候选收 294 / 仅基线收 0）；
  若该 +10.4% 的索取增量按比例落到真机 ⇒ 副露/轮 约 **0.87 → 0.95**，
  **2+ 副露态占比 17.3% → 约 19%**（几何量级估计，非精确换算）；
- **听牌率**：2+ 副露态的听牌率 75.4%、门清 41.1%（R1176，我方）
  ⇒ 期望增益 ≈ (19% − 17.3%) × (75.4% − 41.1%) ≈ **+0.5 ~ +0.9pp**；
- **和牌率**（听牌→胡 兑现约 45%）⇒ 期望 **+0.2 ~ +0.5pp**；
- **净分/房** ⇒ 粗估 **+10 ~ +25**（若机制生效且无"副露换番"的净损失）；
- **番/胡 略 ↓**（副露换速度的固有代价，属预期内）；
- **最可能结果**：机制读数干净（副露/房、索取率明显上升），主端点落在 **z < 1.5 的灰区或小幅为正**。


- **机制**：副露/房 ↑、**番/胡 略 ↓**（副露换速度的固有代价）、听牌巡目 ↓；
- **主端点**：**不预测正效应**。**同类风险已知**：§Q-2"碰专属放宽到过天花板"、§V"副露门槛无钱可捡"——
  它们测的是**单纯放宽**；本臂是"放宽 + 学习筛选"，**机制不同但风险同类**；
- **量级（若走正向）**：和牌率 +0.5~+1.5pp，净分/房 +10~30；
- **最可能的结果**：机制读数干净（副露/房 明显上升），主端点**灰区**。

## 4. 判词分支

- **B1**：主端点 z ≥ 1.50 且机制达标 ⇒ **采用**；随后把"选择性副露"写入后续所有臂的底；
- **B2**：机制达标但主端点不达 ⇒ **记录"机制成立、主端点未证实"**，**不再在副露门槛上继续调剂量**（防重复交学费）；
- **B3**：机制**不达标**（副露/房没升或超窗 >0）⇒ 作废并回查（模型是否加载 / 视图字段）。

## 5. 起役清单

```powershell
python -X utf8 -c "import run_bot; p=run_bot.STRATEGY_FACTORIES['speedvaluemeld'](); assert p._mnet is not None, '副露网缺失！'; print('model ok')"
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/arm_smoke.py --class bot.speedvaluemeld.SpeedValueMeld --kind window --n 400
python -X utf8 tools/ab_ctl.py start speedvalue,speedvaluemeld 1 --bundles=speedvalue "--started=<切换时刻>"
```

## 6. 已知风险

1. **同类风险重演**：claim 层放宽历史上不赚钱；本役是它在"学习筛选"版本下的**第一次干净判决**；
2. **跨阶段臂**：本臂只动 window 层（draw 0.0% 已证）⇒ 判词必须**同时**看主端点与机制列，避免"副露多了但没赚"被误读成成功。

---

## 7. 起役前修订（2026-09-24 07:4x，R1247/R1248）：**候选剂量 `claim_p` 0.60 → 0.45**

> **为什么改**：本役**尚未起役** ⇒ 属"起役前一次性登记"，不违反"役中不改阈值"。
> 修订只改**剂量**（同一机制、同一 MRO、同一模型），**主/副端点与判据门槛一个字都不改**。

### 7.1 新证据①：剂量阶梯（真机窗口语料，62,258 窗口 / 可索取 4,160）

`tools/offline_replay.py --files 300 --base speedvalue --cand <档> --phase window`（本次实测）：

| 档（`claim_p`） | 收副露 | 仅候选收 | 仅基线收 | 副露量倍数 | 索取率（/可索取） |
|---|---|---|---|---|---|
| 基线 `speedvalue` | 2,818 | — | — | 1.00 | 67.7% |
| **0.60（原登记）** | 3,114 | +296 | 0 | **1.11×**（+10.5%） | 74.9% |
| 0.50 | 3,300 | +482 | 0 | 1.17×（+17.1%） | 79.3% |
| **0.45（本次改为候选）** | 3,410 | +592 | 0 | **1.21×（+21.0%）** | 82.0% |
| 0.40 | 3,519 | +701 | 0 | 1.25×（+24.9%） | 84.6% |
| 0.35 | 3,611 | +793 | 0 | 1.28×（+28.1%） | 86.8% |

- **每一档都是严格超集**（`仅基线收 = 0`）、引擎/异常 = 0；单调剂量曲线干净 ✓
- 新增注册（**只改注册表，无新文件**）：`speedvaluemeldp50/p45/p40/p35`（=`SpeedValueMeld(claim_p=…)`）；
  延迟复测（800 真机窗口、BelowNormal）：**p45 max 0.81ms、>1500ms = 0** ⇒ 不影响"超窗 = 0"护栏。

### 7.2 新证据②：同房内"副露量 → 分"（房间固定效应）

`var/_meld_value.py`（本役 87 房、348 个 (房,座位) 单元、组内去均值）：

- **每 +0.1 副露/轮 ⇒ 分/轮 +0.530（t = +3.24）**（R²=0.029）
- 同房头对头（§V.34）：我们 **副露/轮 比同桌少 ~0.05**（0.297 vs 0.357）
  ⇒ 折算 **≈ −0.27 分/轮 ≈ −21 分/房**，与"强手房 −27.7"同量级。
- ⚠ **这是关联不是因果**（副露多可能是强 bot 的**标志**，也可能是领先者才敢副露）；
  但它**同房同对手**、并且与 R1011/R1013 的 **P(爆头|副露数)=19.7%→71.4%**（爆头 ×2）机制吻合。

### 7.3 结论：原登记的 0.60 是**欠剂量**

原档只补 **+10.5%** 副露量，而实测缺口是 **~+20%**（表内相对量）⇒ **只补一半**，
且 §3 自己预测"和牌率 +0.2~0.5pp、最可能灰区" ⇒ 按 §G 的教训（**别让预测效应落在仪器分辨率之下**），
**这一役有很大概率白跑**。改为 **0.45**（= 补 +21.0%，**刚好对齐同桌副露量**）⇒ 机制效应翻倍，
且仍是"学习网认为 ≥45% 把握"的窗口，不是裸放宽。

### 7.4 判词分支（按本次剂量**加强**，不是放宽）

- **B1**：主端点 z ≥ 1.50 且机制达标 ⇒ 采用（后续所有臂以"选择性副露"为底）；
- **B2′**：机制达标（副露/房 ≥ **+15%**）但主端点不达 ⇒ **本轴正式结案**（因为此次已在**对齐同桌的剂量**上测过）
  —— 这正是原 §4 B2"不再在副露门槛上调剂量"的**强版**：**不再有第二次机会，也不追加剂量档**；
- **B3′**：机制不达标（副露/房未升）⇒ 回查（模型是否加载 / 视图字段）后作废；
- **入口门失败兜底**：若 `speedvaluemeldp45` 任一入口门（足迹/冒烟/延迟/模型在场）不过 ⇒ **退回 `speedvaluemeld`（0.60）**，
  并按原 §4 执行，**不临时造新档**。

### 7.5 起役清单（只把候选名换掉）

```powershell
python -X utf8 -c "import run_bot; p=run_bot.STRATEGY_FACTORIES['speedvaluemeldp45'](); assert p._mnet is not None, '副露网缺失！'; print('model ok claim_p=', p.claim_p)"
python -X utf8 tools/offline_replay.py --files 300 --base speedvalue --cand speedvaluemeldp45 --phase window --lowprio   # 期望 1.21x / 仅基线收 0
python -X utf8 tools/offline_replay.py --files 300 --base speedvalue --cand speedvaluemeldp45 --phase draw   --lowprio   # 期望 0.0%
python -X utf8 var/_switch_campaign.py --baseline speedvalue --candidate speedvaluemeldp45        # dry-run
python -X utf8 var/_switch_campaign.py --baseline speedvalue --candidate speedvaluemeldp45 --go
```

---

## 7. 追加护栏（2026-09-24 09:1x 起役前预登记，R1287）：**强手房一致性**

**为什么**：正式赛（决赛）的对手密度 ≈ **强手房**（top32 在场），而 `_gate2` 主端点是**混合口径**。
⇒ 本役除主端点外，**必须**额外报**分层读数**：
`python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>`
（① **强手房 分/房** ② **强手房 第1率**），以及同席机制口径
`python -X utf8 var/_lowprio_run.py -- python -X utf8 var/_seat_h2h.py --since "<役起点>" --by-arm --top 32`
（**听牌率 / 赢分/轮**，与 `hu_gap_split --by-arm` 互补）。

**判定规则（写死）**
1. 主端点 z ≥ 1.50 且 **强手房两列均不劣于基线** ⇒ **采用**；
2. 主端点 z ≥ 1.50 但 **强手房两列任一显著劣于基线** ⇒ 记 **B2（主端点成立但强手房未确认）**：
   **不作为正式赛选臂**（正式赛几乎全是强手房），继续按役序测下一轴；
3. 强手房样本不足（< 15 房/臂）⇒ 只记录，**不据此翻转**主端点结论。

**口径注**：本节是**起役前追加**（非役中改阈值）；主端点与 z 阈值**一概不变**。
（役 2 的判词仍按它自己的原预登记口径，不适用本节。）

**「显著劣于基线」的钉死定义（R1288）**：强手房分/房的 `候选 − 基线` **< −2×SE合并**
（`SE合并 = sqrt(SE_候选² + SE_基线²)`，各自用强手房子样本的 `SD/√n`）⇒ 判为显著劣化；
若任一侧强手房 < 15 房 ⇒ **不判**（只记录）。本节与 §V.66 的噪声口径一致。

---

## 8. 追加读数列（2026-09-24 09:2x 起役前预登记，R1297）：**决赛相似层（≥2 名 top32）**

**为什么**：实测我方 `分/房` 随**同桌 top32 人数**单调下降 ——
0 个：**+61.0**（n=28）→ 1 个：**−19.4**（n=37）→ 2 个：**−55.6**（n=22）→ **3 个：−95.1（n=8）**；
第 1 率同向（28.6% → 21.6% → 18.2% → **0.0%**）。
而 9/23 三测的**赛事累计**是 `rank=7 / total_score=-277 / games_played=160`（≈ **−92~−138 分/房**，按 2~3 房折算）
⇒ 与我们"3 个 top32"层（−95）**同量级**。
⚠ **勘误（R1298）**：那句"决赛 −138.5/房"的**归因是错的** —— 决赛阶段（21:49–21:55）只有重复的"已确认出席本阶段"，
`status=closed` 时**并没有打到房**（最后一条"本场结束"是 21:45:07，属前一阶段）
⇒ 该数字是**赛事累计**，不是"那一场决赛"的难度。
本节的依据是**难度曲线本身**（+61 → −19 → −56 → −95），与决赛数字无关。

**新增读数（必报，不替代主端点）**
```powershell
# 在 §V.70 的强手房（≥1）之外，再报"≥2 名 top32"层：房数 / 分/房 / 第1率
python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>     # 现有工具（分层列）
```
判定：若该层**样本 ≥ 15 房/臂**，且 `候选 − 基线 < −2×SE合并` ⇒ **按 §V.70 的 B2 处理**（不采用进正式赛）；
样本 < 15 房/臂 ⇒ **只记录**（当前 3 强手层只有 ~5% 的房，约 4 房/臂/役 ⇒ 不可判，必须靠 ≥2 层与 强手房 层叠起来看）。

**参考量级**：本役 95 房里，0/1/2/3 名强手的分布是 **28/37/22/8** ⇒ ≥2 强手占 **31.6%** ⇒ 80 房/臂时可预期 ~25 房/臂，**足够进入判定**。


# 役 7 预登记（2026-09-24）：**赢分大小**轴 = `SpeedValue` + 爆头可达性值函数 V（`speedvaluebaotouv5`）

> 判据**起役前一次性登记**，役中不改。
>
> **⚠ 编号与执行顺序**：文件名沿用**历史编号**（`campaign7`），**实际执行役序见 `next-month-plan-20260923.md` §V.42** ——
> 本节所登记的这一轴排在 **役 4**（在 `speedvaluebc` 之后、`speedvaluemeldp45` 之前）。
>
> **前置依赖**：本臂挂在 `speedvalue` 之上 ⇒ **需役 2 判词为「采用 speedvalue」**。
> 若役 2 判「不采用」，本役需先造 `c151` 族底的双胞胎（见 §6 风险 4），**不在本役范围内硬上**。

## 0. 目标缺口（为什么打这一轴）

§V.36 的同席对账把 `分/轮` 缺口按 `胡率 × 赢分/胡` 拆开：

| | 胡率 | 赢分/胡 | 赢分/轮 |
|---|---|---|---|
| 我方 | 24.74% | **17.95** | +4.44 |
| top32 | 26.70% | **19.78** | +5.28 |

> 缺口 **+0.84/轮 ＝ 胡率 +0.39（46%）＋ 赢分大小 +0.45（54%）**

⇒ 现有四个排队臂（`speedvaluebc` / `speedvaluemeldp45` / `speedvaluerank` / `speedvalueplain`）
**全部只打"听牌率→胡率"那 46%**；**"赢得大"这 54% 此前无候选**。

**机制侧证据**：`hu_gap_split` 同房口径 **爆头态率 5.6%（我方） vs 7.5%（top32）**——**他们进入爆头态的机会多 34%**
⇒ 这是**决策层**差异（不是运气），正是值函数要修的东西。
R559：`bot/baotou_value.py` 的 V 对"该局以爆头胡"的**局级 AUC = 0.771**、分位极差 12×。
（§V.38 判死的是**手写**爆头规则 0.2%/0.3~0.4%/**0.08%**，与本轴不同路。）

## 1. 臂（单变量证据）

| 项 | 内容 |
|---|---|
| 类 | `bot/speedvaluebaotouv.SpeedValueBaotouV5`（**只新增文件**，R1255） |
| MRO | `SpeedValueBaotouV5 → SpeedValue → SpeedC151 → …`（**同一族底**） |
| 唯一分叉点 | **只覆盖 `value_of`**：`V_new = V_speedvalue + W_TILES × W_UKEIRE[s] × V(弃牌后状态)` |
| `_pick_discard` / 胡 / 杠 / 抓打圈 / 索取门控 | 全部沿用 `SpeedValue`（**一字不改**） |
| 模型 | `var/baotou_v1.pt`（**起役前必须断言可加载**，否则本臂退化为纯 `speedvalue` = no-op） |
| 剂量 | **`W_TILES = 5`（最小剂量）**；实测 5/10/20 三档足迹 **13.5% / 13.5% / 13.6%** ⇒ 5 已饱和 |
| 足迹（保真口径） | 60 文件 4,330 决策 **13.5%**；**150 文件 10,699 决策 14.8%**；**action 差异 0**、异常 0 |
| 延迟 | p50 0.0ms / p95 9.4ms / **max 14.9ms** / **>1s = 0** |
| **方向核对**（R1257） | 改动 530 手决策中，候选那一手 V 更高 = **100.0%**（均值 +0.0227）；**留白/弃白改动 = 0** |

## 2. 判据（预登记，役中不改）

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| **主** | 复盘**和牌率/房** | **z ≥ 1.50**，各臂 ≥80 房有复盘、覆盖 ≥70% | `var/_gate2.py` |
| 副（**本役特有：必须为副**） | 复盘**番/房** | **候选 > 基线**（本轴的全部理由就是"赢得大"） | 同上 |
| **机制（本役核心）** | **爆头/胡** 与 **赢分/胡** | 两者**均上升**；同房口径 + | `var/_seat_h2h.py`、`tools/hu_gap_split.py` |
| 护栏 1（**本役特有**） | **胡率** | **不得低于基线 1σ**（追爆头换速度是本轴的最大风险） | `tools/hu_gap_split.py --by-arm` |
| 护栏 2 | ledger 第一率 | 候选 ≥ 基线 | `var/_first_rate_readout.py` |
| 护栏 3 | ledger 净分/房 | 同号（不阻塞） | `tools/ab_readout.py` |
| 熔断 | `var/_breaker_watch.py` | 两档不触发 | — |

> **判词纪律**：本役**不允许只看主端点**。若"主端点不达但番/房与赢分/胡明显上升"，记 **B2（机制成立、主端点未证实）**，
> 并在**正式赛选臂**时把它作为"×2 番机器"的备选（§V.29 的选臂规则里已含"强手房分/房"口径）。

## 3. 事前预测（先写下来）

- **机制**：**爆头/胡 上升 2~5pp**（现 22.5% vs top32 28.7%）、**番/胡 +0.03~0.08**（现 1.31 vs 1.42）；
- **代价**：**听牌率/胡率可能下降 0~1pp**（V 会牺牲部分速度和牌去换爆头态）；
- **净效应**：**分/轮 +0.1~0.3**（即 +8~24 分/房，视它能把 54% 的那一半补多少）；
- **主端点**：**不预测正效应**（和牌率可能持平或略降）；
- **最可能结果**：机制列明显（爆头/番 上升）、主端点灰区或小幅负 —— **这正是"两半必须一起看"的原因**。

## 4. 判词分支

- **B1**：主端点 z ≥ 1.50 且机制（爆头/胡 + 番/胡 + 赢分/胡）上升 ⇒ **采用**；
- **B2**：机制达标（爆头/胡 ≥ +2pp 且番/胡上升）但主端点不达 ⇒ **记为"赢分大小轴成立、和牌率未证实"**，
  保留为正式赛备选臂（与 §V.29 的选臂口径并行比较），**不再在同一轴上调 W_TILES**（剂量已饱和）；
- **B3**：机制不达标（爆头/胡 没升）或**胡率跌破基线 1σ** ⇒ 作废，回 `speedvalue`；
  若属后者，说明"追爆头在这个规则下净负"，把 V 族**整体结案**。

## 5. 起役清单

```powershell
python -X utf8 -c "import run_bot; p=run_bot.STRATEGY_FACTORIES['speedvaluebaotouv5'](); import bot.baotou_value as bv; bv.load(); print('V 模型 ok, W_TILES=', p.W_TILES)"
python -X utf8 var/_multi_footprint.py --base speedvalue --cands speedvaluebaotouv5 --files 150   # 期望 ≈14.8% / action 0
python -X utf8 var/_m10_latency_gate.py --arms speedvalue,speedvaluebaotouv5 --calls 60           # >1s 必须为 0
python -X utf8 var/_switch_campaign.py --baseline speedvalue --candidate speedvaluebaotouv5        # dry-run
python -X utf8 var/_switch_campaign.py --baseline speedvalue --candidate speedvaluebaotouv5 --go
```

## 6. 已知风险

1. **V 是预测器不是因果**：AUC 0.771 只说明它能排序"哪手更容易走到爆头"，不保证按它走就更好；
2. **换速度的风险**（最大）：我们本来就少 3.7pp 听牌率 ⇒ 若胡率掉 >1pp，可能净负；
3. **模型在场依赖**：`var/baotou_v1.pt` 缺失/加载失败 ⇒ 本臂静默退化（起役前必须断言）；
4. **族底依赖**：本臂挂在 `speedvalue`；若役 2 判"不采用"，需另造 `c151` 族底双胞胎（**本役不硬上**）；
5. **剂量已饱和**：5/10/20 足迹 13.5/13.5/13.6% ⇒ 本役**不设剂量阶梯**，一判定论。

---

## 7. 追加护栏（2026-09-24 09:1x 起役前预登记，R1287）：**强手房一致性**

**为什么**：正式赛（决赛）的对手密度 ≈ **强手房**（top32 在场），而 `_gate2` 主端点是**混合口径**。
⇒ 本役除主端点外，**必须**额外报**分层读数**：
`python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>`
（① **强手房 分/房** ② **强手房 第1率**），以及同席机制口径
`python -X utf8 var/_lowprio_run.py -- python -X utf8 var/_seat_h2h.py --since "<役起点>" --by-arm --top 32`
（**听牌率 / 赢分/轮**，与 `hu_gap_split --by-arm` 互补）。

**判定规则（写死）**
1. 主端点 z ≥ 1.50 且 **强手房两列均不劣于基线** ⇒ **采用**；
2. 主端点 z ≥ 1.50 但 **强手房两列任一显著劣于基线** ⇒ 记 **B2（主端点成立但强手房未确认）**：
   **不作为正式赛选臂**（正式赛几乎全是强手房），继续按役序测下一轴；
3. 强手房样本不足（< 15 房/臂）⇒ 只记录，**不据此翻转**主端点结论。

**口径注**：本节是**起役前追加**（非役中改阈值）；主端点与 z 阈值**一概不变**。
（役 2 的判词仍按它自己的原预登记口径，不适用本节。）

**「显著劣于基线」的钉死定义（R1288）**：强手房分/房的 `候选 − 基线` **< −2×SE合并**
（`SE合并 = sqrt(SE_候选² + SE_基线²)`，各自用强手房子样本的 `SD/√n`）⇒ 判为显著劣化；
若任一侧强手房 < 15 房 ⇒ **不判**（只记录）。本节与 §V.66 的噪声口径一致。

---

## 8. 追加读数列（2026-09-24 09:2x 起役前预登记，R1297）：**决赛相似层（≥2 名 top32）**

**为什么**：实测我方 `分/房` 随**同桌 top32 人数**单调下降 ——
0 个：**+61.0**（n=28）→ 1 个：**−19.4**（n=37）→ 2 个：**−55.6**（n=22）→ **3 个：−95.1（n=8）**；
第 1 率同向（28.6% → 21.6% → 18.2% → **0.0%**）。
而 9/23 三测的**赛事累计**是 `rank=7 / total_score=-277 / games_played=160`（≈ **−92~−138 分/房**，按 2~3 房折算）
⇒ 与我们"3 个 top32"层（−95）**同量级**。
⚠ **勘误（R1298）**：那句"决赛 −138.5/房"的**归因是错的** —— 决赛阶段（21:49–21:55）只有重复的"已确认出席本阶段"，
`status=closed` 时**并没有打到房**（最后一条"本场结束"是 21:45:07，属前一阶段）
⇒ 该数字是**赛事累计**，不是"那一场决赛"的难度。
本节的依据是**难度曲线本身**（+61 → −19 → −56 → −95），与决赛数字无关。

**新增读数（必报，不替代主端点）**
```powershell
# 在 §V.70 的强手房（≥1）之外，再报"≥2 名 top32"层：房数 / 分/房 / 第1率
python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>     # 现有工具（分层列）
```
判定：若该层**样本 ≥ 15 房/臂**，且 `候选 − 基线 < −2×SE合并` ⇒ **按 §V.70 的 B2 处理**（不采用进正式赛）；
样本 < 15 房/臂 ⇒ **只记录**（当前 3 强手层只有 ~5% 的房，约 4 房/臂/役 ⇒ 不可判，必须靠 ≥2 层与 强手房 层叠起来看）。

**参考量级**：本役 95 房里，0/1/2/3 名强手的分布是 **28/37/22/8** ⇒ ≥2 强手占 **31.6%** ⇒ 80 房/臂时可预期 ~25 房/臂，**足够进入判定**。


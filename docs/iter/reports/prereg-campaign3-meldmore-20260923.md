# 役 3 预登记（2026-09-23）：有界的副露放宽 `SpeedMeldMore0`

> **[SUPERSEDED 已作废]** 本版役 3（副露放宽 `SpeedMeldMore0`）已实测到天花板（§Q-2 归档），并被 `next-month-plan-20260923.md` §V.9/§V.16 的役序定稿取代。**不在役序内，仅留档**。

> 状态：**候选已量足迹、未排批**。等役 2（c151 vs speedvalue）判词出来后，按
> `next-month-plan-20260923.md` §B 分支决定是否插入。**本役只为真机设计**。

## 1. 为什么是这条轴

| 证据 | 数值 | 出处 |
|---|---|---|
| 顶级队列副露/局 | **1.278** | `_cohort_profile.py`（53 房 c151，同房配对） |
| 我们副露/局 | 1.092 | 同上 |
| 缺口 | **+0.186/局 = +14.9/房** | 同上 |
| 我们自己的历史因果证据 | c151 把副露/房从 67.7 → 87.3（+0.24/局）⇒ **+53.8 净胜/房（t=1.99）** | `_replay_endpoint.py` + ledger |
| 碰的决策率 | 我们 **79.0%** vs 顶级实现率 71.0%（其决策率估 ≈92%） | §6 of diagnostics |
| 我们臂在真机上的忠实度 | 生产 == 离线，**分歧 0.0%** | `_prod_arm_divergence.py` |

## 2. 候选与足迹（真机状态，20 房，3518 个合法窗口）

| 臂 | 碰决策率 | 吃决策率 | 相对 c151 新增 | 决策 max/p99 ms |
|---|---|---|---|---|
| `SpeedC151`（基线） | 79.0% | 58.5% | — | 323.7 / 164.7 |
| **`SpeedMeldMore0`** | **80.8%** | **73.6%** | **+24 碰 / +344 吃（20 房）** | 11.8 / 3.7 |
| `SpeedMeldMore2` | 82.6% | 74.5% | +46 / +365 | 11.6 / 3.5 |
| `SpeedMeldTol4` | 82.0% | 75.6% | +39 / +391 | 2.9 / 1.9 |

- 选择 **`SpeedMeldMore0`（TOL=0）**：语义是"**等向听且真进张不降**"才接受，**绝不接受向听变差、
  绝不接受进张变少** ⇒ 下界有界；足迹 +18.4 决策/房 ⇒ 折算实现后 ≈ +14/房，**刚好补齐 15/房 的缺口**。
- 组合方式：`class Speed<Campaign2Winner>MeldMore0(<Campaign2Winner>, SpeedMeldMore0)`（MRO），
  或直接把 c151 换成役 2 胜者后按同样 MRO 组合（**新建文件，不改既有**）。

## 3. 判据（预登记，先于数据）

- **主端点**：复盘**和牌率/房**（`var/_replay_endpoint.py`），要求 **z≥1.50**，各臂 **≥80 房**，
  且复盘覆盖 ≥70% 房；
- **副端点**：ledger 净分/房 **t≥1.50**（各 ≥150 房）——若主端点先到而副端点未到，
  **只做"条件采用"**并继续攒房；
- **机制核对（必须同号）**：副露/房 必须上升（`_replay_endpoint.py` 的 `melds`），
  且**和牌率不得下降**（这是本役的直接机制）；
- **护栏**：第一率（候选 ≥ 基线）；`_breaker_watch.py` 两档不触发；
- **回滚**：`var/_keeper_strategy.txt` 指回役 2 胜者。

## 4. 已知风险（明写）

- **自对弈台在这条轴上不可信**：台子本身副露 **+26%**（R1008），而 R977 的"放松副露门槛
  （tol）"是在**台子**上测出的 **z=−4.51**。本役的全部理由正是"台子不能判这条轴"，
  但这同时意味着**我们只有一个不可信的负面证据 + 一个真机正相关证据** —— 所以用
  **有界版（More0）+ 和牌率主端点 + 小房数**，把下行限制住。
- 若 More0 真机为负：立刻回退，并转测"只放宽碰、不动吃"的分支（碰的差值更干净）。

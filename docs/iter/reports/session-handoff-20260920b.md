# 交接件（第二份）—— 2026-09-20 下半场（R698 ~ R732）

> **为什么另起一份**：上一份 `session-handoff-20260920.md` 已经追加到 §17.9，正文 §0~§16 里有多处**已被更正的结论**
> （§4c-66 的"0 对持平"、R686 的分层、"兑现率 −3.6pp"、路线 B 的先验…）。
> 这正是项目纪律 §9.12 说的"边做边追加的长文档必须带更正索引"的病。
> **本文是"合并后的当前事实"，不含历史分层**；要追过程再回读 journal 与上一份交接件。

## 0. 一句话

**生产/基线 = `speedtugc`；2 臂战役 `speedtugc` vs `speedc151` 正在跑（14:54:07 起）**；
本会话把"差距在哪、能不能动"重算了一遍，**结论比上半天更硬也更小**：
差距 = **66% 胡率 + 34% 打点 + 0% 失分侧**（同房对账 ≈90 原始分/房）；
**打点由 c151 在测，胡率只有 c135 一个等价物（已预登记）**。

## 1. 实时状态（2026-09-20 17:2x）

| 项 | 值 |
|---|---|
| A/B | `{"a":"speedtugc","b":"speedc151","rooms":1,"started":"2026-09-20 14:54:07","bundles":["speedc151"]}` |
| 房数 | `speedc151` **4** / `speedtugc` **3**（2 房/臂/小时） |
| 进程 | `_ab_driver` 44716 / `match_super` / `run_bot` 在位；**A/B 模式下没有 keeper** |
| 自愈 | 计划任务 `HangzhouMajAutoHeal` 每 5 分钟 → `_ensure_all.py`（LastTaskResult=0、Missed=0） |
| 执行层 | `preflight` **READY**（v34 一致 / 引擎单测 / **延迟 p99=23ms、0 超窗** / 自愈链） |

**红线**：① 绝不杀 `run_bot`；② 一账号一房（E002）；③ 战役期间不改 `bot/` 热路径；
④ 长战役结束必须做一次生产路径自检；⑤ **绝不手动跑 `var/_rotate_token.py`**。

**ETA**：12 房/臂 ≈ **09-20 21:52**；100 房 ≈ 09-22 17:52；**150 房（判据点）≈ 09-23 18:52**。

## 2. 判据与换算（**当前唯一有效的一套**）

### 2.1 换算表（R720 更正："一房 80 局"不是 50）

| 口径 | 换算 |
|---|---|
| **胡率 → 分/房** | **+1pp ≈ +18.9 原始分/房** |
| **+30 分/房 ⇔** | **胡率 +1.6pp**（或成型 +4pp、打点标签 +8pp） |
| 成型 → 分/房 | +1pp ≈ **+7.5 分/房**（= 兑现率 × 18.9） |
| 打点标签 → 分/房 | 1pp ≈ **3.7 分/房**（由分值通道直接标定） |
| 均净胜/房 | = 4/3 × 原始分/房 |

### 2.2 样本量与功效（SD(房)=205 实测）

| n/臂 | SE(Δ) | Δ=+29 的 t | 要 t=1.50 需 Δ≥ | 要 t=1.96 需 Δ≥ |
|---:|---:|---:|---:|---:|
| 100 | 29.0 | 1.00 | 43.5 | 56.8 |
| **150** | **23.7** | **1.23** | 35.5 | 46.4 |
| 225 | 19.3 | 1.50 | 29.0 | 37.9 |
| 398 | 14.5 | 2.00 | 21.8 | **28.5** |

### 2.3 本役裁决（`docs/预登记-三臂战役-20260920.md` §11.3，已写死）

| 150 房读数 | 动作 |
|---|---|
| t ≥ 1.50 且 三护栏（原始 t / 桌强调整 t≥0 / 第一率≥基线） | **采用 `speedc151`** |
| **0.60 ≤ t < 1.50** | 不判负、不采用 ⇒ **延到 ~225 房/臂**，同阈值重判 |
| t < 0.60 | 按 futility 判 null |
| t ≤ −3.0 | drop-arm |

### 2.4 熔断器（R699 校准）

快档 **6 房 / −300**（原 4 房）；慢档 12 房 / −150。
**触发后先判"是臂还是窗口"**：若同时命中了"机制未生效的臂"⇒ 是窗口效应，不是臂。

## 3. 差距的最终图景（同房对账，R717~R719）

同批 **742 个房**（我们在座、同房同时段）：

| 组 | 原始分/房 | 赢局占比 | 赢均分 | 非赢均分 |
|---|---:|---:|---:|---:|
| **我们** | **−18.6** | 24.2% | 17.9 | −6.1 |
| **top32** | **+80.0** | 27.3% | 19.3 | −6.1 |
| 其他 | −33.8 | 23.4% | 17.4 | −6.0 |

⇒ 差距 **+98.6 原始分/房**；逐局分解（恒等式，残差 0）：**Δp·(w−l) + p·Δw + (1−p)·Δl = +0.744 +0.382 +0.000 = +1.126 分/局**
⇒ **66% 赢频率（胡率）+ 34% 赢分值（打点）+ 0% 失分侧**（≈90.1 分/房，与 98.6 同量级）。
榜单交叉验证：`/portal/api/leaderboard?period=all` 给 top32 中位 **+79.7**、我们 **−19.7**（rank 600、第一率 17.0%）。

## 4. 已关闭的方向（**不要复活**，含本会话新关的）

| 方向 | 死因 | 出处 |
|---|---|---|
| **对手模型 / 行为克隆对手** | 强手在可测维度（安全度）与池内几乎不可分（TV 0.036 vs 零分布 0.017） | R701 |
| **"模仿强者"（路线②）在弃牌轴** | **等价于 c135**：top32 可见口径命中率 93.8~95.3%，c135 **98.1%** | R730 |
| **多步进张（二步）** | top32 相对**原始** max-u1 只偏离 **0.76%** ⇒ 他们的偏离几乎全来自"算进河/副露"，不是多步 | R729 |
| **形状类 / 安全类 / 白数** | R539 逐一否掉（含"白数是退化特征"） | R539 |
| **兑现率轴** | 大样本（2,400 文件）只剩 **−1.3pp ≈10 分/房**，< 30 分/房门槛 | R706 |
| **自摸 / 等张价值 / 保留生牌** | 不可达（无臂动过 ±2σ）；c220 footprint 2.4%、c221 footprint **0.0%** | R672/R694/R696 |
| **副露量本身** | 2 向听反例（副露缺口同量级但成型打平） | R687 |
| **对手强度自适应 / 房内分差自适应** | 无可用旋钮（判据方向反 + 阈值饱和） | R592/R667 |
| **C036 弃胡换爆头 / 防御喂牌** | 已复核为正收益 / 可忽略 | R561/R399 |

## 5. 候选与预登记

| 候选 | 通道 | 状态 | 关键数字 |
|---|---|---|---|
| **`speedc151`** | **打点**（34%） | **在跑**（14:54:07 起） | 机制双通道触发：签名 **−9.9pp**、副露覆盖 **+9.2pp(z=2.48)**；标签：爆头 +8.3pp(1.2σ)、胡值 +2.3 |
| **`speedc135`** | **胡率**（66%） | **已预登记**（`docs/预登记-c135战役-20260920.md`） | 条件成型 **+3.8pp(z=2.2)**；单变量阈值 1.96 ⇒ **398 房/臂 ≈ 8.3 天**；平衡概率 **31%**；⚠ 自己 26 房实测为负；⚠ M=10 max 2.06s 超预算，须先降延迟 |

## 6. 赛前就绪（本会话实测过的项）

| 项 | 状态 |
|---|---|
| `tools/preflight.py`（T-24h） | ✅ **READY**（今天两次实测） |
| `exec_leak_check` / `river_fidelity` / `melds_fidelity` | ✅ 可跑且达标（代打率是"发布后"口径；河 少1.01、副露 −0.291）；两个保真工具已改为 **Idle 自降优先级** |
| `verify_four_way` / `_runbook_lint` / `_runbook_syntax` / **`_runbook_ps_syntax.ps1`** | ✅ 全绿（含 **.ps1 UTF-8 with BOM** 纪律） |
| `var/_mixed_room_check.py` | ✅ **本役零混臂**；历史 4 例混臂房仍需在读数时剔除 |
| `tools/rules_guard.py` | ✅ 通路验证（404 TOURNAMENT_NOT_FOUND 是预期的：**正式赛未发布**） |
| **T-24h 换 scoped token + 规则实测** | ⏳ **唯一剩下的一项**，依赖官方发布 `tid`（`/api/tournaments` 现为 `[]`） |

## 7. 对既有结论的更正索引（本会话）

| 更正 | 内容 |
|---|---|
| **R698** | §4c-66"0 对持平"不复现（−11.7pp, z=−3.0）；并发现 `_by_startshanten.py` **丢掉整个庄位（25% 样本）** |
| **R700** | R686"起手 2 向听打平"不成立：四档落后几乎一样大（+0.041~+0.066 档） |
| **R706** | "兑现率 −3.6pp" → **−1.3pp**；分摊从 48/52 改为 **73/27**（成型/兑现） |
| **R717 → R718 → R719** | 我先报"四成差距未识别"（单位错）⇒ 再报 51/35/13（重复计一次频率效应）⇒ **最终 66/34/0（恒等式）** |
| **R720/R721** | 换算表 **×50 → ×80 局/房**；影响面已封闭（只污染 09-14 两份报告 + R703 推导） |
| **R699** | 熔断器 4→6 房（历史 83 次回放校准；4 房档至今 2 次触发全是误报） |
| **R713/R715/R727** | 本役与 c135 那役的样本量/延期规则（用项目自己的 futility 边界 0.60，不用我自造的 1.20） |
| **R729/R730** | "多步进张"与"模仿强者"两条方向**关闭**（等价于 c135） |
| **R734~R740** | c135 的**延迟阻塞已解**（`bot/speedc135d.py` 40ms + 4 项护栏单测全过；预登记 §1bis）；**三个机制类分析工具的归属口径已与 `ab_readout`/`ab_adopt` 对齐**（排除旧批次尾单房） |
| **R736/R737** | 两条候选**一阶可加**（交互项 +1.4 分/房）⇒ 都兑现时我们 ≈ **+26 原始分/房**，距 top32 入场线只差 ~2 |

## 8. 下一步（含触发条件）

1. **09-20 ≈21:52**：12 房/臂 —— 跑闸门留档（`pair_signature` / `_meld_rate_by_arm` / `_tenpai_by_arm --since 14:54:07`）；预期两项仍过、无新信息；
2. **~100 房**：三轴仪表盘 + 打点标签（看守恒有没有破）；
3. **09-23 ≈18:52**：150 房 ⇒ 按 §2.3 四档判读：
   - 采用 ⇒ 走 `ab_adopt` 三护栏 + 切换生产 + 赛前冻结；
   - 延期 ⇒ 跑到 225 房再判；
   - null/负 ⇒ **在"串行开 c135 那役（8.3 天）"与"冻结 + 赛前工程"之间选**（**需用户拍板**）；
4. **提前止损的三个条件**（只有这三个）：① 熔断且判定为臂的问题；② `ab_adopt` 给 t ≤ −3.0；③ 执行层真实故障（preflight 不再 READY / 代打超限 / 混臂房）；
5. **T-24h**：官方发布 `tid` 后换 scoped token + `rules_guard` 实测。

# 役 6 预登记（2026-09-24）：**纯进张出牌器** = `SpeedValue` 换掉 V 权衡（`speedvalueplain`）

> 判据**起役前一次性登记**，役中不改。前置：役 3（BC）/役 4（副露量）/役 5（教师排序器）之后的候选。

## 0. 目标缺口（为什么打这一轴）

| 证据 | 内容 |
|---|---|
| `bot/speedc217.py` 自述（真机读数） | `speedc135`（带番值倾向的"真进张弃牌"）**单独用最差之一：−0.418 分/席局**，而它的进张命中率 **99%**（过程侧最优）⇒ 怀疑 **"用番值换胡率"净效应为负** |
| R1201 + R1216（两套独立口径） | 我们的**单步最优命中率已追平 top32**（`real_ukeire` 口径 94.7~95.4%；**确定性规则口径偏差仅 1.5~1.8%**）⇒ "明显打错"不是缺口 |
| R1202（共同起点） | **同一中盘状态下我们到 k7 的听牌率仍差 −5~−7pp** ⇒ 差距在**并列取舍的长期价值** |

**机制假设**：`SpeedValue` 用拟合 V（含 番 / 白板 / 活进张）取代纯 max-ukeire。
若"用宽度换番值"在**中盘推进**上是净负，则**换回纯进张**应提升推进率。

## 1. 臂（单变量证据）

| 项 | 内容 |
|---|---|
| 类 | `bot/speedvalueplain.SpeedValuePlain(SpeedValue)`（**只新增文件**；R1215） |
| MRO | `SpeedValuePlain → SpeedValue → SpeedC151 → SpeedC150 → …`（**同一族底**） |
| 唯一分叉点 | **只覆盖 `_pick_discard`**：直接调用 `SpeedTUGC._pick_discard`（= `_best_discard_t`，**纯进张**；该实现不调用 `super()` ⇒ unbound 调用安全）；异常回落 `SpeedValue` |
| 其余层 | 胡 / 自杠 / 抓打圈 / 副露窗口 / 兜底 **100% 沿用** |
| **无模型依赖** | 不需要模型文件 ⇒ **少一个"静默 no-op"失败模式**（仍保留 smoke） |
| 就绪 | 注册 ✓；契约测试 **4/4** ✓；`arm_smoke` draw **p50 0.6ms / max 108.7ms** ✓（四轴最快） |
| 足迹（保真口径） | **22.4%（4,797/21,412）、action 差异 0、异常 0** ✅ |

## 2. 判据（预登记，役中不改）

| 档 | 端点 | 阈值 | 工具 |
|---|---|---|---|
| **主** | 复盘**和牌率/房** | **z ≥ 1.50**，各臂 ≥80 房有复盘、覆盖 ≥70% | `var/_gate2.py` |
| 副 | 复盘**番/房** | z ≥ 1.50 且与主**同号** | 同上 |
| **机制 1** | 决策改动率 | **∈ [10%, 40%]**（保真足迹 22.4%）且 **action 差异 = 0** | `var/_mech_gate.py` |
| **机制 2** | **同状态推进率**（s0=1, k4→k7） | **候选 ≥ 基线** | `var/_advance_from_common_origin.py --by-arm` |
| 护栏 1 | ledger **第一率** | 候选 ≥ 基线 | `var/_first_rate_readout.py` |
| 护栏 2 | ledger **净分/房** | 同号（不阻塞） | `tools/ab_readout.py` |
| 熔断 | `var/_breaker_watch.py` | 两档不触发 | — |

## 3. 事前预测（先写下来）

- **机制 1**：改动率应 ≈ **22.4%**（纯进张 vs V 的分歧）；
- **机制 2**：**应上升**（这是本轴存在的理由）——若**不升**，"番值换胡率是净负"这条假设被证伪；
- **★ 权衡预测（本役特有）**：**主端点若正、副端点（番/房）很可能为负**——
  放弃番值倾向 ⇒ 和牌更多但**每胡番更小**。因此本役**不能只报主端点**，必须同时报副端点与"大赢占比"；
- **净分**：取决于"多胡的收益"能否覆盖"番小的损失"（粗估：若和牌率 +0.5~1.5pp、番/胡 −0.02~−0.05，净效应接近 0，可能小正或小负）；
- **最可能结果**：主端点与副端点**异号**，落在"权衡"而非"改进"。

## 4. 判词分支

- **B1**：主端点 z ≥ 1.50 **且副端点同号** ⇒ **采用**（说明纯进张在两项上都不吃亏，是纯改进）；
- **B2**：**主正副负（或主负副正）** ⇒ **记录为"速度/番权衡"结果**：**不自动采用**，
  需结合护栏（第一率、净分/房）与役 7 的后续设计决定（例如做"部分纯进张"剂量档）；
- **B3**：机制 1 不达标（改动率 ≪10%）⇒ 作废并回查；**机制 2 不升 ⇒ 证伪"番值换胡率净负"假设**。

## 5. 起役清单

```powershell
python -X utf8 tools/ab_ctl.py stop
python -X utf8 tools/arm_smoke.py --class bot.speedvalueplain.SpeedValuePlain --kind draw --n 400
python -X utf8 tools/ab_ctl.py start <基线>,speedvalueplain 1 --bundles=<基线> "--started=<切换时刻>"
# 或：python -X utf8 var/_switch_campaign.py --baseline <基线> --candidate speedvalueplain --go
```
（本臂**无模型依赖**，故起役清单不需要"模型在场断言"。）

## 6. 已知风险

1. **可能牺牲大牌**：纯进张会减少"保住番值形状"的决策 ⇒ 爆头/大赢占比可能下降（§3 的权衡预测已在先）；
2. **历史读数来自旧窗口**：c135 的 −0.418 分/席局是旧基线下测的，本役是它在**现役基线**上的干净对照；
3. **与役 3/5 的关系**：三条轴都在"中盘并列取舍"上，但**权重方向相反**（BC/排序器学教师偏好；本轴偏向纯宽度）
   ⇒ 若本轴与它们**结论相反**，说明"教师偏好"与"宽度"之间的取舍是真问题（这本身是有价值的信息）。

---

## 7. 追加护栏（2026-09-24 09:1x 起役前预登记，R1287）：**强手房一致性**

**为什么**：正式赛（决赛）的对手密度 ≈ **强手房**（top32 在场），而 `_gate2` 主端点是**混合口径**。
⇒ 本役除主端点外，**必须**额外报**分层读数**：
`python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>`
（① **强手房 分/房** ② **强手房 第1率**），以及同席机制口径
`python -X utf8 var/_lowprio_run.py -- python -X utf8 var/_seat_h2h.py --since "<役起点>" --by-arm --top 32`
（**听牌率 / 赢分/轮**，与 `hu_gap_split --by-arm` 互补）。

**判定规则（写死）**
1. 主端点 z ≥ 1.50 且 **强手房两列均不劣于基线** ⇒ **采用**；
2. 主端点 z ≥ 1.50 但 **强手房两列任一显著劣于基线** ⇒ 记 **B2（主端点成立但强手房未确认）**：
   **不作为正式赛选臂**（正式赛几乎全是强手房），继续按役序测下一轴；
3. 强手房样本不足（< 15 房/臂）⇒ 只记录，**不据此翻转**主端点结论。

**口径注**：本节是**起役前追加**（非役中改阈值）；主端点与 z 阈值**一概不变**。
（役 2 的判词仍按它自己的原预登记口径，不适用本节。）

**「显著劣于基线」的钉死定义（R1288）**：强手房分/房的 `候选 − 基线` **< −2×SE合并**
（`SE合并 = sqrt(SE_候选² + SE_基线²)`，各自用强手房子样本的 `SD/√n`）⇒ 判为显著劣化；
若任一侧强手房 < 15 房 ⇒ **不判**（只记录）。本节与 §V.66 的噪声口径一致。

---

## 8. 追加读数列（2026-09-24 09:2x 起役前预登记，R1297）：**决赛相似层（≥2 名 top32）**

**为什么**：实测我方 `分/房` 随**同桌 top32 人数**单调下降 ——
0 个：**+61.0**（n=28）→ 1 个：**−19.4**（n=37）→ 2 个：**−55.6**（n=22）→ **3 个：−95.1（n=8）**；
第 1 率同向（28.6% → 21.6% → 18.2% → **0.0%**）。
而 9/23 三测的**赛事累计**是 `rank=7 / total_score=-277 / games_played=160`（≈ **−92~−138 分/房**，按 2~3 房折算）
⇒ 与我们"3 个 top32"层（−95）**同量级**。
⚠ **勘误（R1298）**：那句"决赛 −138.5/房"的**归因是错的** —— 决赛阶段（21:49–21:55）只有重复的"已确认出席本阶段"，
`status=closed` 时**并没有打到房**（最后一条"本场结束"是 21:45:07，属前一阶段）
⇒ 该数字是**赛事累计**，不是"那一场决赛"的难度。
本节的依据是**难度曲线本身**（+61 → −19 → −56 → −95），与决赛数字无关。

**新增读数（必报，不替代主端点）**
```powershell
# 在 §V.70 的强手房（≥1）之外，再报"≥2 名 top32"层：房数 / 分/房 / 第1率
python -X utf8 var/_verdict_by_elite.py --since "<役起点>" --arms <A>,<B>     # 现有工具（分层列）
```
判定：若该层**样本 ≥ 15 房/臂**，且 `候选 − 基线 < −2×SE合并` ⇒ **按 §V.70 的 B2 处理**（不采用进正式赛）；
样本 < 15 房/臂 ⇒ **只记录**（当前 3 强手层只有 ~5% 的房，约 4 房/臂/役 ⇒ 不可判，必须靠 ≥2 层与 强手房 层叠起来看）。

**参考量级**：本役 95 房里，0/1/2/3 名强手的分布是 **28/37/22/8** ⇒ ≥2 强手占 **31.6%** ⇒ 80 房/臂时可预期 ~25 房/臂，**足够进入判定**。

